{
  "tasks": [
    {
      "id": "9a215a82",
      "cron": "37 4,16 * * *",
      "prompt": "projectsites.dev ADMIN QUALITY loop — deepen every /admin section (perf, security, polish). Real Chromium, seed ps_session from `get-secret E2E_API_KEY` (from ENV, never inline). Verify-before-implement each fire (git log + prod), then advance ONE coherent slice across: (8) PERF — Lighthouse/CWV per admin route; drive LCP≤2.0s / INP≤200ms; fix the worst offender this pass. (9) SECURITY — per section's endpoints: org-scoping (orgId from c.get('orgId'), NEVER a client x-org-id header — no IDOR); flag-off → 404 not 403; no secret/PII in responses or logs; rate limits on public routes. (10) POLISH+DOCS — give ONE section a measurable UX/aesthetic upgrade to AI-vision ≥8/10 (motion, empty-state, keyboard, brand tokens) and update e2e/FEATURES.md + e2e/COVERAGE.yml + the section doc. As you scan each section, VISION-INSPECT the screenshot and use common sense to implement obvious improvements inline. Fan out 4-5 parallel agents on DISTINCT files. Append findings + closures to `apps/project-sites/_LOOP_LEDGER.md`. GUARDRAILS: FE deploy = `cd apps/project-sites/frontend && npm run build:prod && npm run deploy:production` (R2; verify served hash + real-browser); worker via CI (dirty tree blocks local wrangler — commit + push). `git status` first; `git pull --rebase --autostash` before push; commit ONLY your files; NEVER touch .claude/loop.md, .claude/scheduled_tasks.json, apps/project-sites/src/generated/app_js.ts, apps/project-sites/src/services/analytics_events.ts. Verify REAL prod surfaces, never a compile. Approval-required ONLY for DROP TABLE / secret rotation / bulk mutation. One coherent slice per fire; never idle.",
      "createdAt": 1789094564869,
      "lastFiredAt": 1789333620986,
      "recurring": true,
      "createdBySessionId": "85a03033-85a0-4858-a750-2eee19918ecc",
      "createdByPid": 68518,
      "createdByProcStart": "Thu Sep  3 18:00:04 2026"
    },
    {
      "id": "be7021bd",
      "cron": "41 */3 * * *",
      "prompt": "projectsites.dev FULL-FLOW E2E loop — prove the COMPLETE real user flows AROUND the golden path (the ones the FULL JOURNEY loop doesn't deeply cover), end-to-end + HEADLESS on PROD (real local Chromium; seed ps_session from `get-secret E2E_API_KEY` (from ENV, never inline) at the post-sign-in boundary). Verify-before-implement each fire (git log + prod). Read `apps/project-sites/_APP_COMPLETION.md` § B FIRST; advance the highest-value UNCHECKED flow, driving it green + fixing every break at ROOT CAUSE + shipping a durable headless probe under `e2e/admin-verify/` wired into `run-all.mjs`. Flows: (B.1) GUEST ACQUISITION FUNNEL — homepage `/` → search a real business → results render → sign-in CTA → `/create` entry, UNAUTH, real-browser, 0 console errors + operable (the pre-auth funnel a real prospect walks); (B.5) BILLING FULL — embedded checkout mounts → complete a Stripe TEST-mode checkout → webhook → subscription flips `active` → entitlements unlock in /admin/billing (causal display==store); (B.6) EDITOR ROUND-TRIP — open a site in /admin/editor → change a requirement → the live {slug}.projectsites.dev updates → publish (real edit→publish persistence, the canonical loop.md flow); (B.7) AUTH — magic-link request→verify + Google OAuth the headless-verifiable portions. VISION-INSPECT each surface + implement obvious UX/visual/copy/a11y improvements inline (every fix pairs a small value-add). Tick the § B map box + record the proof (commit sha + probe) + recompute % done; append to `apps/project-sites/_LOOP_LEDGER.md`. GUARDRAILS: FE deploy = `cd apps/project-sites/frontend && npm run build:prod && npm run deploy:production` (R2; verify served hash + real-browser); worker via CI (dirty tree blocks local wrangler — commit + push, CI deploys clean main); editor (bolt.diy `app/`) → Pages `bolt-diy`. `git status` first; `git pull --rebase --autostash` before push; commit ONLY your files; NEVER touch .claude/loop.md, .claude/scheduled_tasks.json, apps/project-sites/src/generated/app_js.ts, apps/project-sites/src/services/analytics_events.ts. Verify REAL prod surfaces (curl/Playwright), never a compile. Approval-required ONLY for DROP TABLE / secret rotation / bulk customer mutation. One coherent flow per fire; never idle.",
      "createdAt": 1789094608415,
      "lastFiredAt": 1789352689906,
      "recurring": true,
      "createdBySessionId": "85a03033-85a0-4858-a750-2eee19918ecc",
      "createdByPid": 68518,
      "createdByProcStart": "Thu Sep  3 18:00:04 2026"
    },
    {
      "id": "962f8874",
      "cron": "13 */2 * * *",
      "prompt": "projectsites.dev GOLDEN-JOURNEY DELIVERY — BUILD + DELIVER exactly ONE brand-new website this fire (Brian wants more delivered sites — he only received 1; SPEND the credits, that IS the point, do NOT verify-and-plateau). Verify-before-implement: git status + prod 200 first. Steps in a real flow: (1) pick a REAL local business not already built — use the Lead Scanner (`/api/leads` / lead scanner) or a real Places/OSM business search; (2) create it via the create-from-search flow into Brian's org (org-brian-001, owner brian@megabyte.space) so the completion email reaches him; (3) trigger the AI site-generation build and watch it to `published` (background-poll the workflow, ~6-40min; if the container evicts/errors, retry or pick another business — do NOT give up, drive to a delivered site); (4) VERIFY delivery: `{slug}.projectsites.dev` loads a real business-specific H1 + 0 console errors, AND the completion/delivery email actually sent to brian@megabyte.space (check notifications table / SES send log / Resend). Also confirm the navbar logo + wordmark render TRANSPARENT (the AL-224 fix: transparent logo-icon.png + stripped wordmark, big + compact, no opaque box). Report the live URL + email-delivery status + a one-line quality note. GUARDRAILS: commit ONLY your files; git pull --rebase --autostash before push; NEVER touch .claude/loop.md, .claude/scheduled_tasks.json, apps/project-sites/src/generated/app_js.ts, apps/project-sites/src/services/analytics_events.ts; worker via CI; editor→Pages bolt-diy; template→template repo. Approval-required ONLY for DROP TABLE / secret rotation / bulk customer mutation. One delivered website per fire; never idle.",
      "createdAt": 1789094632169,
      "lastFiredAt": 1789361304593,
      "recurring": true,
      "createdBySessionId": "85a03033-85a0-4858-a750-2eee19918ecc",
      "createdByPid": 68518,
      "createdByProcStart": "Thu Sep  3 18:00:04 2026"
    },
    {
      "id": "80ad2286",
      "cron": "47 */2 * * *",
      "prompt": "projectsites.dev FULL JOURNEY (golden) loop — prove the WHOLE product end-to-end in a real browser (NOT mocked), then EXHAUSTIVELY verify every aspect of the admin dashboard reflects it. Real Chromium; seed ps_session from `get-secret E2E_API_KEY` (from ENV, never inline). Verify-before-implement each fire (git log + prod; never touch files dirty in another session). Advance ONE coherent slice of the golden path, fixing every break at ROOT CAUSE:\n(1) LEAD SCANNER → find a real business (Nominatim/Overpass fallback when Places 403) + generate a claim link;\n(2) CLAIM → open the claim link, confirm it resolves to a real lead/site + adopt;\n(3) BUILD → trigger site-gen from claim/create and watch to `published` (the build IS the authorized acceptance test — SPEND to prove it, never punt);\n(4) VIEW → load the published {slug}.projectsites.dev: 0 console errors, real H1 + content;\n(5) ANALYTICS → visit the site to emit a pageview, then confirm /admin analytics shows a PERFECT, SIMPLE traffic view (causal: I visited → it counts; display == D1 store).\n(6) VERIFY ALL ADMIN ASPECTS — after the build, walk EVERY /admin section in the real browser and confirm each TRULY reflects the new site + honest data. Run `node e2e/admin-verify/contract-sweep.mjs` (all sections PASS) + `reconcile-surfaces.mjs` (display==store, 0 divergences) + the `verify-*-causal.mjs` probes. Then per-section confirm PROPAGATION of the newly-built site: Sites list shows it + correct status; Analytics shows its traffic; Forms shows a submitted contact form; Media shows its generated assets; Snapshots shows the initial snapshot; Audit shows the build events; SEO/site-features reflect it; Domains/hostnames resolve; Editor opens the site incl. the Functions + Data workbench tabs (they must show the site's REAL functions/data, NOT mock); Billing/entitlements, Team, Env-vars, Voice, MCP each render true. Fix any lying-empty / wrong-source / dead control / thin render at root cause + a regression test.\nAs you scan each section VISION-INSPECT the screenshot and implement obvious improvements inline (contrast, spacing, first-result CTA, skeletons, gorgeous polish) — every fix pairs a small value-add.\nALSO make the TEMPLATE better EVERY fire — push reusable wins to github.com/HeyMegabyte/template.projectsites.dev (next build, NO redeploy): per-industry 3D/WebGL hero scenes (three.js/React-Three-Fiber, prefers-reduced-motion + static fallback + LCP-safe) + advanced gorgeous layouts (bento, scroll-driven, glass+grain, cinematic motion), each measurably more beautiful.\nAppend findings + closures to `apps/project-sites/_LOOP_LEDGER.md`. GUARDRAILS: FE deploy = `cd apps/project-sites/frontend && npm run build:prod && npm run deploy:production` (R2; verify served hash + real-browser); worker via CI (dirty tree blocks local wrangler — commit + push); template changes land in the template repo + editor(bolt.diy `app/`) changes deploy to Pages `bolt-diy`. `git status` first; `git pull --rebase --autostash` before push; commit ONLY your files; NEVER touch .claude/loop.md, .claude/scheduled_tasks.json, apps/project-sites/src/generated/app_js.ts, apps/project-sites/src/services/analytics_events.ts. Verify REAL prod surfaces (curl/Playwright), never a compile. Approval-required ONLY for DROP TABLE / secret rotation / bulk mutation. One coherent slice per fire; never idle.",
      "createdAt": 1789165362448,
      "lastFiredAt": 1789355820205,
      "recurring": true,
      "createdBySessionId": "85a03033-85a0-4858-a750-2eee19918ecc",
      "createdByPid": 68518,
      "createdByProcStart": "Thu Sep  3 18:00:04 2026"
    },
    {
      "id": "9d105289",
      "cron": "29 1-23/2 * * *",
      "prompt": "projectsites.dev GENERATED-SITE QUALITY loop — the CORE PRODUCT. Audit DEPLOYED generated sites on PROD (real local Chromium, headless) and make them BEAT the source. Verify-before-implement each fire (git log + prod). Read `apps/project-sites/_APP_COMPLETION.md` § C/§ D FIRST; pick a real published site (`{slug}.projectsites.dev` — e.g. vanta-strength-austin, ironhaus-houston, vantage-digital-studio-portland) and advance the highest-value UNCHECKED quality dimension, fixing at ROOT CAUSE in the TEMPLATE (`github.com/HeyMegabyte/template.projectsites.dev` — lands next build, NO redeploy) or the site-gen prompt / `src/services/build_validators.ts` — NEVER a one-off patch to one deployed site. Dimensions (§ C): (C.1) build_validators invariants live on prod — required files (webmanifest/robots/humans/sitemap/security.txt/favicons), every internal asset resolves, meta title 50-60 / desc 120-156, JSON-LD ≥4 accurate blocks, single H1, no banned-slop; (C.2) CWV — LCP≤2.0s / CLS≤0.05 / INP≤200ms, Lighthouse Perf≥75; (C.3) axe 0 violations @6 breakpoints; (C.4) SEO+GEO — canonical on custom hostname, OG 1200×630 ≤100KB, sitemap+lastmod, robots/security.txt/llms.txt, per-route metadata; (C.5) JSON-LD per route (accurate types, Rich-Results valid); (C.6) PWA — manifest+maskable icons+offline.html+sw; (C.7) BEAT-THE-SOURCE — denser/faster/more-accessible than the original (competitor floor ≥15%). ALSO cover the PLATFORM's OWN public face (§ D): projectsites.dev `/` + blog/changelog/status/privacy/terms render+SEO+a11y, and a real 404 status (no soft-404). Ship a durable prod probe under `e2e/site-quality/` wired into `run-all.mjs`. VISION-INSPECT screenshots + implement obvious improvements inline. Tick the § C/§ D map box + record the proof (commit sha + probe) + recompute % done; append to `apps/project-sites/_LOOP_LEDGER.md`. GUARDRAILS: template changes land in the template repo (git push, next build); FE deploy = `cd apps/project-sites/frontend && npm run build:prod && npm run deploy:production` (R2; verify served hash + real-browser); worker via CI (dirty tree blocks local wrangler — commit + push). `git status` first; `git pull --rebase --autostash` before push; commit ONLY your files; NEVER touch .claude/loop.md, .claude/scheduled_tasks.json, apps/project-sites/src/generated/app_js.ts, apps/project-sites/src/services/analytics_events.ts. Verify REAL prod surfaces (curl/Playwright), never a compile. Approval-required ONLY for DROP TABLE / secret rotation / bulk customer mutation. One dimension per fire; never idle.",
      "createdAt": 1789165375178,
      "lastFiredAt": 1789358340829,
      "recurring": true,
      "createdBySessionId": "85a03033-85a0-4858-a750-2eee19918ecc",
      "createdByPid": 68518,
      "createdByProcStart": "Thu Sep  3 18:00:04 2026"
    },
    {
      "id": "70cb385b",
      "cron": "11 */2 * * *",
      "prompt": "projectsites.dev ADMIN INTEGRITY loop — keep every /admin section rendering true + honest. Real Chromium, seed ps_session from `get-secret E2E_API_KEY` (from ENV, never inline). Verify-before-implement each fire (git log + prod first — concurrent sessions finish work), then advance ONE coherent slice across: (1) RENDER+A11Y — `node e2e/admin-verify/admin-surf-audit.mjs` and again with VIEWPORT=390; fix any console error / error-boundary crash / blank / axe-WCAG / best-practice at ROOT CAUSE + a regression test. (2) TRUTHFUL DATA — `node e2e/admin-verify/reconcile-surfaces.mjs` + a D1 ground-truth COUNT sweep on the real org; fix any lying-empty / wrong-source (display MUST == store). (3) TRUTHFUL MUTATIONS — run the `e2e/admin-verify/verify-*-causal.mjs` probes (write→read-back); fix any lying-success / dropped write. As you scan each section, VISION-INSPECT the screenshot and use common sense to implement obvious UX/visual/copy/a11y improvements inline — every fix pairs a small value-add. Fan out 4-5 parallel agents on DISTINCT files. Append findings + closures to `apps/project-sites/_LOOP_LEDGER.md`. GUARDRAILS: FE deploy = `cd apps/project-sites/frontend && npm run build:prod && npm run deploy:production` (R2; verify served main-*.js hash + real-browser); worker via CI (dirty tree blocks local wrangler — commit + push, CI deploys clean main). `git status` first; `git pull --rebase --autostash` before push; commit ONLY your files; NEVER touch .claude/loop.md, .claude/scheduled_tasks.json, apps/project-sites/src/generated/app_js.ts, apps/project-sites/src/services/analytics_events.ts. Verify REAL prod surfaces (curl/Playwright), never a compile. Approval-required ONLY for DROP TABLE / secret rotation / bulk customer mutation. One coherent slice per fire; never idle.",
      "createdAt": 1789165382721,
      "lastFiredAt": 1789361304593,
      "recurring": true,
      "createdBySessionId": "85a03033-85a0-4858-a750-2eee19918ecc",
      "createdByPid": 68518,
      "createdByProcStart": "Thu Sep  3 18:00:04 2026"
    },
    {
      "id": "8f563ffb",
      "cron": "21 */3 * * *",
      "prompt": "projectsites.dev ADMIN COMPLETENESS loop — push every /admin section to functionally complete. Real Chromium, seed ps_session from `get-secret E2E_API_KEY` (from ENV, never inline). Verify-before-implement each fire (git log + prod), then advance ONE coherent slice across: (4) REAL JOURNEYS — in a real browser run the money flows END-TO-END, NOT mocked: create→build→publish a site; submit a public contact form → confirm it appears in /admin/forms; connect an MCP → confirm active; billing checkout mounts. Fix any break at root cause. (5) EDGE STATES — force empty / loading / error / populated per section; make each graceful (first-result CTA, skeleton, error-boundary + retry, undo). (6) CONTRACT — `node e2e/admin-verify/admin-contract.mjs`; every section routed + nav-reachable + mounted + data-testid; add missing rows; fix drift. (7) EVERY CONTROL REAL — click every button/link/form/modal/shortcut in ONE section; any stub/TODO/dead control → build the real behavior. (8) EDITOR FUNCTIONS + DATA TABS (Brian directive 2026-09-05) — the bolt.diy editor's Functions + Data workbench tabs (`app/components/workbench/FunctionsPanel.tsx`, `app/components/workbench/DataPanel.tsx`) are visually-built but MOCK-DATA (FunctionsPanel renders hardcoded MOCK_ROUTES/MOCK_BINDINGS like /api/booking,/api/contact; DataPanel has a mock mode). VISUALLY INSPECT both → RE-IMAGINE → implement in TOTALITY, connected properly to the REAL project: Functions panel reads the open bolt project's `functions/` folder + real Workers-for-Platforms routes/bindings (per ADR 0035 — functions/ replaced AI Agents); Data panel connects to the site's real D1 tables/bindings (list tables, row counts, browse). Replace ALL mock data + wire every control (create/edit/deploy a function; query/inspect data). Deploy to Pages `bolt-diy` (GLOBAL key); verify in a real browser both tabs show live, connected data with 0 console errors + every control works. NEVER re-delete these tabs — they are WANTED. As you scan each section, VISION-INSPECT the screenshot and use common sense to implement any obvious improvements. Fan out 4-5 parallel agents on DISTINCT files. Append findings + closures to `apps/project-sites/_LOOP_LEDGER.md`. GUARDRAILS: FE deploy = `cd apps/project-sites/frontend && npm run build:prod && npm run deploy:production` (R2; verify served hash + real-browser); worker via CI (dirty tree blocks local wrangler — commit + push, CI deploys clean main); editor (bolt.diy `app/`) → Pages `bolt-diy`. `git status` first; `git pull --rebase --autostash` before push; commit ONLY your files; NEVER touch .claude/loop.md, .claude/scheduled_tasks.json, apps/project-sites/src/generated/app_js.ts, apps/project-sites/src/services/analytics_events.ts. Verify REAL prod surfaces, never a compile. Approval-required ONLY for DROP TABLE / secret rotation / bulk mutation. One coherent slice per fire; never idle.",
      "createdAt": 1789165394592,
      "lastFiredAt": 1789361699800,
      "recurring": true,
      "createdBySessionId": "85a03033-85a0-4858-a750-2eee19918ecc",
      "createdByPid": 68518,
      "createdByProcStart": "Thu Sep  3 18:00:04 2026"
    }
  ]
}
