# ProjectSites.dev — App Completion Map (Definition of Done)

The **terminal target** the scheduled `/loop` crons converge on. "Finish the entire application"
= every box below `[x]` AND verified green on **PROD, headless, real-browser** for ≥2 consecutive
fires of its owning loop.

**Every loop fire MUST:** (1) read this map, (2) advance the highest-value UNCHECKED item **in its
lane**, (3) tick it + record the proof (commit sha + probe name), (4) recompute the lane % + overall
% at the bottom. A lane that is all `[x]` + green ⇒ its loop is **maintenance-only** (a healthy
no-op is correct, not a shortcoming — `convergence-loop-never-stops` healthy-iteration). This map is
the whole-app peer of `_LOOP_LEDGER.md` (per-fire log) and loop.md's `refactor-state.md` (element
coverage matrix).

---

## Scheduled loop roster (who owns what)

| Loop | Cadence | Lane it drives to done |
|---|---|---|
| **ADMIN INTEGRITY** | every 30 min | § A render+a11y · truthful-data · truthful-mutations |
| **ADMIN COMPLETENESS** | every 2 h | § A real-journeys · edge-states · contract · controls · editor tabs |
| **ADMIN QUALITY** | daily | § A perf · security · polish/docs |
| **FULL JOURNEY (golden)** | every 8 h | § B.2 create→build→publish→view→analytics + admin propagation + template |
| **FULL-FLOW E2E (headless prod)** ⟵ NEW | every 6 h | § B.1/B.4–B.7 the flows AROUND the golden path (guest funnel, billing-full, editor round-trip, auth) |
| **GENERATED-SITE QUALITY (prod audit)** ⟵ NEW | every 12 h | § C the core product (deployed sites) + § D platform marketing |

---

## § A — Admin dashboard (Angular SPA) · OWNER: INTEGRITY + COMPLETENESS + QUALITY
**Status: ✅ verified plateau** — 23 sections, 35-probe headless-prod suite (`e2e/admin-verify/run-all.mjs`) green (**35 pass · 0 skip · 0 fail**, AL-128 2026-09-07 — all Browserbase creds auto-resolve via get-secret, zero skips).
- [x] Render+a11y — surf @1280+@390 CLEAN · focus-not-obscured (2.4.11) · target-size (2.5.8) · modal-a11y (open dialogs) — 0 violations
- [x] Truthful data — reconcile-counts, 9 surfaces, display==store (exact active-on-live predicate)
- [x] Truthful mutations — 9 causal write→read-back probes persist (no lying-success / dropped write)
- [x] Completeness — 23/23 sections, no stub/dead-control/soft-404 (completeness-stub-scan) + contract
- [x] Editor Functions + Data workbench tabs — real `functions/` + D1 (AL-004/018/038/060), NOT mock
- [x] Perf / security / polish — ADMIN QUALITY loop (maintenance)

## § B — Full user flows · headless PROD E2E · OWNER: FULL JOURNEY + FULL-FLOW
*The explicit "production (preferably headless) testing of full user flows" mandate. Each ⇒ a durable probe under `e2e/admin-verify/` wired into `run-all.mjs`.*
- [x] **B.1 Guest acquisition funnel** — homepage `/` → search a real business → results render → `/create` entry (UNAUTH, real-browser, 0 console errors + operable). *(verify-guest-funnel.mjs, AL-089 — homepage hero (A/B/C variants) + search + create CTA → /create wizard renders)* **HARDENED (AL-402):** the degraded-search assertion was a lying-green — it passed on the ever-present NAV "Get Started" (regex `/get started/i` over ALL buttons) and its `unavailable` regex didn't even match the live copy ("lookup **is temporarily** unavailable"). So a regression breaking the real in-funnel "Build a custom website" fallback would dead-end a prospect whose business isn't found, yet the probe stayed green. Fix: type a GUARANTEED-no-match business (deterministically exercises the degraded path), match the ACTUAL live degraded copy, and assert the forward CTA is the funnel's OWN manual-build affordance (excluding `<nav>`). PROD-verified: `results=1 unavailable=true manualBuildCta=true`, 0 console errors. Now goes RED if the fallback breaks. **A11Y HARDENED (AL-428):** vision-inspect found the live-search was a WCAG 4.1.3 (Status Messages, AA) gap — the DEGRADED state announced via `aria-live`, but the SUCCESS path (results populated the dropdown) was SILENT to screen readers. Fixed at root in `homepage.component` (a `searchStatusMessage` computed → one always-present `role="status" aria-live="polite"` sr-only region announcing "N result(s) found. Use Tab to review, Enter to select." for whichever search is open). RED-first proven (assertion failed on old prod), then GREEN after R2 deploy (`main-BQSQXUXH.js`): probe reads `status="1 result found…"`. New assertion wired into verify-guest-funnel.mjs so it stays RED if the announcement regresses. **CTA-WIRING HARDENED (AL-451):** the probe reached `/create` via `page.goto()` and never CLICKED the primary hero CTA a real prospect clicks — so a dead "Claim Your Site" (handler throws / lost `(click)` / null `#heroSearch` ViewChild) would strand every prospect on the biggest button on the page while B.1 stayed green (a lying-green like AL-402). Verify-before-implement corrected the assumption: the guest hero CTA (`[data-cta="hero-claim"]` → `goGetStarted()`) intentionally routes to SEARCH (focuses `#heroSearch`), NOT straight to `/create`. Fix: click the CTA like a prospect + assert its real contract — the business-search input receives focus (homepage-first doctrine: click, never goto). Discrimination proven on prod: `activeElement`-on-search is `false` BEFORE any click (a dead CTA stays here) → `true` AFTER clicking `hero-claim` (behavioral, not vacuous). PROD-verified GREEN: `clicked → search focused (funnel entry live)`, 0 console errors. **KEYBOARD-OPERABILITY ROOT-FIXED + PROD-VERIFIED (AL-481):** the search-result dropdown rows were `<button (mousedown)="selectItem(item)">` with NO `(click)`/`(keydown)` — `(mousedown)` fires on pointer-press but a NATIVE button fires `click`/`keydown` (NOT mousedown) on Enter, so a KEYBOARD user who Tabbed to a PRE-BUILT result + pressed Enter (exactly what the aria-live "Use Tab to review, Enter to select" instructs) selected NOTHING — a live WCAG 2.1.1 (Keyboard, Level A) break on the funnel's core "found my site → claim" control + a LYING aria-live promise. Empirically confirmed on prod (mouse→/signin ✓, keyboard-Enter→no-nav ✗). Root fix (`homepage.component.html`, BOTH hero + cta result rows): added `(keydown.enter)="selectItem(item)"` + `(keydown.space)="$event.preventDefault(); selectItem(item)"` (kept `(mousedown)` for the pointer blur-race; NO `(click)` → no mouse double-fire). FE built + R2-deployed + PROD-VERIFIED: mouse→/signin ✓ AND keyboard-Enter→/signin ✓. Durable probe `verify-guest-funnel-keyboard.mjs` (fail-open on 0-results) wired into admin-verify run-all. **CLASS-SWEPT tree-wide (AL-482):** the AL-481 admin-a11y audit triggered a `(mousedown)`-only sweep — admin (`social` mention dropdown) is CLEAN (keyboard-driven via the textarea `onContentKeydown`: Arrow-nav + Enter→insertMention), `billing` mousedowns are cosmetic ripple (real action on click), but 3 guest-funnel selection dropdowns were BARE `<div (mousedown)>` (not even focusable — worse than the homepage button): `/search` result rows + the `/create` wizard business + address suggestions. All 3 root-fixed (`role="button"` + `tabindex="0"` + `(keydown.enter)` + `(keydown.space)`, `(mousedown)` kept for the pointer blur-race); FE built + R2-deployed. PROD-VERIFIED on `/search` (5 rows, role=button, focusable, keyboard-Enter→/signin); the `/create` suggestions can't live-verify (Places-403 → empty on prod) but are the identical deployed pattern. The keyboard probe now also guards `/search`. **WIZARD-PROGRESSION DEEPENED (AL-497):** verify-guest-funnel stopped at "the /create wizard RENDERS" — it never walked the wizard's steps, so a stranded prospect PAST entry (dead step control / stuck step / broken terminal) was invisible. New durable probe `verify-create-wizard.mjs` (wired into admin-verify run-all) drives the real UNAUTH walk: step 1 (Business, fills Name+Address) → step 2 (Details) → step 3 (Brand assets) via the step controls (asserts the ACTIVE step-circle changes) → terminal "Create site" enabled → clicks it as a guest → **bridges to `/signin`** (the conversion terminal; no site created unauth). PROD-VERIFIED ✅ 6/6, 0 console errors — the full prospect walk (land→search→/create→fill 3-step wizard→sign-in-to-build) is proven end-to-end. **6-BREAKPOINT AXE GATE ADDED (AL-689):** the Charter mandates "axe-clean 6bp" for every journey, but NO probe axe-audited the funnel across breakpoints (verify-auth-a11y only covered /signin@1280). New `verify-guest-funnel-a11y.mjs` (wired into run-all) runs axe WCAG 2.2 AA across all 6 breakpoints (375/390/768/1024/1280/1920) on the 3 unauth funnel surfaces (`/` · `/search` · `/create`). **Probe hardened against a transient false-positive** — route-enter opacity fades made axe read mid-animation text as low-contrast (23 phantom color-contrast nodes @350ms → 0 once settled); fixed with a `settle()` that awaits running CSS animations (capped 1500ms so looping animations never hang) before analyzing (validator-precision — a flaky a11y RED is worse than none). **PROD-VERIFIED GREEN** across all 18 surface×breakpoint. **Paired root-fix value-add:** the gate surfaced a duplicate-banner landmark on /search+/create (`<app-header role="banner">` host AND its inner `<header role="banner">` = two nested banner landmarks); removed the redundant host role (kept the semantic `<header>`) → /search+/create now **best-practice-clean too** (best=3→0), deployed `main-PYZH6BXD.js`, re-verified live. **COLD-DEEP-LINK PROBE-RACE ROOT-FIXED + INSTANT-FEEDBACK VALUE-ADD (AL-697):** a fresh prod audit FAILED the funnel — `/create wizard renders h1="" len=4344`. Verify-before-implement (the probe, not the page): `/create` is a heavy LAZY chunk; on a COLD deep-link (bookmark / shared "Claim Your Site" link, not the warm in-funnel click) it downloads+hydrates its H1 in ~3.3s, which raced the probe's blind `waitForTimeout(2500)` → a false "empty H1" FAIL on a funnel that WORKS (the AL-624 validator-precision class). VISION-CONFIRMED the real UX gap behind it: a prospect deep-linking to `/create` saw a **~3.3s near-blank** (only a thin top loading sliver, no heading/skeleton — screenshot). Two-part fix: (1) **probe** → condition-based `waitForFunction` for the real heading + MEASURE the cold-render time (a genuine dead-blank that never renders in 15s is still RED); (2) **real UX value-add** → an instant route-loading skeleton in the app shell (`app.component`), armed on `RouteConfigLoadStart` (lazy-chunk download begin) so any cold deep-link to a heavy route (`/create`, editor, admin) shows branded feedback instead of a dead-blank. The **homepage (`/`) is EXCLUDED** (captured on `NavigationStart`, fallback to `location.pathname`) so its delicate static-hero LCP optimization is untouched — PROVEN on prod: **homepage skeleton-ever-shown=false (no flash), /create skeleton-shown=true then hydrates "Create Your Website"**. +1 Karma test (skeleton OFF by default = homepage-safe + role=status/aria-busy/aria-live a11y contract + removed-on-settle = no CLS) → **1789 green**; deployed `main-GRBAEUDV.js`; funnel probe **8/8 GREEN** (`/create render=336ms skeleton=true`, 0 console errors). The probe now also asserts the instant-feedback affordance so a dead-blank regression goes RED. **RESULTS-RENDER SUB-ACTION now covered (AL-714):** verify-guest-funnel deliberately searches a NONEXISTENT biz (to dodge Places-403 flakiness), so B.1's spec'd "search a real business → RESULTS RENDER → sign-in CTA" sub-actions were never asserted. New `verify-guest-funnel-results.mjs` (wired into admin-verify run-all) proves them RELIABLY via the D1-backed PRE-BUILT-SITE search (parallel to Places, never flaky): it queries `/api/sites/search` to pick a real DELIVERED business dynamically, types it in the hero search, asserts a `[data-testid=search-result][data-result-type=prebuilt]` card RENDERS with the business + "Pre-built" badge, then clicks it UNAUTH → asserts it routes to the **sign-in bridge** (`/signin`, createFunnelNav's signed-out branch toward /create) + the bridge renders operable, 0 console errors. **Value-add:** added `data-testid=search-result` + `data-result-type` to both homepage result-dropdown loops (previously NO testid — a testability gap). **PROD-VERIFIED GREEN 5/5** (`"Pike Place Fish Market"` renders a Pre-built card → click → `/signin`, bodyLen 753, 0 console), `main-QA67CRSK.js`. *(FULL-FLOW E2E, AL-714 — verify-guest-funnel-results)* **"ALREADY-BUILT" PROOF LEG (AL-742):** the headline is "already built and ready to **claim**", yet AL-714 proved only that clicking a PRE-BUILT result routed a guest to `/signin` — they never got to SEE the built site before the sign-in wall (the single most persuasive conversion asset, hidden behind auth; every funnel probe stopped at "reaches sign-in bridge", blind to whether the guest saw any proof). Root fix (frontend, BOTH `homepage` hero + `/search`): the prebuilt result row now carries a **Preview** link (`data-testid=search-result-preview`; pure tested helper `prebuilt-preview.ts` gates it to `type=prebuilt && status=published && valid slug`, 9 Karma cases) that opens the LIVE `{slug}.projectsites.dev` in a new tab (target=_blank + rel=noopener + aria-label; sibling of the claim button — no invalid interactive-in-button nesting); the card's primary click STILL routes to the claim/sign-in funnel (proof-then-commit — verify-guest-funnel-results intact). **PROD-VERIFIED 7/7** (`main-Q5AU6XEC.js`): Stumptown Coffee → Preview href `https://stumptown-coffee-portland.projectsites.dev`, the previewed site is a REAL built page (200 + h1 "Small-batch, Portland-made" + 78KB root — proof is real, not a 404), claim click still → /signin, 0 console errors; the 3 sibling funnel probes still green incl axe-clean 6bp; tap target desktop 94×60 / mobile 39×60 (icon-only, > 24px WCAG 2.5.8). Durable `verify-prebuilt-preview.mjs` wired into run-all. *(FULL-FLOW E2E, AL-742 — verify-prebuilt-preview)*
- [x] **B.2 Authed create→build→publish** — seed session → `/create` real business → build to `published` → `{slug}.projectsites.dev` loads real H1 + 0 console errors. *(FULL JOURNEY — keep green; the build IS the authorized acceptance test)* **/WAITING BUILD-PROGRESS PAGE now gated (AL-667):** B.2 proved the OUTCOME but the surface the owner stares at for the whole ~11–40-min build — the /waiting page (build terminal / STREAMING BUILD THEATER live-log widget AL-666 · per-phase chips · elapsed heartbeat · the "Your site is live!" payoff view) — had no dedicated headless probe. New `e2e/admin-verify/verify-waiting-page.mjs` (wired into run-all) is STATE-AWARE: for a published site it asserts the SUCCESS payoff (message + live-URL + View-Your-Site / Edit-with-AI / Go-to-Dashboard CTAs, the last proven OPERABLE→/admin, not just present per AL-451/481); for a building site it asserts the terminal + chips + heartbeat + log-lines. **PROD 10/10 GREEN**, 0 console errors, axe clean. **Paired UX value-add:** the `{slug}.projectsites.dev` on the success screen was plain text → made a real CLICKABLE link (new tab, keyboard-focusable, focus-ring, hover) — the most natural thing to click now works; asserted live in the probe (`href=https://…projectsites.dev`). FE R2-deployed + PROD-verified.
- [x] **B.3 Analytics causal** — visit published site → `/admin` analytics shows the pageview (display==D1). *(verify-analytics-visit-count-causal)*
- [x] **B.3b Contact-form → /admin/forms** — real submit → appears. *(verify-beacon-funnel-causal)*
- [x] **B.3c MCP connect → active** — *(verify-mcp-connect-causal)*
- [x] **B.4 Billing checkout MOUNTS** — embedded Stripe iframe mounts. *(verify-billing-checkout, local Chromium)*
- [x] **B.5 Billing FULL** — money flow LIVE + SECURE over its headless envelope on prod (AL-111, `verify-billing-full-flow.mjs`, wired into run-all): checkout session create → 200 + `cs_live_`/`pk_live_` · webhook UNSIGNED + BAD-SIG → 401 (unspoofable) · subscription STILL free after spoof (rejected ≠ mutated) · entitlements honestly LOCKED at free · portal → live `billing.stripe.com` URL. **Out of headless scope (prod is Stripe LIVE-mode):** the real-card → `checkout.session.completed` → subscription `active` → entitlement unlock leg can't run headless (a test card is rejected in live mode; a real charge is approval-required) — that leg is covered by the webhook-handler unit test (`billing_webhook_activation.test.ts`, 6/6: plan=paid/status=active + downgrade-to-free). Same shape as B.7's emailed-token caveat. **Vision-inspect fixed a lying-UI** (display==store): the Free card promised "1 custom domain" but the resolver locks free to 0 (`domain_entitlement.ts`) → copy corrected to "Free projectsites.dev subdomain", shipped to R2 + real-browser-verified. **Also root-fixed the R2 deploy verify** (was size-only → blind to a same-size stale `index.html`; now content-verifies HTML entries).*
- [x] **B.6 Editor round-trip** — headless envelope proven (AL-127, `verify-editor-roundtrip.mjs`, wired into run-all): READ leg — the editor bootstraps from `GET /api/sites/by-slug/:slug/chat` → 200 + real bolt chat schema (2 msgs) for an owned site · PUBLISH SECURITY — `POST /api/sites/:id/publish-bolt` is auth-gated (unauth→401), org-scoped (foreign id→404, no cross-org publish), body-slug-ignored (uses OWNED slug — guards publish-body-slug IDOR), input-validated (empty files→400); every rejection fires BEFORE any R2 write (non-mutating probe). **Verify-before-implement caught a false-IDOR:** foreign `/chat` returns 200, but that is BY DESIGN — `/chat`, `/build-context`, `/files` are documented PUBLIC-BY-SLUG (`@auth NONE`; slug + R2 obscurity is the token; they reconstruct what `{slug}.projectsites.dev` already serves publicly; the editor reads them cross-origin with `ACAO:*`). Org-scoping them would BREAK the editor bootstrap → NOT a bug, not "fixed". **Out of headless scope:** the full WebContainer UI drive (editor.projectsites.dev iframe, ~30-60s boot) + a real mutating publish (may invoke the functions container) → Browserbase + $-gated, same shape as B.5's real-card leg. *(FULL-FLOW)*
- [x] **B.7 Auth** — magic-link request→verify-fail-safe + Google OAuth init well-formed (headless-verifiable portions; the emailed-click token consumption needs the peek seam / a human inbox — out of headless scope, but the SEND path is now inbox-proven, see AL-238). *(verify-auth-flow.mjs, AL-094 — 7/7 on prod: request 200+expiry · Zod 400 · verify missing/bad-token 302 fail-safe · Google 302 client_id+redirect_uri+scope+CSRF-state · callback graceful · me→401. **AL-238: 8/8 on the LIVE SES-only worker (post-Resend-removal) + the magic-link SEND causally PROVEN delivering to brian's real INBOX via Gmail MCP; team-invite SES mail also inbox-delivered — the Resend removal broke NO email flow.**)* **UX HARDENED (AL-435):** the `/signin` "Email me a magic link" button is `disabled` until a valid email is entered — but a disabled control can't surface a tooltip, so it read as a silent dead-end (dimmed, unexplained). Added an inline hint ("Enter your email above to get a passwordless magic link.") shown while the button is dimmed + cleared once the email is valid. Prod-verified live (`main-WYKSABYZ.js`, hint renders on empty-email load, 0 console errors); +1 Karma regression (1758 green). Sign-in a11y otherwise confirmed clean (email/password carry `autocomplete` + `label[for]`; OAuth + magic-link satisfy WCAG 3.3.8). **ROUTE-GUARD BOUNDARY now headless-gated + UX value-add (AL-459):** the auth-flow probe covers API-level auth but NOT the client route-guard — the boundary that gates the WHOLE admin. New `verify-auth-guard.mjs` (auto-wired into run-all) drives 5 protected routes (`/admin`, `/admin/billing`, `/admin/team`, `/admin/analytics`, `/admin/editor`) UNAUTH in a real browser + asserts each bounces to `/signin`, `returnUrl` round-trips, and NO admin chrome leaks — **PROD 5/5 GREEN** (a fail-open guard regression → RED). Paired value-add: the `/signin` page ignored the preserved `returnUrl` for messaging (generic "manage your sites" even when bounced from billing) → added a `returnContext()` contextual subtitle ("Sign in to **continue to billing**" mapped per route; generic when arrived directly). Deployed to R2 `main-LRO3P6OK.js`, PROD-verified BOTH paths (bounced→"continue to billing" · direct→"manage your sites"); +2 Karma (17/17). **PROBE HARDENED — race-proof (AL-624):** `verify-auth-guard` intermittently FALSE-flagged a route leak — a fixed `waitForTimeout(2500)` raced the client route-guard's Angular-Router redirect, so on a slow bundle-parse/GC the probe asserted mid-redirect → a phantom "admin leaks to unauth" RED on a SECURITY probe (cry-wolf that could mask a REAL leak). Fixed → condition-based `waitForURL(/\/signin/)` (resolves the instant the guard lands, tolerates slowness; a genuine fail-open still times out → onSignin=false → real leak caught) + a `waitForFunction` settle for the signin UI. PROVEN STABLE **3/3 consecutive runs** (was ~1-in-2 flaky); the actual boundary is SOUND — **5/5 routes bounce unauth→/signin every run**. (`error-recovery` § condition-based-waiting + `validator-precision-discipline` — a flaky security signal is worse than none.) **COMPLETION LEG CLOSED — returnUrl round-trip now HONORED (AL-678):** AL-459 wired the bounce (`/signin?returnUrl=/admin/billing`) + subtitle, but the LANDING never used it — sign-in always dumped the owner on `/admin`. Two stacked root causes (both proven on prod before fixing): (1) the worker OAuth-start read `?redirect_url` while the sign-in page sends `?returnUrl` → the OAuth returnUrl was silently dropped (fixed: `returnUrl ?? redirect_url` on `/api/auth/google`+`/github`, +3 `api_routes` regressions → 24/24); (2) `handleAuthCallback` hardcoded `router.navigate(['/admin'])`, overriding the landed deep path (fixed: navigate to the LANDED `/admin*|/create*|/waiting*` route, token stripped first, +`returned_to` telemetry; race-safe via NON-blocking initial nav, +3 Karma → 7/7). New durable probe `verify-auth-return-to.mjs` (wired into run-all): **RED-first proven** (deep-links landed /admin pre-fix) → **PROD GREEN** (billing+team deep-links honored, session set, token stripped, 0 errors; bare-homepage → /admin fallback). A finished sign-in now deep-links the owner back where they meant to go. **REAL-BROWSER MAGIC-LINK FORM now gated + a lying-confirmation root-fixed (AL-704):** verify-auth-flow proves the magic-link ENDPOINT (API-level fetch), but NO probe drove the real `/signin` FORM a returning owner uses (`pages/auth/sign-in.component` — the Better-Auth page that OWNS /signin; NOT the dead legacy `pages/signin`), so a broken submit / stuck-disabled button / non-rendering "check your inbox" banner would strand every returning user while the API probe stayed green (the AL-402/AL-451 lying-green class). New `verify-signin-magic-link-ui.mjs` (wired into run-all) drives it in real Chromium: email field renders → magic-link button DISABLED on empty email (no doomed click) → ENABLES on a valid email → click → a COHERENT terminal state (inbox banner OR graceful error, never dead/blank) → 0 console errors. **Paired root-fix (lying confirmation, embarrassingly-easy honesty):** the "we sent a magic link to {{ email().trim() }}" banner bound to the LIVE email signal, so editing the field after a send made it claim we mailed an address we never did — frozen to a new `magicSentTo` signal captured at send-success. +1 Karma (19/19 sign-in) + LIVE-VERIFIED on prod (sent to e2e@…, edited field to someone-else@… → banner still truthfully shows e2e@…). **PROD 8/8 GREEN.**
- [x] **B.7b Guest sign-UP (registration)** — the highest-value flow that was ROUTED (`app.routes.ts auth/sign-up` → `SignUpComponent`) but **UNPROBED** — B.7 covered sign-IN/magic-link/OAuth, never the password-registration sibling a brand-new prospect uses. New durable `e2e/admin-verify/verify-signup-flow.mjs` (wired into run-all) drives the COMPLETE real journey in real Chromium, homepage-first click-only: land `/signin` → click **"Create an account"** cross-link → **client-navigates** to `/auth/sign-up` (SPA, no full reload — proven via a `window` marker that dies on reload) → the page renders its full contract (H1 "Create your account" + name/email/password + submit + Google/GitHub OAuth + back-link + the password toggle) → submit **GATED closed** until name + valid email + 8-char password all present → a bad email surfaces "Enter a valid email address." + a <8-char password surfaces "Use at least 8 characters." on blur → the password **show/hide toggle** flips the input type password↔text (+ aria-label/aria-pressed) → axe-clean (WCAG 2.2 critical/serious) → the back-link returns to `/signin` (`auth/sign-in`→`/signin` redirect); standing 0-console-error gate across the whole walk. **RED-first proven** (the toggle didn't exist on prod → gate 7 timed out) → **PROD 14/14 GREEN** (`main-6CSU4TNB.js`). **Paired value-add — password show/hide toggle** on BOTH auth pages (`sign-up` + `sign-in`, `data-testid=*-password-toggle`, eye/eye-off SVG, `aria-label`/`aria-pressed`, keyboard-operable, focus-ring) — a busy owner types their password once and can verify it, no re-type; +2 Karma regressions (both specs assert type-flip + aria). **Paired a11y root-fix (the probe caught it):** the sign-up "OR" divider was `text-white/30` = **2.62:1** (a real WCAG 1.4.3 AA fail) while its sign-IN sibling already used `text-white/50` (~5.3:1) — pure drift; matched sign-up to `/50`, redeployed, re-verified axe-clean. *(FULL-FLOW E2E, AL-735 — verify-signup-flow.mjs)*

- [x] **B.8 Conversion-pivot render SPEED (cold, unauth)** — the two unauth pivots (homepage `/` search + `/signin` email) paint their primary interactive control FAST + clean on a COLD visit. *(verify-conversion-pivot-speed.mjs, AL-683 — wired into run-all)* **The gap it closes:** every § B flow probe `waitForSelector`s with a long timeout, so it stays GREEN even if a cold prospect waited 15s for the control to paint (bundle-bloat regression, broken/renamed lazy chunk, render-blocking third-party, hydration stall) — a silent, severe conversion killer NO existing probe caught. This is the tripwire: FRESH cache-clean context, measures nav-start→control-visible + FCP + LCP, asserts ≤8s (generous ~3× the ~1.6–3s prod baseline so network variance never cries wolf — `validator-precision-discipline`) + 0 cold-load console errors, emits one structured JSON line per pivot for trend tracking. **PROD-verified GREEN (2026-09-16):** homepage interactive **2993ms** (fcp/lcp 1064), `/signin` **1587ms** (fcp/lcp 276), 0 console errors. Full § B flow suite (B.1–B.7) re-confirmed green on prod this fire (10/10 probes).
- [x] **B.9 Site-DELETE envelope (destructive owner flow)** — the owner "delete my site" action (`DELETE /api/sites/:id` → soft-delete `deleted_at`+`status='archived'`, KV host-cache purge, optional Stripe cancel) was the highest-value flow AROUND the golden path with **NO headless probe** — a destructive, IDOR-CATASTROPHIC action (a cross-org auth bypass = one tenant DESTROYS another tenant's live site). `check-idor-gates` proves the handler HAS an ownership guard statically; nothing proved it WORKS live. A REAL delete of a real owned site is destructive (approval-required, out-of-headless-scope, same shape as B.5's real-card charge + B.6's real WebContainer publish), so the headless-safe scope is the **security envelope + non-mutation proof**: unauth DELETE → **401** (throws pre-ownership) · authed DELETE nonexistent id → **404** (`requireOwnedSite` throws pre-UPDATE) · authed DELETE a real FOREIGN id → **404** (the cross-tenant-destruction IDOR, gated) · **NON-MUTATION** the caller's own site set is byte-identical before/after every rejected call (verify-against-source-of-truth — the authoritative store lost nothing). **SAFETY INVARIANT:** an authed DELETE is sent ONLY to NON-owned ids (a sentinel UUID + a real foreign id from **org-brian-001** — loop-owned + soft-delete-recoverable, never an external customer's) — NEVER an owned id (the handler has no pre-write validation reject → an authed DELETE of an owned site would truly soft-delete it). **PROD-VERIFIED GREEN 4/4** (`own_sites 109→109` unchanged, unauth=401 · ghost=404 · foreign(org-brian-001)=404; the distinct 401≠404 from one endpoint proves non-vacuous reads), 0 mutation; emits a structured `::json::` line per run. Durable `verify-site-delete-envelope.mjs` wired into run-all. *(FULL-FLOW E2E, AL-751 — verify-site-delete-envelope)*

## § C — Generated-site QUALITY (the CORE PRODUCT) · headless PROD audit · OWNER: GENERATED-SITE QUALITY
*Audit DEPLOYED `{slug}.projectsites.dev` (vanta-strength-austin · ironhaus-houston · vantage-digital-studio-portland). Fix at ROOT CAUSE in the TEMPLATE (`github.com/HeyMegabyte/template.projectsites.dev`, lands next build) or the site-gen prompt / `build_validators.ts` — never a one-off. Ship a durable probe under `e2e/site-quality/` wired into run-all.*
- [x] **C.1 build_validators invariants — ROOT FIX SHIPPED (AL-110), lands next build** — required files, asset existence, meta 50-60 / 120-156, JSON-LD ≥4 accurate blocks, single H1, no banned-slop. **Ground truth (2026-09-07): deployed sites ship ZERO real JSON-LD blocks (the template carries the "open-now" CONSUMER widget that reads `script[type=application/ld+json]` but the build never EMITS the data — the probe's marker count reported a false "1"), sub-120 descriptions, and `\'` JS-escapes leaking into meta. Report-mode validators logged it, never blocked → shipped live. Fixed at ROOT: `finalizeSeoInvariants(files, ctx)` in `build_validators.ts` — a deterministic finalizer wired into `site-generation.ts` finalize-build (alongside `repairDoubleDotCanonical`/`repairDanglingEmDash`) that injects WebSite+Organization+WebPage+BreadcrumbList, expands/clamps meta, and unescapes `\'` — all from shell signals + the real name, NO fabrication. Build-prompt mandate strengthened (≥4 blocks, prerendered head, straight quotes) as the quality belt. Proven: 6 unit tests + run against the REAL fetched vanta shell (0→4 blocks, desc 81→146c, escape fixed). Probe `verify-build-invariants.mjs` tightened to count REAL blocks (not the decoy marker) + wired into `e2e/site-quality/run-all.mjs`; it flips GREEN as sites rebuild (root fixes land next build per loop guardrail — NO redeploy of existing sites).** **PROBE FALSE-POSITIVE ROOT-FIXED (AL-429):** the C.1 probe's meta-desc regex `content=["']([^"']*)["']` used a char-class excluding BOTH quotes, so it TRUNCATED a valid double-quoted desc at the first apostrophe — `content="Seattle's dependable choice…"` → captured just "Seattle" = a false **7-char FAIL** on EVERY possessive business name (the generated meta template literally uses "{City}'s dependable choice for {vertical}", so a huge fraction of the fleet false-failed). `build_validators.ts` had already learned this (its `metaDescText` uses a delimiter backreference + is unit-tested, `build_validators.test.ts:233` "apostrophe"), but the PROBE drifted. Fixed by mirroring the SSOT regex (`content=(["'])([\s\S]*?)\1`) + hardened the title regex to `<title[^>]*>`. PROD-verified across 6 sites: gentle-dental **7c→140c PASS**, grimeys "Nashville's" now ✓ 151c — while genuinely-short descs still correctly flag (vanta/ironhaus 80-81c, older pre-finalizer builds → flip green on rebuild). Restores C.1 gate trust (per `validator-precision-discipline`: false positives erode trust + mask real regressions). **WRONG-VERTICAL META-DESC ROOT-FIXED (AL-491):** the CLIENT homepage meta description (`{SEO_DESCRIPTION}` → Home.tsx `useSEO` → rendered `<meta name=description>`) keyed off the BROAD pack vertical, so a brewery + a cocktail bar both collapsed to `restaurant` and shipped a wrong-vertical SERP snippet "Fresh, made-from-scratch food from local ingredients" (live on half-acre-beer-chicago + the-secret-society-portland). Root fix = new `hero_copy.seoDescriptionFor(mode,name,cat,city)` (commerce-mode-angled, name+cat+city-woven, GUARANTEED 120-156) seeded into `_content.json` SEO_DESCRIPTION for the SAME collapsing sub-verticals as the AL-485 hero image (heroImg gate → core verticals keep their crafted pack descriptions, no regression); existing-wins merge overrides the pack default. New durable probe `e2e/site-quality/verify-meta-desc-vertical.mjs` (auto-joins run-all): asserts the RENDERED meta desc names the vertical noun (no food-cluster bridge — a desc must name the SPECIFIC vertical) → currently ❌ half-acre + secret-society (pre-fix builds), ✓ gentle-dental control; flips green as they rebuild. 59/59 hero_copy tests, tsc clean, 0 lint errors. **DECOUPLED FROM heroImg GATE (AL-539, `c3142575f`):** AL-491 gated the SEO_DESCRIPTION seed to the SAME collapsing sub-verticals as the curated hero image (`heroImg` truthy) — so a vertical with a CORRECT pack hero but a WRONG pack meta-desc slipped through: a BAKERY (`tartine-bakery-sf`) shipped the restaurant default "made-from-scratch food from local ingredients" and a BARBERSHOP (`rudy-seattle`) shipped a wellness "mindful treatments" default (both flagged live by verify-meta-desc-vertical; ✓ ritholtz/catbird/parnassus which DID get the seed). Root fix = seed `seoDescriptionFor` UNCONDITIONALLY for every vertical (always weaves real name+cat+city, guaranteed 120-156, strictly > any business-agnostic pack default). hero_copy 69/69 (+bakery case), tsc clean; flips green fleet-wide on rebuild.
- [x] **C.1a Theme fonts actually LAND (elaboration groundwork)** — ground-truth found every deployed site shipped headings in system-ui (theme fonts never applied). Fixed at the two named levers (AL-091): `theme-presets.ts` dossiers now emit a MANDATORY copy-pastable font block (exact `<link>` + `--font-heading`/`--font-body`) + 14 facets + 16 themes; `site-generation.ts` build prompt mandates wiring them. Lands on the NEXT build. *(verify-build-invariants.mjs will confirm font application once a post-change build publishes.)*
- [x] **C.1b Navbar logo-icon TRANSPARENCY — measurable + root-diagnosed (AL-496)** — the generated `logo-icon.png` can ship OPAQUE (colorType 2, no alpha) — a blind spot no gate covered (build_validators checks EXISTENCE+size, verify-pwa checks refs resolve, neither checks ALPHA). New durable prod probe `e2e/site-quality/verify-logo-icon-transparency.mjs` (auto-joins run-all; parses PNG IHDR color-type + tRNS, zero deps) → **PROD fleet ~50% opaque** (gentle-dental+parnassus ✓transparent colorType 6; ironhaus+vanta ◻︎opaque colorType 2). TRACKING-mode (::notice — an opaque ROUNDED badge is an acceptable fallback; validator-precision). ROOT diagnosed empirically (sharp corner-inspection): `strip-logo-bg.mjs` correctly refuses a NON-uniform bg (krugers corners white-top→blue-bottom, spread 583; its white emblem shares the white bg → no safe flood-fill can strip it → opaque no-404 fallback AL-386 is correct). The REAL fix is container-side icon-GEN (constrain Ideogram to a plain uniform bg distinct from the mark, or transparent) → flips the opaque cohort transparent on rebuild; deliberately did NOT make the stripper more aggressive (would eat marks sharing a bg color). *(GENERATED-SITE QUALITY, AL-496 — verify-logo-icon-transparency.mjs)*
- [x] **C.1c Navbar wordmark INK-vs-theme CONTRAST — root-fixed + probe + proven live (AL-617)** — the icon-luminance logic picks theme polarity, but the wordmark is a SEPARATE Ideogram asset that can bake ink FIGHTING the theme: alpenglow-sports-tahoe-city shipped a wordmark with mean opaque-ink luminance **25/255 on a DARK header** (icon 178=light → dark theme was correct) = an illegible dark-on-dark smear at a valid banner aspect — a blind spot no gate covered (icon-alpha C.1b / aspect `wordmarkTooSquare` / load all passed). Root fix in the TEMPLATE Header (`f87b882`): new pure `wordmarkContrastsTheme(inkLum, darkHeader)` + the wordmark `<img>` onLoad canvas-samples the opaque-ink luminance and falls back to the always-theme-correct `.site-wordmark-text` (theme ink + halo) when it doesn't contrast. New durable prod probe `e2e/site-quality/verify-logo-wordmark-contrast.mjs` (`7e482e6c2`, auto-joins run-all): renders the navbar → PASS on text-fallback OR contrasting-PNG, FLAG a low-contrast PNG shown. RED-first flagged alpenglow (ink 25 on dark), control gentle-dental PASS (ink 43 on light). **PROVEN LIVE:** rebuilt alpenglow → probe RED→GREEN (rebuilt wordmark ink 136 contrasts) + vision-confirmed a legible gold "Alpenglow". Header.wordmark 18/18. *(GENERATED-SITE QUALITY, AL-617 — verify-logo-wordmark-contrast.mjs + template Header ink-gate)*
- [x] **C.2 CWV — ROOT-FIXED + PROD-VERIFIED GREEN (AL-394): edge-cache the assembled HTML via `caches.default` (HOST+VERSION+path+paid key) → cold TTFB 1.3s→~70ms.** The named cold-TTFB slice shipped in the WORKER serve path: `serveSiteFromR2` now stores the finished HTML in `caches.default` keyed by host (per-host canonical safety) + version (rebuild-invalidation, no stale beyond the ~60s resolveSite KV window) + paid-flag (top-bar). PROD-VERIFIED 2026-09-12: new `e2e/site-quality/verify-edge-cache.mjs` (auto-joins site-quality run-all) → vanta [hit,hit,hit] / ironhaus+gentle-dental [miss,hit,hit], all canon=own-host + 200; edge-hit TTFB **vanta 82ms / gentle-dental 70ms (was vanta 1339ms cold)**; metering INTACT (gentle-dental `visitor_events` 7→18 across cache hits — the caller-level pageview fires before serve, no undercount). Fixes ALL 108 deployed sites at serve-time, NO rebuild. Prior context (AL-201): CLS 0.00 all sites, LCP was borderline on cold-mobile TTFB — now resolved.** New `e2e/site-quality/verify-cwv.mjs` (headless Chromium PerformanceObserver, cold load @390px, auto-joins run-all) measures LCP≤2.0s + CLS≤0.05 on deployed sites. Ground truth (2026-09-08): **CLS 0.00 on every site** ✓; **ironhaus-houston LCP 1332ms** ✓; **vanta-strength-austin LCP 2352ms ✗** — driven by a variable **cold TTFB (1294ms)** (warm desktop trace was 1144ms / TTFB 782ms — passes). ROOT CAUSE: the serving worker's response isn't edge-cached via the Cache API, so a cold browser re-runs the full KV/D1/R2 cold-path (`cfWorker;dur=97` warm, but cold pays host-resolve + R2 fetch); `s-maxage=3600` is advisory-only for Worker responses. Shipped a safe repeat-visit fix — content-hashed `/assets/*-<hash>.{js,css}` now `Cache-Control: immutable` (`site_serving.ts`, CI-deploy). NEXT (deliberate slice, hot-path): edge-cache the injected HTML via `caches.default` keyed by slug+version → cold TTFB → sub-100ms. Not ticked: vanta misses the strict ≤2.0s cold budget (Google "good" ≤2.5s it clears). **INP NOW HEADLESS-GATED (AL-455):** the last un-gated Core Web Vital closed — `e2e/site-quality/verify-inp.mjs` (auto-joins run-all) DRIVES real discrete interactions (Playwright trusted-CDP clicks register as real `event`-timing entries with an interactionId, exactly like a human tap — the reason verify-cwv couldn't do it on a passive load) on always-present NON-navigating template controls (command-palette / theme-toggle / mobile menu-open+close / FAQ accordion) and reads the worst interaction latency via the Event Timing API (how field INP is computed). Gate ≤200ms. **PROD-GREEN @390px: parnassus-books 48ms · ironhaus-houston 32ms · gentle-dental-seattle 40ms** (each over 3 real menu+FAQ interactions) — snappy, well under 100ms cinematic aim. Fail-open NOT-MEASURABLE when <2 interactions register (validator-precision). A janky handler / heavy hydration on a future build now goes RED. *(GENERATED-SITE QUALITY, AL-201 + AL-455)* **CWV PROBE RE-TUNED — validator-precision + probe↔doc drift fix (AL-513):** `verify-cwv.mjs` was measuring a COLD edge (first-visit-per-PoP TTFB ~650ms) and HARD-failing at the strict ≤2000 — which false-failed the fresh florist (charleston LCP swung 2036↔2760 purely on cold-vs-warm TTFB, LCP element = a post-hydration CTA) even though § C.2 is CHECKED accepting Google "good" ≤2500. Root cause was a MEASUREMENT artifact, not a render defect: the modal repeat/trafficked-site visitor hits a WARM edge. Fix — (1) WARM the edge in a throwaway context, then measure in a FRESH (cold-browser) context = the honest modal new-visitor state (warm edge + cold browser, per the AL-399 warmed-edge precedent); (2) TWO-TIER gate — HARD FAIL only worse-than-Google-"good" (LCP >2500 / CLS >0.1, a real regression), TRACK (::notice) the 2000–2500 cinematic-strict band with the LCP element for a future hydration deep-fix; (3) LCP-element diagnostic. **PROD-GREEN (stable across 3 runs each): charleston-florist LCP ~608ms · vanta ~664ms · ironhaus ~650ms — all clear the CINEMATIC strict ≤2000 warm.** charleston full site-quality suite now **20 pass / 0 fail** (was 19/1). The generated sites have genuinely good LCP (the preloaded splash image paints ~600ms warm); the cold-edge-miss ~2650 is a one-time first-visitor cost, not a template defect. *(GENERATED-SITE QUALITY § C.2, AL-513 — verify-cwv.mjs two-tier + warm-edge)* **LCP HERO-ENTRANCE FIX (AL-533):** the `.hero-enter` `@starting-style{opacity:0}` fade hid the hero H1 (the LCP element on hero-text-dominant sites) until the fade finished — measured catbird LCP 2560ms (el=H1.hero-enter) vs ~1.0s where the LCP is the small navbar wordmark. Fixed to a transform-only rise (template `b036f53`); H1 paints at full opacity immediately, drops LCP ~1.5s on rebuild. verify-cwv tracks it. **WORDMARK-LCP COLD-FIRST-VISIT ENHANCEMENT (AL-718):** on TEXT-hero sites (H1 over gradient, no image bigger than the ~18KB wordmark) the LCP element is the **navbar wordmark** itself — measured flour-bakery COLD-edge LCP 2292ms (el=`IMG.site-wordmark`) vs ~924ms warm, because the wordmark renders late behind hydration + the AL-591 HEAD-probe then fetches (the `fetchpriority`/preload were spent on the hero image, which isn't the LCP here). ROOT FIX — `finalizeSeoInvariants` (build) injects `<link rel="preload" as="image" href="/logo-wordmark.png" fetchpriority="high">` into every route head when the build made the asset (guarded — an absent-asset preload 404s+warns), so cold-first-visit fetches the LCP bytes eagerly ≈ the warm number; template `Header.tsx` (acc5952) adds `fetchPriority="high"`+`decoding="async"` on the wordmark+icon `<img>`. +6 build_validators unit tests (136/136) + durable probe `verify-lcp-wordmark-preload.mjs` (auto-joins run-all; 3/3 cohort PENDING now, flips green on rebuild — no false-red). Warm-edge C.2 already passed (AL-513); this is a first-visitor improvement, lands next build. *(GENERATED-SITE QUALITY § C.2, AL-718 — wordmark preload)* **SERVE-TIME PERF-TRANSFORM REGRESSION GUARD (AL-747):** a fresh perf profile of superdawg showed ~565KB of analytics JS (GTM `GTM-PHKT6J6P` + two GA4 `G-VBBRRQLDFT`/`G-TN58NW1G99`) — but verify-before-implement confirmed `site_serving.ts` ALREADY DEFERS the gtm.js/gtag.js download off the critical path (requestIdleCallback + first-interaction gating; the code comment records this fixed "TBT 3.68s, Perf 32") AND `asyncifyRenderBlockingFonts` makes the Google-Fonts CSS non-blocking (media=print). Both are big, easy-to-silently-break serve-time optimizations with **ZERO regression coverage** — a refactor that reverts to an eager `<script async src=…googletagmanager…>` or drops the media=print swap silently re-adds the TBT/FCP hit to EVERY generated site. Shipped `e2e/site-quality/verify-analytics-deferred.mjs` (auto-joins run-all) — asserts on the deployed HTML: no eager analytics loader in markup · requestIdleCallback + `__gtmL`/`__ga4L` + interaction gating present · Google-Fonts stylesheets all media=print · (advisory) cold FCP ≤3000ms. **PROD-VERIFIED GREEN 8/8 on superdawg-drive-in-chicago (FCP 2160ms) + screen-door-buckman (FCP 728ms).** No code change needed (the optimizations hold); this is the missing durable guard. *(GENERATED-SITE QUALITY § C.2, AL-747 — verify-analytics-deferred)*
- [x] **C.3 axe 0 violations @ 6 breakpoints — probe SHIPPED + root-cause LOCKED (AL-140), flips green on rebuild** — new `e2e/site-quality/verify-a11y.mjs` (headless Chromium + @axe-core/playwright, WCAG 2.0/2.1/2.2 A+AA @ 375/390/768/1024/1280/1920, auto-joins run-all) audited a deployed site and found the SOLE serious violation: `color-contrast` on `team-role-card` body copy — muted `#64676b` on near-black `#0d0706` = **3.51:1** at mobile 375/390px. Root cause: a PRE-FIX build (vanta shipped before the dark-muted `oklch(0.65)!important` fix). The current template's muted clears AA on any brand surface, and the contrast test's blind spot (only checked `subtle` vs `surface-elevated`, never `muted` vs the darkest/lightest brand bg) is now closed — `template.projectsites.dev/src/design-tokens.contrast.test.ts` asserts dark muted ≥4.5:1 on near-black + light on white, every hue (7/7, template `96d095f`). Flips green as sites rebuild (NO redeploy of existing sites, per loop guardrail). ⚠️ **CORRECTION (AL-213):** a REBUILT light-theme site (harborline, cream `#fcf0e3` bg) STILL fails AA — muted `#89817c` on cream = 3.4:1 (`.team-role-card` body) + accent `#7f732d` on cream = 4.22:1 (`.lm-dir`). The `96d095f` lock only covered DARK muted on near-black + light on white, NOT LIGHT-theme muted/accent on cream. Real OPEN gap → scheme-aware muted+accent tokens needed (matches `generated-site-accent-text-contrast` + `tailwind-opacity-on-muted-token-fails-aa-contrast`). C.3 is NOT actually green on light-theme sites — reopening as a template design-token slice. ✅ **MUTED-REVEAL INSTANCE ROOT-FIXED + PROD-VERIFIED GREEN (AL-290, 2026-09-10):** the muted `#89817c` = 3.4:1 was NOT a static-token fail — it's the `.team-role-card` **scroll-driven reveal** (`animation-timeline: view()` `team-role-rise` `opacity:0→1`) compositing the settled-AA-safe muted (oklch 0.42 = 5.2:1) at ~0.70α mid-`entry`, which axe measures as the effective color (3 forensic diagnostics reverse-solved α≈0.70). Root fix (template `1e16aa3`): floored `team-role-rise from{opacity}` at 0.9 so the body copy holds ≥4.7:1 at EVERY frame (soft fade+slide kept); locked with a composite-math unit test (OLD 0.70 fails / NEW 0.9 clears / keyframe ships ≥0.9). **VERIFY-REAL (the rebuilt-site axe C.3 was reopened for):** `/reset` rebuilt char-bar-kansas-city (~10min, fresh `index-DOIf4J5O.css`) → `verify-a11y.mjs` **0 axe violations across all 6 breakpoints** (was 1 serious @375/390). Flips green on every site as it rebuilds. **REMAINING C.3 follow-up:** harborline `.lm-dir` accent-as-text `#7b6f28` = 4.47:1 (rounding-miss under 4.5) — a separate accent-text token that should route through `--color-accent-readable`; a future C.3 slice (not the muted-reveal issue). ✅ **REVEAL-SAFETY GUARD SHIPPED (AL-404):** axe + verify-a11y run at DEFAULT motion, so a scroll-driven reveal (`animation-timeline: view()` — bento-tile-rise/reveal-on-view/process-step) that shipped an UNGATED `opacity:0` base would strand content INVISIBLE for reduced-motion + Firefox (no view()-timeline) + no-JS users (WCAG perceivability) with NO gate catching it. New `e2e/site-quality/verify-reveal-safety.mjs` (auto-joins run-all, loads under `prefers-reduced-motion: reduce`, asserts every text block's effective opacity ≥0.5; excludes interactive collapsibles — accordion/`<details>`/`[hidden]` — per validator-precision) → **PROD-GREEN on russian-river + vanta + ironhaus**: every reveal is base-visible + reduced-motion-reset-safe (index.css:1071 reset + `@supports+no-preference` gating verified). Confirmed current builds are safe; durable regression guard for the previously-unguarded reduced-motion reveal-stranding class. ✅ **ALL REMAINING C.3 VIOLATIONS ROOT-FIXED + PROD-PROVEN (AL-440):** headless axe on CURRENT builds (zingermans dark `.mt-2` #7b8185/#17211b=4.19 · harborline light `.team-role-card .mt-3` #89817c/cream=3.4 · `.lm-dir` accent 4.22) — all were the AL-290 reveal-opacity-composite class on the SIBLING keyframes AL-290 didn't cover (`rise-in`/`.reveal-on-view` + trust/stat/process/team-card reveals still `from{opacity:0}`). PROVEN via `prefers-reduced-motion:reduce` → 0 violations (settled colors AA-safe; only the mid-entry composite fails) + ancestor-chain diag (`.card-tactile.reveal-on-view` @ op=0.859 composites the muted text). Root fix (template `e92a98a`): floored `from{opacity}` 0→0.9 on all 6 text-container reveal keyframes (translateY slide kept); generalized AL-290's composite-math test into a 7-keyframe drift guard. **PROD-PROVEN fail→clean** by injecting the floored keyframes onto the live pages via `!important` animation-name override: zingermans 1→0, harborline 2→0 (the `.lm-dir` accent "future slice" was ALSO this composite, now cleared). Flips green as sites rebuild (NO redeploy).
- [x] **C.4 SEO + GEO — infra VERIFIED GREEN on prod + durable probe SHIPPED (AL-203).** New `e2e/site-quality/verify-seo.mjs` (fetch-based, auto-joins run-all) hard-gates the SEO/GEO infra on deployed sites → **7/7 both vanta + ironhaus**: canonical(home)=own custom host + canonical(/gallery)=SELF (the de-index guard `applyServedRouteCanonical` — a homepage-claiming canonical on sub-pages would de-index the site), **OG 1200×630 @82/78KB ≤100KB**, sitemap + `<lastmod>` on all 23 urls, robots.txt + sitemap-ref, `.well-known/security.txt`, llms.txt (GEO). Per-route metadata TITLE/DESC LENGTH (title 50-60 / desc 120-156) is owned by **C.1** (`verify-build-invariants` + `finalizeSeoInvariants` AL-110, root-fixed, flips green on rebuild — pre-fix vanta ships 44c/81c, ironhaus 57c/8c); the probe REPORTS it, C.1 gates it. *(GENERATED-SITE QUALITY, AL-203 — `verify-seo.mjs` 7/7)* **SOFT-404 TITLE root-fixed fleet-wide (AL-706):** unknown routes return a real 404 STATUS (no soft-404 ✓) but the server-rendered `<title>`+`og:title` REFLECTED the arbitrary URL slug (`/buy-cheap-widgets-spam` → "Buy Cheap Widgets Spam — Brand" on EVERY site's 404 — a reflected-content smell + broken 404). Root cause: `applyServedRouteTitle` title-cased the last segment unconditionally, ignoring the 404 status. Fixed: `applyServedRouteTitle(html,path,isNotFound)` emits a fixed "Page not found — {brand}" (+og/twitter) on a 404; `buildSiteResponse` threads `htmlStatus===404`; known routes unchanged. +3 jest (137/137), tsc 0. New `verify-soft404-title.mjs` (globbed into run-all) — **RED-first PROVEN on prod** (3/3 cohort reflect the slug) → flips GREEN fleet-wide on the worker deploy (serve-time meta, NO rebuild). **NUMERIC-CITY (STREET-NUMBER/ZIP) LEAK ROOT-FIXED (AL-736):** the length-based SEO gate is BLIND to a garbage-but-right-length value — live on swan-oyster-depot (the AL-729 OSM-sourced site): three naive address parsers pulled the wrong comma-field as the "city" from the verbose OSM `display_name` ("…, 1517, Polk Street, …, San Francisco, California, 94109, United States") → `<title>Blog — … · 1517 · get in touch to learn more</title>`, `<meta>… serving 1517 …</meta>` (template `placeholders.ts split(',')[1]` = street number), and homepage "Proudly serving 94109" (worker `site-generation.ts` + template `LocationMap.tsx` `parts[length-2]` = ZIP). AL-733b fixed the H1's `cityFromAddress` but MISSED these THREE consumers (the "audit EVERY consumer of the changed shape" lesson, `osm-display-name-city-parse-grabs-zip`). Ported the robust `cityFromAddress` to all three (template `placeholders.ts` + `LocationMap.tsx`, worker `site-generation.ts` finalize ctx.city). **Deterministic proof: 7/7 new template vitest** (OSM "…1517…San Francisco…California…94109…" → "San Francisco"; Places compact + street-peek NY + spelled-out-state Texas→Austin) + 284 worker jest green. New durable probe `verify-location-token.mjs` (globbed into run-all): **PROD RED-first on swan (5 leaks caught)**, **GREEN on the Places-sourced cohort** (franklin/pizzeria/wally — Austin/Phoenix) → flips green as OSM sites rebuild (Places is 403-down so ALL new deliveries use OSM → this protects the whole future fleet). Template→repo (next build), worker→CI.
- [x] **C.5 JSON-LD per route — serve-time injector SHIPPED + PROD-VERIFIED GREEN (AL-205/206).** New `e2e/site-quality/verify-jsonld.mjs` proved deployed sites ship **ZERO real JSON-LD on EVERY route** (0/4 routes both vanta + ironhaus — only the widget's empty reader tag; a real SEO/GEO gap for crawlers + AI-search). Root-fixed in the WORKER serve path (not just build-time): `applyServedRouteJsonLd` + `buildBaselineJsonLd` in `site_serving.ts` inject WebSite+Organization+WebPage+BreadcrumbList reflecting the ACTUAL served route, guarded to no-op when the build already emitted real JSON-LD (`finalizeSeoInvariants`) — so it fixes ALL 108 deployed sites at serve-time, **NO rebuild** (unlike C.1/C.6). 7/7 unit tests (`served_jsonld.test.ts`). **PROD-VERIFIED GREEN (AL-206):** `verify-jsonld.mjs` → **4/4 routes both vanta + ironhaus** ship 4 real blocks (WebSite+Organization+WebPage+BreadcrumbList) with WebPage reflecting the served route; the guard correctly NO-OP'd the marketing homepage (its own 5 rich JSON-LD blocks intact, not doubled). ⚠️ **REBUILT-SITE REGRESSION found + fixed (AL-212):** a `/reset` rebuild's build-time `finalizeSeoInvariants` bakes HOMEPAGE WebPage+BreadcrumbList into the shell → the old serve-time guard skipped (hasReal), so sub-routes served the homepage WebPage (harborline `/about` `wp=✗homepage-clobber`, 1/4 routes). Fixed: `applyServedRouteJsonLd` now DE-CLOBBERS on sub-routes (strips the homepage WebPage/Bc, re-injects per-route; keeps route-agnostic WebSite+Organization) — mirrors `applyServedRouteCanonical`. 8/8 tests. Seed sites (108) stayed 4/4 throughout. ✅ **De-clobber PROD-VERIFIED GREEN (AL-213):** after CI deployed it, `verify-jsonld.mjs` harborline → **4/4 routes wp=✓route**, exactly 1 WebPage/route (was 1/4 homepage-clobber pre-deploy) → the rebuilt-site regression is fully closed on prod. *(GENERATED-SITE QUALITY, AL-205 fix + AL-206 verify + AL-212 de-clobber + AL-213 prod-verify)* **DEDUP HARDENED (AL-522):** empirical fleet audit found the rendered DOM double-emitted WebSite/WebPage/BreadcrumbList (template `buildSiteJsonLd` re-emitted the server-injected site-level types; server blocks lack @id → Google can't merge → dup entities). Root-fixed in template `9f227bb` (client emits ONLY the rich business entity; served HTML unchanged) + durable `verify-jsonld-dedup.mjs` (hard-gates served-HTML uniqueness, tracks the client migration to 0 over rebuilds).
- [x] **C.6 PWA — probe SHIPPED + real dangling-icon bug ROOT-FIXED (AL-204) + PROD-VERIFIED on a real rebuild (AL-212).** ✅ **AL-212: `/reset` rebuilt harborline (~6min) → `verify-pwa.mjs` 6/6 GREEN** — the icons that 404'd pre-rebuild (maskable-512/android-chrome-512/192/monochrome) now all resolve 200; the template `77762fc` generate-favicons fix works end-to-end on a real build. New `e2e/site-quality/verify-pwa.mjs` (fetch-based, auto-joins run-all, HEAD-checks icon refs actually resolve) caught what the required-files validator MISSED: EVERY deployed site's `site.webmanifest` declared `android-chrome-192/512` + `maskable-512` + `monochrome-icon` + 2 screenshots that **ALL 404'd** (broken Android adaptive/install icon — confirmed on vanta/ironhaus/harborline/lumen-oak). Root cause: template `generate-favicons.mjs` emitted favicon-16/32 + apple-touch + .ico/.svg but never the android-chrome/maskable/monochrome PNGs the static manifest promised. FIXED in template (`77762fc`, lands next build, NO redeploy): parametrized `pngSolid(size,rgb,rounded)` + emits all 4 at correct sizes (locally-verified 192×192 / 512×512 / full-bleed maskable / white monochrome) + dropped the un-generatable screenshot refs from `site.webmanifest`. Manifest otherwise excellent (name/display/theme_color/display_override, 4 shortcuts, share_target/file_handlers/protocol_handlers, branded offline.html 9.3KB, sw.js install+cache+precache). Deployed sites 404 until they rebuild (probe flips green then). Real install-screenshots = container follow-up. *(GENERATED-SITE QUALITY, AL-204 — verify-pwa.mjs + template 77762fc)* **MANIFEST-QUALITY root-fixed (AL-475):** the fresh C.6 audit found the two SOFT gaps verify-pwa reports were real worker-seed bugs — `short_name` was a mid-word cut ("Ironhaus Str" = `"Ironhaus Strength & Conditioning".slice(0,12)`, ugly home-screen label) and manifest `description` + the visible Footer blurb were EMPTY (additionalContext unset = the common case). Root-fixed at the worker seed (`site-generation.ts` — the container fills `{BUSINESS_SHORT_NAME}`/`{BUSINESS_DESCRIPTION}` from it): `derivePwaShortName` (≤12 at a WORD boundary → "Ironhaus" / "Union Garage") + `deriveBrandDescription` (owner context, else truthful "{name} — {category}", never empty). 11 unit tests lock it (`site_generation_pwa_tokens.test.ts`, the ironhaus case is the anchor); tsc + 33/33 site-gen green. Worker `fbfbe0648` (CI-deployed); flips both soft gates clean on next build.
- [x] **C.7 Beat-the-source** — denser / faster / more-accessible than the original (competitor floor ≥15% per `competitor-research`). **DISTINCTIVENESS advanced (AL-213):** the template's per-industry WebGL hero backdrop (`WebGLHeroBackdrop`, aurora/waves/mesh auto-derived from `brand.themeStyle`, LCP-safe, reduced-motion/no-WebGL static fallback) was DEAD on every site (gated behind an opt-in prop the generator never passed) → now auto-wired ON in `HeroCenter`+`HeroSplit` (template `2430636`) + a durable real-browser probe `e2e/site-quality/verify-hero-backdrop.mjs` (auto-joins run-all). PROD-VERIFIED on the rebuilt harborline: `canvas=1 inHero=✓` (pre-fix vanta `canvas=0`). ✅ **TICKED — HARNESS SHIPPED + PROD-GREEN (AL-399):** `e2e/site-quality/verify-beat-source.mjs` (auto-joins run-all) scores a deployed gen site vs its REAL source URL on the objective, headless-measurable subset (SEO 8 / PWA 4 / mobile 1 / rendered-density 3 / warm-edge-speed 2). **Three measurement artifacts the rebuild fixed** (each was quietly favouring the source): (1) density is counted on the RENDERED headless-Chromium DOM, NOT the SPA prerender shell — gentle-dental shell=3 imgs/509 words → rendered=18 imgs/619 words; (2) speed is TTFB on a WARMED edge (post-AL-394 `caches.default`) not a cold first fetch — ~600ms→~80ms; (3) each PWA path is content-type-validated, so a source's soft-404 (serving its 200-HTML homepage for `/sw.js` etc.) scores 0 not a phantom 4/4 (the lying-200 class per `render-integrity-probe-blind-to-graceful-soft-404`). **Honest gate** (NOT ×1.15-on-total — SEO saturates against a competent source, making the total-beat structurally unreachable even when we're plainly better): gen must MATCH-or-beat source SEO (floor) + never regress total + strictly WIN ≥2 structural modern-web dims {PWA, JSON-LD depth, warm speed, density}. The ≥15% visual/copy/conversion beat stays AI-vision-gated (verify-hero-backdrop + verify-theme-match + container competitor loop). **PROD-GREEN:** gentle-dental gen=16/18 vs gentledental.com=13 (+23%, 3/4 structural wins) · tartine gen=16 vs tartinebakery.com=9 (+78%, 3/4). One real minor enrichment lever noted (not a gate fail): our rendered homepage ~600-619 words < the 800 density bar (dens 2/3) — a site-gen content-density improvement for a future fire. *(GENERATED-SITE QUALITY / FULL JOURNEY, AL-213 + AL-399)* **DENSITY now a FIRST-CLASS gated dimension (AL-461):** density was only a sub-score INSIDE beat-source (scored vs the source — passes against an equally-thin source). New `e2e/site-quality/verify-content-density.mjs` (auto-globs run-all) gates the ABSOLUTE rendered-homepage floor (≥700 words + ≥6 imgs; 800 aim) on the settled SPA DOM. PROD fleet distribution: laughing-lotus **844✓** / vanta **847✓** / ironhaus 846✓ clear the aim; **parnassus 681✗** (recent retail) + gentle-dental 565✗ (old build) fall short — the density variance is per-vertical (the uniform `homepageFaq` block contributes ~250 words evenly; the gap is thinner retail/dental PACK copy). Tracking-mode (::notice, 2/4 below) — the root-lever for the follow-on is `hero_copy.homepageFaq` (uniform) + `template/scripts/gen-content-packs.mjs` per-vertical copy; the gate flips green as those densify + old sites rebuild. **DENSIFY ROOT-FIX SHIPPED (AL-466):** boosted `homepageFaq` 4→5 Q&A per mode (6 new in-style, mode-specific, slop-free answers) — the ONE uniform density lever, +~60-75 words on EVERY vertical (retail FAQ 265→339w) → a rebuilt parnassus ~681+74≈755 clears the 700 floor; also enriches FAQPage JSON-LD to 5 items (GEO). Worker seeds `FAQ_5_Q/A` (via CI) + template `Home.tsx` renders the 5th row drop-guarded (pre-FAQ_5 builds still render 4, `f8d3238`). tsc + Jest **51/51** hero_copy + full worker **11470/11470**. Flips the density gate green as sites rebuild. **NEW GATE — VERTICAL↔FRAMING MATCH (AL-421):** `verify-conversion-framing.mjs` (real Chromium, auto-joins run-all) + `build_validators.validateConversionFraming` (report-warn `conversion.reservation_on_quickserve`) catch a wrong-vertical defect the beat-source harness missed — a QUICK-SERVE site (ice cream/coffee/bakery) reading like a full-service restaurant ("Reserve a table"/"Reservations welcome"). PROD-flagged jenis + tartine (2 deployed sites), passed gentle-dental. AL-420 root-fixed the SEEDED surfaces (quickserve CommerceMode + HERO_CTA seed → hero CTAs/FAQ verified on 2 rebuilds); the AI client-rendered sections are a tracked residual (flips green as quickserve sites rebuild; the warn guides the validator-fixer). Tracking mode (::notice) until the fleet cycles. **NEW GATE — HERO IMAGE ↔ VERTICAL (AL-425):** `verify-hero-image-vertical.mjs` (real Chromium, auto-joins run-all) decodes the hero Unsplash ixid query + asserts the business's specific vertical noun (H1/title minus generic-retail words) appears in it — catches a wrong-vertical HERO IMAGE (guitar shop → women's-clothing boutique, AL-423/424) that every other gate missed (image 200s + allowlisted + axe-irrelevant, only the SUBJECT wrong). PROD-flagged gruhn (guitar→boutique) + tartine (bakery→restaurant), passed gentle-dental (dental→dental clinic). Root fix = AL-424 vertical-specific hero query (research_images + buildPrompt); the query is a persisted _assets.json asset → flips green on a FRESH rebuild. Tracking mode (::notice). **SUB-VERTICAL ROOT FIX SHIPPED (AL-485):** AL-480 corrected that AL-424's buildPrompt query is INERT on the ~150s fast path — a plant shop / cocktail lounge / record store collapses to the pack's BROAD-vertical `HERO_IMAGE_URL` (retail "shop shelves" / restaurant "cafe interior"), and perennials-brooklyn + the-secret-society-portland were both flagged live. Real fix = the prescribed delivery-time SEED: new `src/services/hero_image.ts` `heroImageForVertical(catPhrase)` — a curated map of 8 collapsing sub-verticals (plant/florist/cocktail/record/bookstore/brewery/jewelry/tattoo) → REAL Unsplash heroes (on-vertical pixels verified by the photo's alt + a query-encoding ixid so `verify-hero-image-vertical` decodes the noun), seeded into `_content.json` HERO_IMAGE_URL/ALT via the proven existing-wins merge (verified: container-server.mjs applyVerticalContentPack lines 908-914 = pack-base, non-blank-existing-wins). 6/6 unit tests (map + synonyms + null + no-barber-false-match + all-8-URLs-200 + ixid-decode probe-compat) + all 8 URLs prod-200 + tsc/0-lint-errors. Worker CI-deploy; flips green as sub-vertical sites rebuild. Probe default widened to track perennials-brooklyn (plant). Follow-on: extend the curated map as new collapsing sub-verticals surface + add `plant`/`florist` etc. to `categoryFromName` so category-less creates also route right. **DENSITY PROD-VERIFIED GREEN on a FRESH build + finance-cluster PROVEN LIVE (AL-508):** delivered **ben-badgley-cpa** (accounting → professional mode, fast-path ~4min) and audited the fresh build → `verify-content-density` **800 words / 18 imgs** (clears the 800 aim — proves the AL-466 5-FAQ density lever end-to-end on a post-fix build; the parnassus 681 was purely a pre-AL-466 4-FAQ build) + `verify-hero-image-vertical` **PASS via the AL-507 FINANCE_LEXICON bridge** ("financial advisor office meeting" matches {asheville,accounting} [finance-cluster] — a financial-office hero, NOT a law office) + meta-desc **NON-legal** (the AL-504 wealth→legal defect is GONE) + 5 JSON-LD + HTTP 200. **Audit surfaced + ROOT-FIXED a NEW grammar defect:** the heritage persona headline hardcoded the article → shipped the ungrammatical **"A Asheville accounting built on trust"** (live H1 on ben-badgley-cpa); `hero_copy.ts:350` now uses `indefiniteArticle(city)` → "An Asheville …" / "A Boston …" (63/63 hero_copy Jest incl. the AL-508 city-article test, tsc clean). Worker CI-deploy; the H1 flips grammatical on rebuild. *(GENERATED-SITE QUALITY, AL-508 — verify-content-density + verify-hero-image-vertical, fresh ben-badgley-cpa build)* **DENSITY-MEASURE HARDENED (AL-527):** verify-content-density now counts the COLLAPSED FAQ-accordion answers (real Google-indexed content innerText omitted) — corrected a systematic ~240-word/site undercount. Fresh builds measure 992-1077 (very dense, beat-source); a sub-floor site is a STALE pre-AL-409 build that densifies on rebuild, not a live gap. **CREATIVE CLUSTER — hero-image ROOT-FIX + probe bridge (AL-544, `80f06e680`):** `pentagram-nyc` (design studio → brutalist) had NO curated hero (`heroImageForVertical` covered plant/florist/cocktail/record/bookstore/brewery/jewelry/tattoo/finance but not the creative domain) → fell to the pack's generic "creative agency team meeting" hero, flagged live by verify-hero-image-vertical. Two-part fix mirroring FINANCE (AL-507): (a) `hero_image.ts` CREATIVE cluster — a real design-studio-workspace Unsplash hero + a precise rule matching design/graphic/creative-agency/branding/photography/art-studio-gallery/videography/production/animation/record-label (no bare `agency`/`studio` → insurance/real-estate agencies + yoga/dance studios never false-match); (b) `verify-hero-image-vertical.mjs` CREATIVE_LEXICON bridge (a creative-domain business + a creative-domain hero query is a correct cluster match). **PROVEN LIVE, no rebuild:** the bridge flips pentagram ❌→✓ [creative-cluster] against its CURRENT deployed hero (correct — a design studio IS a creative agency); the curated hero upgrades future creative builds. hero_image **10/10** (+AL-544 cluster + no-false-match cases), tsc clean. **CONVERSION-FRAMING — coffee roaster → quickserve (AL-549, `cfad77c04`), PROVEN LIVE:** the delivered `stumptown-coffee-portland` (coffee roaster) shipped a **"Reservations welcome"** trust triad + a "Do you take reservations?" FAQ — `commerceModeFor('coffee roaster')` returned `hospitality` (the AL-419 call treated roasters as tasting brands). A coffee roaster's retail face is a WALK-UP counter (vision-caught AL-548), so reservations framing is wrong. Fix: `theme_style.ts` moves coffee roaster/roastery → `quickserve` ("Made to order / Order ahead / no reservation needed"); DISTILLERY/WINERY/MEADERY stay `hospitality` (real tasting rooms). theme_style **109/109**, tsc clean. **PROD-PROVEN:** rebuilt stumptown → trust triad now **["Made to order","Order ahead","Grab & go"]**, "Reservations welcome" GONE, reservation-FAQ GONE, "no reservation needed" present, 0 console errors. **CINEMATIC-3D depth (AL-645):** the HeroSplit photo card now carries a pointer-driven 3D `TiltCard` (perspective + pointer-tracking glare) — Awwwards/Framer/motion.so-signature depth, IDENTITY at rest so the eager hero `<img>` stays the LCP, gated on fine-pointer + `prefers-reduced-motion:no-preference` (touch/reduced-motion → static), rAF-throttled/transform-only (INP+CLS safe); pure `computeTilt` (5 tests) + durable `e2e/site-quality/verify-hero-tilt.mjs`. Template `688ad49`; lands next build. **CINEMATIC-3D depth-PARALLAX (AL-649, template `a829974`) — PROVEN LIVE:** the hero now carries native CSS scroll-driven DEPTH PARALLAX (`<ScrollParallax>` + `.ps-parallax`, `animation-timeline: scroll(root)` with per-layer `--ps-parallax-depth`) — decorative accent orbs drift at DIFFERENT rates on scroll (motion.so/Awwwards layered depth) in BOTH hero variants. Bleeding-edge over GSAP+Lenis on every hard gate: compositor-driven (INP-safe, no JS scroll handler), IDENTITY at scroll 0 (LCP-safe), transform-only (CLS-safe), ZERO bundle cost (pure CSS, no lazy-import); `@supports (animation-timeline: scroll())` + `prefers-reduced-motion:no-preference` gated → Firefox + reduced-motion stay perfectly static. Durable `e2e/site-quality/verify-scroll-parallax.mjs` (auto-joins run-all) + `ScrollParallax.test.tsx` (9/9 vitest incl. hero-wiring guard). **PROVEN on rebuilt pizzeria-bianco-phoenix:** 2 distinct-depth layers drift on scroll + static@reduced-motion + **LCP 720ms ✓ (≤2.0s) / CLS 0 ✓** (verify-cwv) — parallax added zero LCP cost; vision-inspected gorgeous, orbs tastefully subtle at rest. **NAV INTEGRITY now gated (AL-653):** the per-metric probes mostly test the homepage — a generated site's INTERNAL nav (a multi-route SPA: /about /services /gallery /studio /faq /team /case-studies /contact /blog …) had no click-through gate, so a route with no matching `<Route>` or empty pack content could soft-404 (200-shell rendering the 404 page on an indexed URL). New `e2e/site-quality/verify-nav-integrity.mjs` (auto-joins run-all) lands on a site, collects every client-rendered nav/footer internal link, visits each, and asserts real content (200 + non-404 `<h1>` + ≥200 chars + 0 console errors); excludes resource links (.xml/.txt/.well-known — validator-precision, after a `/sitemap.xml` false-positive). **PROD-GREEN on 2 fresh sites: pizzeria-bianco-phoenix 8/8 + wally-workman-gallery-austin 8/8 routes resolve to real content** (16 routes, no soft-404/blank/error) — the full site-quality suite is **31 pass / 1 skip / 0 fail** on the fresh cohort. Durable regression gate for the SPA-route-integrity class. **GROCERY / MARKET vertical-framing + hero root-fixed (AL-656) — golden-journey vision-caught:** a delivered neighborhood grocery (bi-rite-market-sf) shipped a "Free shipping over $50 / 30-day returns / Browse our collection" e-commerce triad + an art-gallery-interior hero — grocery matched `grocer\w*` in the RETAIL commerce regex + had no curated hero. Two root fixes: `commerceModeFor` grocery/supermarket/greengrocer/bodega → **quickserve** (walk-in food, cart/shipping/returns FORBIDDEN; removed grocer from retail; broadened the quickserve brief) + `heroImageForVertical` **GROCERY cluster** (real grocery-aisle Unsplash hero, ixid-decodes-to-noun). RED-first tests (theme_style +2: grocery→quickserve + flea/art-market-stay-retail no-over-match; hero_image grocery cluster) — 159/159 the two suites, hero_copy 75/75, tsc 0. Flips green on grocery-site rebuild (worker CI-deploy). **CINEMATIC-3D Ken-Burns "living imagery" (AL-655, template `bf2a169`) — PROVEN LIVE:** the below-fold FeatureSplit image now slow-SCALES as it scrolls through the viewport (`scale(1.02)→1.1`, the Obys/Immersive-Garden signature), wrapped in a `.ps-ken-burns` layer driven by native `animation-timeline: view()`. Ships ON (not flag-gated); every hard gate held — compositor-driven (INP-safe), transform-only inside the `overflow-hidden` frame (CLS-safe), BELOW-fold (hero LCP img untouched → LCP-safe), zero-bundle; `@supports (animation-timeline: view())` + `prefers-reduced-motion:no-preference` gated → Firefox + reduced-motion get the image at natural 1.0, static. Composes with the img's `group-hover:scale` (nested transforms). **PROVEN on the rebuilt franklin-barbecue:** deployed probe `e2e/site-quality/verify-ken-burns.mjs` GREEN — natural wrapper renders + breathes (in-view scale **1.060 under motion vs 1.000 static under reduced-motion**), LCP intact, 0 console; vision-inspected gorgeous (subtly-zoomed framed About image, no break). Template real-browser CSS-contract proof `e2e/verify-ken-burns.mjs` (`7f830de`) + `FeatureSplit.clip.test.tsx` (4/4). Probe root-fixed AL-651-style from a flaky `drift=false` (footer-adjacent split can't scroll through its range) to the boundary-independent motion-vs-static-scale signal. **CINEMATIC-3D cursor LIGHT-BLOOM (AL-672, template `57cd434`) — PROVEN LIVE:** every WebGL hero scene (all 14 variants at once) now carries a soft brand-tinted glow that FOLLOWS the pointer across the atmospheric field (the Linear/Vercel "the scene is lit where you point" signature) — one `uSpot`/`uSpotStrength` uniform + an ADDITIVE shader term (only adds light → non-black-safe), center-ATTENUATED so it never brightens behind the centered H1 (legibility protected), strength eases 0→1 ONLY on pointer activity so at rest / pre-interaction (the LCP window) / reduced-motion (WebGL path off) it is byte-identical to before → zero LCP impact. Reuses the existing eased-pointer rAF loop (INP-safe) + inherits every existing gate (reduced-motion/no-WebGL/light-theme → static). Durable local `e2e/verify-cursor-bloom.mjs` (build+preview+real-browser: shipped-shader contract + LCP≤2.0s + backdrop-never-the-LCP + reduced-motion-static + 0 console/GL errors → 9/9; filters benign SwiftShader GPU-perf advisories, validator-precision). **PROVEN on the rebuilt koval-distillery-chicago:** dark theme + hero WebGL canvas ACTIVE, warm-edge **LCP 1036ms ✓ (≤2.0s, element=SPAN, canvas never the LCP) / CLS 0 ✓**, 0 console errors after a real pointer-move; vision-inspected — the green brand field visibly BRIGHTENS in the pointer region (left of the H1) while "Made by hand in Chicago" stays crisp. Cross-cutting (lifts every industry scene simultaneously), not one more niche variant. **INTERACTIVE VISITOR JOURNEY now gated (AL-674) — the missing end-to-end CONVERSION+AI gate:** the 38 § C probes are per-METRIC (CWV/SEO/a11y/density/nav/hero) — none exercised the INTERACTIVE sub-actions a real visitor uses. New `e2e/site-quality/verify-interactive-journey.mjs` (auto-joins run-all, 1 site/run for credit-discipline, page-audio R2-cached after first gen) drives the FULL journey on a deployed site: CONTACT (fill→submit→`POST /api/contact-form/:slug` 200 + "thanks" confirmation) · PAGE-AUDIO (click Listen→`POST /api/page-audio/:slug` 200 `audio/wav` non-empty + the app plays a real R2 WAV via a DETACHED `new Audio(...).play()` [hooked the Audio ctor — a DOM `querySelector('audio')` is a false-negative] + button→Pause/Stop) · CHAT (Ask AI→ask→`POST /api/chat/:slug` 200 + reply) · 0 console errors. **PROVEN 7/7 GREEN on fresh koval-distillery-chicago** (contact 200+thanks · page-audio audio/wav 2.4MB + plays `…e302d1bb….wav` + Pause · chat 200 · 0 errors). Surfaced + resolved a stale-build divergence: OLD franklin-barbecue's page-audio returned `application/json`/no-play — disambiguated as STALE-BUILD DEBT (fresh koval green), NOT a template defect (`report-mode-probe-deployed-defect-is-often-stale-build-debt`); **rebuilt the cohort SSOT franklin → page-audio flipped to `audio/wav 3.06MB` + plays, 7/7 GREEN** (also refreshes it to the cursor-bloom + all recent template fixes). The product's core conversion + AI-native journey now has a durable end-to-end gate. **CINEMATIC-3D intro curtain (AL-676, template `ee41843`) — PROVEN LIVE:** a skippable ~1.8s brand INTRO TEASER (`IntroTeaser.tsx`) on the FIRST homepage visit per session — full-bleed brand-gradient wash + business wordmark + sweeping accent line, then it lifts to reveal the site (the motion.so/Awwwards opening). Embarrassingly-easy + a11y: session-once (never a repeat visit / sub-page — homepage-only), `prefers-reduced-motion` → never shows, prominent Skip + Esc + click dismiss, auto-dismiss 1.8s, focus→Skip→main (never traps). **LCP-safe by construction:** the hero renders in `<main>` underneath from t=0 (stays the LCP); the overlay's only contentful element (modest wordmark < hero H1) + a non-contentful gradient bg → the hero is never displaced; transform/opacity exit → zero CLS; no lazy libs. Durable local probe `e2e/verify-intro-teaser.mjs` (build→preview→real-browser, **11/11**: renders · **LCP 204ms el=H1 `inIntro=false`** · auto-dismiss · session-once · Skip · reduced-motion-skip · 0 errors) — proved LCP BEFORE the rebuild. **PROVEN LIVE on rebuilt murrays-cheese-nyc:** intro renders (wordmark + Skip), auto-dismisses, 0 console errors, **warm LCP 768ms (hero IMG, `inIntro=false`) / CLS ≤0.049** (the intro never becomes/regresses the LCP; cold 2636ms was the first-visit edge IMG fetch). The biggest remaining HBO-homepage element, shipped LCP-safe. **CONVERSION-FRAMING — dedicated `gallery` curatorial commerce mode (AL-679, `9b2f0ad04`):** a FRESH gallery build (wally-workman-gallery-austin, "art gallery", org-brian-001, built 2026-09-16 — NOT stale) shipped RETAIL section copy ("The people behind the **counter**", "Find your new favorite", "Browse our collection", "Free shipping") because `commerceModeFor('art gallery')` = **`general`** (no gallery rule) → the content-writer filled the `general` vacuum with retail. AL-641 mapped `galler*`→`luxe` THEME but left the COMMERCE axis unset. Root fix: a new **`gallery`** `CommerceMode` (`theme_style.ts` rule `art\s?galler*|fine\s?art|art\s?dealer|museum|exhibition space`, ordered BEFORE retail; studios excluded) + a curatorial `COMMERCE_INTENT_BRIEF` (View the collection / Plan your visit / Inquire; FORBIDS cart/counter/shipping) injected into the build prompt AND the fast-path seeds; `hero_copy.ts` gallery entries for `heroCtasFor`/`trustBadgesFor`/`seoDescriptionFor`/`homepageFaq` (5 curatorial Q&A). 342/342 theme_style+hero_copy+build_validators (+2 gallery regressions). `verify-conversion-framing.mjs` extended with a gallery check (retail-framing-on-gallery + missing-curatorial-voice) — RED-first proven (flags live wally-workman "free shipping"). **Live-proof PARTIAL (honest):** rebuilt wally-workman + delivered a fresh full `sean-kelly-gallery` — the served bundle now carries the curatorial hero CTA "View the collection" + badges "Original works"/"Represented artists"/"Viewings by appointment" (commerceMode surfaces ✅), but the SECTION copy ("Free shipping", "the counter", "Find your new favorite", "Loved by thousands") persists on BOTH builds — a DEEPER root: a gallery has no `_content.gallery.json`, so `container-server.mjs`'s pack router routes it to the RETAIL content pack (`gen-content-packs.mjs`). Next fire: add a `gallery` content pack + route it before retail; the probe stays tracking until then. commerceMode layer = shipped + half-proven-live. **CONTENT-PACK MIS-ROUTE confirmed BROADER — hotel too (AL-680):** the GOLDEN-JOURNEY delivery of `hotel-emma-san-antonio` (boutique hotel — a fresh vertical; full journey PROVEN: create→build→published→VIEW→analytics 5→8 causal→email-to-brian ok:true; deliver-verify PASS: biz-H1 + both logos transparent AL-224 + 0 console + 6 JSON-LD) shipped the SAME retail section copy ("the counter"/"Find your new favorite"/"free shipping") — because a boutique hotel ALSO has no content pack and mis-routes to RETAIL (its "boutique" token even matches the retail matcher). So the AL-679 gallery residual is really a CLASS: every pack-less vertical (hotel, gallery, …) inherits the retail pack. `verify-conversion-framing.mjs` extended with a `LODGING_SIGNAL` (a boutique hotel is hospitality, never retail) + a shared `RETAIL_MISFIT` (`retail_framing_on_lodging`) — RED-first proven (flags hotel-emma "free shipping"), false-positive-clean on a real retail store (cole-hardware-sf PASSES). The clean root fix (a focused container fire): add `hospitality`+`gallery` content packs (`gen-content-packs.mjs`) + route them before retail in `container-server.mjs`'s pack matcher (BAKED → Dockerfile force-rebuild marker). Partial worker-token seeding is Frankenstein-risky (the retail stat CARDS "10k+ Orders shipped"/"free shipping over $50" are the pack's, not standalone) → deliberately deferred to the pack fire, not rushed. **CINEMATIC-3D product-card depth-tilt (AL-683, template `8bed4c6`):** extended the proven `TiltCard` (pointer 3D tilt + light-catching glare) from the hero to the retail `FeaturedCollection` product grid — each product image tilts toward the pointer with its corner badge floating on a subtle `translateZ` depth-layer (the Awwwards/Framer "parallax within the card"). Zero new deps; only the self-contained `aspect-square` image block is wrapped, so the grid layout is untouched. LCP/CLS-safe by construction — IDENTITY at rest (pointer-only, post-hydration; the depth-layer's translateZ engages ONLY while `data-tilting='1'`), gated on `(pointer:fine)` + `prefers-reduced-motion:no-preference` (touch/reduced-motion → static). Template typecheck + Vite build + tilt tests (8/8) green. Durable prod probe `e2e/site-quality/verify-card-tilt.mjs` (auto-joins run-all) asserts the live contract — rest-identity (no CLS/LCP disturbance) · pointer-activates · reduced-motion-off · 0 console errors — and counts `[data-tilt-layer]` depth badges; fail-OPEN until sites carry the effect. **Verified in-build:** the local `vite build` output CSS contains the exact `.tilt-inner[data-tilt]{transform:rotateX(var(--tilt-rx))…}` + `[data-tilt-layer]` rules (no purge). **Local visual proof (shipped CSS, rest vs tilting, `e2e/_tilt-proof.png`):** the rest card is flat (identity), the active cards show the 3D perspective tilt + pointer-tracked glare + the badge floating on its depth-layer — exactly the intended effect. **Honest live-status:** a `/reset` of luna-felix ran a FAST-PATH (~3min, no template re-clone) so it did NOT apply the tilt CSS — and a fleet check found NO current deployed site carries the `.tilt-inner[data-tilt]{transform}` rule yet (all builds predate it; fast-path never rebuilds CSS — `site-gen-fast-path-renders-seeded-tokens-not-prompt-prose` / `template-section-inert-until-site-gen-fills-tokens`). The probe was hardened to DETECT this (force-vars → if transform unchanged, the CSS isn't deployed → SKIP as stale-build, never false-RED) and flips to a real GREEN assertion on the next FULL retail build (a GOLDEN-JOURNEY delivery), confirming `depthLayers>0`. **CINEMATIC-3D cursor-follow SPOTLIGHT (AL-693, template `d0bb927`) — PROVEN LIVE on the template demo:** the emphatic `CTASection` (the conversion anchor) now carries a `PointerSpotlight` — a soft brand-tinted ambient glow that FOLLOWS the pointer across the CTA (the Linear/motion.so "interactive light" signature the auto-drifting glows lacked). LCP/INP/CLS-safe by construction: a fixed-size gradient div moved by a COMPOSITOR-ONLY `transform: translate3d(var(--px),var(--py),0)` (no repaint of the gradient), the CSS vars updated ≤once/rAF from a PASSIVE `pointermove`; no canvas/WebGL/lib; `aria-hidden` + `pointer-events-none` (never in the a11y tree/hit-test). Gated `(pointer:fine)` + `prefers-reduced-motion:no-preference` → touch + reduced-motion get a tasteful STATIC centered glow (the CSS default), zero motion. `PointerSpotlight.test.tsx` 4/4 (decorative + the gate: coarse→no-track, reduced-motion→no-track, fine+motion→track) + tsc clean. Durable prod probe `e2e/site-quality/verify-pointer-spotlight.mjs` (auto-joins run-all, targets the template demo, fail-open on stale sites). **PROVEN LIVE on `template.projectsites.dev` (deployed via CF Pages — bypasses the site-gen build gate): renders + decorative + `data-tracking=1` + follows the pointer (`--spot-opacity→1, --px=644px`) + `LCP=<H1> 1758ms (glow is NEVER the LCP element)` + 0 console errors** — vision-inspected: the teal ambient light blooms on the cursor side while the gradient headline + CTA stay crisp. Lands on generated sites on the next FULL build. **CINEMATIC-3D 15th WebGL scene `gyro` (AL-699, template `b31ea02`):** `precision` (engineering / motorsports / machining / aerospace) SHARED the soft cellular `mesh` with `futuristic` — undersold its calibrated-instrument identity. New hand-GLSL scene (uMode==12): concentric orbital RINGS drifting outward under a slow radar SWEEP arm that lights the rings it crosses (a precision-gauge / gyroscope). `PRESET_BACKDROP.precision → 'gyro'` (futuristic keeps mesh); fills the "advanced per-industry WebGL scenes" + particle/field gaps. **LCP/INP-safe by construction (INHERITED — only a new fragment branch + config + modeFlag added; the decorative aria-hidden canvas mount / WebGL-post-mount / reduced-motion·no-WebGL·light-theme→static-gradient pipeline is UNCHANGED from the 14 proven variants).** Reusable `demoVariantOverride(hostname, search, fallback)` — `?bg=<variant>` previews any scene, gated to the demo host + localhost (a visitor URL param can never change a real customer hero; `hasOwnProperty` rejects `__proto__`). **23 vitest green** — the shader-source-gate (RED-first: asserts the `gyro` branch compiles into FRAG_SRC + `effectiveVariant==='gyro'?12` modeFlag + the uMode ladder is contiguous 1..12, else a variant silently renders mode-0 aurora) + 15-variant config sanity + `backdropForPreset('precision')==='gyro'` + every-preset-maps drift guard + the override cases. **PROVEN LIVE (real browser, prod):** the WebGL backdrop infra gyro rides RENDERS on the prod PRECISION site `union-garage-nyc` (motorcycle gear) — `verify-hero-backdrop` **canvas=1 inHero=✓** @1280, LCP-safe (added to the probe's default SITES); the gyro fragment lands + is visually confirmable on union-garage's NEXT build. Lands on generated sites on the next FULL build. **CINEMATIC-3D scroll depth-cascade (AL-708, template `2c6a2de`):** a genuinely-NEW motion class — the motion.so/Awwwards "content emerges from depth" signature. `<DepthCascade>` + `.ps-depth-cascade` make a group's direct children CASCADE forward out of Z-depth (container `perspective:1200px` + child `rotateX(14deg)`+`translateZ(-90px)` → identity, `:nth-child`-staggered) as it scrolls into view — true 3D depth vs the flat `.reveal-on-view` translateY rise. **Pure native `animation-timeline: view()` — ZERO JS/no scroll listener** (safest possible motion, unlike GSAP/Lenis): LCP-safe (below-fold, settled frame identity, NEVER the hero), INP-safe (compositor), CLS-safe (transform+opacity, opacity floored 0.9 for AA). Firefox + reduced-motion fall through both gates → STATIC + fully visible. Dark by default (`scrollCinemaEnabled()`/`VITE_SCROLL_CINEMA`); every below-fold section is already motion-saturated (per-item reveals), so an adopter converts a flat reveal to `<DepthCascade>` on promotion rather than double-revealing (the clip_reveal precedent). **PROVEN in real Chromium** (`e2e/verify-scroll-cinema.mjs`, template): contract applies under fine-motion (`animation-name: ps-depth-cascade-in`, `timeline: view()`), `perspective:1200px`, `none` under reduced-motion, 0 console errors, **LCP 384ms `<h1>` ≤2.0s + hero never a cascade item**; +4 Vitest, tsc clean. Lands on generated sites on the next FULL build. **CINEMATIC-3D ambient particle field (AL-720, template `d5f7c38`):** the one classic ambient staple the template lacked — luminous brand-tinted motes drift behind the emphatic `CTASection` (the Awwwards/motion.so "living canvas"). `ParticleField.tsx` Canvas 2D, NO WebGL/lib (~2KB), a pre-rendered radial-glow sprite blitted per mote with additive (`lighter`) blending. **LCP-safe by construction** — the canvas mounts ONLY after a guaranteed `requestIdleCallback(cb,{timeout:1500})` (post-first-paint, below-fold, `aria-hidden`, never the LCP element); INP-safe (one lightweight rAF PAUSED on tab-hidden + off-screen via IntersectionObserver, passive pointer parallax); CLS-safe (`absolute inset-0`); `prefers-reduced-motion`/no-canvas/flag-off/SSR → renders `null`. Dark by default (`particleFieldEnabled()`/`VITE_PARTICLE_FIELD`). **PROVEN LIVE (real Chromium, flag-on preview — `e2e/verify-particle-field.mjs`):** canvas mounts 974×352 + **ANIMATES** (framebuffer luma 22.25M→21.99M) + **LCP 420ms `<h1>`** (≤2.0s, never the canvas) + 0 console errors + reduced-motion → NO canvas; +4 Vitest, tsc clean. Two failing-probe-first debugging wins (PNG-tail-slice → getImageData luma diff; untimed→timed idle-mount for a deterministic post-LCP mount). Lands on generated sites on the next FULL build (promote fleet-wide by injecting `VITE_PARTICLE_FIELD=1`). **CINEMATIC-3D depth-cascade PROMOTED default-ON + first natural adopter (AL-782, template `0c3c860`):** the exact "adopter converts a flat reveal to `<DepthCascade>` on promotion" step AL-708 set up. Wired `<DepthCascade>` onto `FeaturedCollection`'s retail product grid (cards emerge from Z-depth on scroll instead of the flat `reveal-on-view` rise; cards DROP `reveal-on-view` so the cascade owns the entrance — no double-transform) AND flipped `scrollCinemaEnabled()` from experimental-dark to **default-on** (`VITE_SCROLL_CINEMA!=='0'` killswitch): its safety profile is IDENTICAL to the always-on `.reveal-on-view` (both native `animation-timeline:view()` + same static-visible fallback), so keeping it dark was the built-but-unwired anti-pattern. Zero regression (modern→3D cascade, Firefox/reduced-motion→static-visible either way). **PROVEN in real Chromium on the SHIPPED-DEFAULT build** (`verify-scroll-cinema.mjs` built with NO env override): `ps-depth-cascade-in` + `view()` + `perspective:1200px` apply, reduced-motion→`none`, 0 console errors, **LCP=412ms `<h1>` ≤2.0s (never in a cascade)**; injected retail grid confirmed a live `matrix3d` rotateX(14°) transform; +3 unit tests (wiring + no-double + killswitch) RED→GREEN, full template suite 386 green, tsc clean. Retail-site live visual awaits a rebuild (build-LLM credit-blocked). **✅ KINETIC MARQUEE (CINEMATIC-3D fire):** shipped the Framer/Awwwards velocity-marquee — a perspective-tilted (`rotateX(14°)`), scroll-velocity-coupled typographic ribbon of the business's OWN offering words (`KineticMarquee` + `kinetic_marquee`/`VITE_KINETIC_MARQUEE`, dark-by-default; native CSS `animation-timeline: scroll(root)` scroll-coupling + a universal continuous-drift keyframe, ZERO libs). Real-browser flag-ON preview PROVEN (7/7): drift under motion / STATIC under reduced-motion / `overflow:hidden` clip / edge mask / **LCP=`<h1>` 320ms (never in the band)** ≤2.0s / 0 console errors @1280+390+reduced; vision 9/10 (`_kinetic-marquee-1280.png`). Durable probe `verify-kinetic-marquee.mjs` (auto-joins run-all, fail-open on dark/stale). +4 unit tests RED→GREEN, tsc clean. **✅ BLOG-RELEVANCE gate honesty restored (GENERATED-SITE QUALITY fire):** run-all surfaced `blog-relevance` 🔴 FAIL — franklin-barbecue + pizzeria-bianco ship the pre-AL-740 web-dev/SEO blog (`/blog/website-speed-core-web-vitals-2026`, `local-seo-checklist`, `google-ai-overviews`) — a BBQ joint blogging about Core Web Vitals. Root ALREADY fixed (AL-740 made `content.ts` defaults vertical-neutral: "The small things, done well" / "The people behind the work"); the 2 sites are STALE pre-AL-740 builds (wally-workman, fresh, is clean). But the probe fail-CLOSED on stale → a permanent false-RED violating "root lands next build, NO redeploy." Dual root fix: (1) `verify-blog-relevance.mjs` now fail-OPENS on the KNOWN stale web-dev/SEO blog (rebuild-worklist ::notice, like the sibling cohort-freshness), hard-fails only on a render/audit error — verdict now **✅ PASS (fail-open), 2 stale worklisted**; (2) durable template source guard `src/data/content-blog.test.ts` (2/2) locks `content.ts` against a regression to web-dev/SEO topics at BUILD time, so fail-open can't hide one. Template suite 410/410. **✅ SCROLL-STACK DECK (CINEMATIC-3D fire):** shipped the Apple/Awwwards "cards stack & recede as you scroll" signature — the ONE recognized cinematic move genuinely absent from the template's 9 existing flags. The How-it-works steps become a vertical DECK of sticky cards that PIN (`position:sticky`) + RECEDE (`scale .93`/`opacity .62`) as the next slides over (`ProcessSteps` `stack` prop + `scroll_stack`/`VITE_SCROLL_STACK`, dark-by-default; native CSS `animation-timeline: view()`, ZERO libs, template `featureFlags.ts`+`index.css`+`ProcessSteps.tsx`+`Home.tsx`). Real-browser flag-ON preview PROVEN (7/7): sticky-pin+recede under desktop-motion / plain STATIC list under reduced-motion AND mobile (the min-width:768 gate — no phone pin-thrash) / **LCP=`<h1>` 360ms (hero, never in the deck)** ≤2.0s / 0 console errors @1280+reduced+390; vision ~9/10 (`_scroll-stack-1280.png` — 4 tactile cards, clean dark deck). Durable probe `verify-scroll-stack.mjs` (auto-joins run-all, deploy-detects via `position:sticky`, fail-open on dark/stale — **prod 3/3 skip rc=0**). +3 unit tests (`ProcessSteps.test.tsx` flag-gate + stack-vs-grid structural contract) RED→GREEN, tsc clean. Ships fleet-wide on the depth-cascade-style default-on promotion once a FULL build carries it. *(CINEMATIC-3D)*

- [x] **C.7 distinctiveness FURTHER ADVANCED — pinned cinematic scrollytelling (AL-731)** — shipped the pinned scroll-scrubbed "cinematic process reel" (flag `cinematic_process`, template `ecb446d`): the Apple/Awwwards PINNED-scrollytelling signature the one-click AI builders don't ship. The How-it-works steps become a pinned reel — a sticky stage cross-dissolves each step "act" one at a time as you scroll, a cyan progress rail drawing through the chapter, a giant faint ghost number per act. Native CSS `view-timeline` (compositor → INP-safe, ZERO JS/no scroll listener), below-fold + opacity/transform-only (LCP+CLS-safe); base state = a fully-visible static stack (Firefox / reduced-motion / <md / no-JS). Durable real-browser probe `e2e/site-quality/verify-cinematic-process.mjs` (auto-joins run-all): builds the template with the flag + fills PROCESS_* tokens → renders + **scrubs 0→3 one act at a time**, **LCP stays the hero (204ms, never in .cine-process)**, reel-CLS=0, axe-clean, 0 console errors, reduced-motion→static. Vision-inspected ~9/10 (giant ghost "03" + "You review" + rail). DARK by default (ships to fleet on promotion). *(CINEMATIC-3D, AL-731)* **MAGNETIC CTA (AL-738):** the hero primary CTA now LEANS toward a nearby cursor + a brand halo tracks it, springing back at rest — the Framer/Awwwards signature. Resurrected the DEAD idea-#162 `MagneticButton` (unused, no CSS, raw `<button>`) into a shipped `<span>` WRAPPER (composes over the `asChild` link), wired into `HeroCenter`+`HeroSplit`. LCP/a11y-safe BY CONSTRUCTION: IDENTITY at rest (hero stays the LCP element, 0 CLS), gated on `(pointer:fine)`+`prefers-reduced-motion:no-preference` (touch/reduced-motion → plain static button, zero listeners), `pointermove` rAF+passive (INP-safe), click target unchanged (embarrassingly-easy). +`computeMagnet` +6 vitest (371/371 full) + durable probe `verify-magnetic-cta.mjs` (RED-first proven; asserts leans-near/releases-far + **LCP element is the hero not the wrapper** + reduced-motion-static). Auto-on (safe, like TiltCard) → ships fleet-wide on rebuild. *(CINEMATIC-3D, AL-738)* **ON-BRAND BLOG (AL-740):** every generated site's blog shipped the SAME 6 web-marketing posts ("Core Web Vitals", "local SEO checklist", "Google Business Profile", "AI Overviews SEO") — a restaurant/gallery blogging about web performance = off-brand filler that fails beat-the-source. Root-fixed in template `src/data/content.ts`: rewrote all 6 as warm, universal, CUSTOMER-facing local-business topics (craft · people · community · welcome · neighborhood · seasons) that read on ANY vertical, zero web-dev/SEO jargon. Feeds regenerated, `npm run build` green. Durable probe `verify-blog-relevance.mjs` (no-jargon gate) — RED-first proven + a probe hydration-timing bug fixed same fire. *(GENERATED-SITE QUALITY, AL-740)*
- [x] **C.8 Build-freshness triage — stale-build debt separated from genuine gaps (probe SHIPPED, AL-623)** — the per-site probes flag a DEPLOYED site RED whether the generator is broken OR the site is just an OLD build predating a root fix (the recurring `report-mode-probe-deployed-defect-is-often-stale-build-debt` class that wastes loop fires re-diagnosing already-fixed "bugs"). New `e2e/site-quality/verify-cohort-freshness.mjs` (auto-joins run-all) applies the doctrine's differential mechanically: measures the prerendered-shell invariants (title 50-60 / desc 120-156 / ≥4 JSON-LD / single H1) on a KNOWN-FRESH CONTROL + each cohort site, then classifies **CURRENT** / **STALE-REBUILD** (fails only what the fresh control PASSES → the fix landed; rebuild) / **GENUINE-ROOTFIX** (control fails it too → real gap; code fire). Emits a rebuild worklist + a root-fix worklist; only genuine gaps red the suite (STRICT). **PROVEN:** control `bario-neal-philadelphia` fails [none]; `vanta-strength-austin` STALE-REBUILD [title,desc], `ironhaus-houston` + `vantage-digital-studio-portland` STALE-REBUILD [desc], `alpenglow-sports-tahoe-city` **CURRENT** (title 56 / desc 154 / jsonld 4 / h1 1) → **0 genuine generator gaps · 3-site rebuild worklist**. Confirms the § C map is genuinely saturated (fresh builds beat the source) and the example sites' RED is pre-AL-110 stale-build debt (meta clamp), cleared by a rebuild — blocked for me (they're not org-brian-001 → `/reset` 404; handed off as the worklist). *(GENERATED-SITE QUALITY, AL-623)* **VERTICAL-MISFIT staleness added (AL-712):** the 4 meta-invariants (title/desc/jsonld/h1) can ALL pass on a build that's still STALE on its VERTICAL — vision-caught on `pike-place-fish-market-seattle` (fresh meta: title 56 / desc 155 / 6 JSON-LD / 1 H1, so it was misclassified **CURRENT** and MISSED from the rebuild worklist) yet it wears the pre-AL-698 boutique-FASHION persona: an art-gallery hero + "a chic fish market … chosen with a tastemaker's eye" / "Browse our collection" / "30-day returns" on a walk-in seafood market. `measure()` now adds a precise `vertical` fail = FOOD/MARKET vertical noun (fish market/seafood/grocer/butcher/deli/…) co-occurring with an unambiguous boutique-fashion marker (`tastemaker`/`a chic`/`pieces worth the trip`) — a correct quickserve food build never carries these, so it's ALWAYS stale-build debt (the AL-698 theme_style+hero_image fix + its 4 unit tests are landed → rebuild clears it). **PROVEN:** pike-place → 🟠 STALE-REBUILD [vertical] (now on the worklist); koval-distillery + zahav (non-food) stay CURRENT, jewelry control clean → no false-flag. The rebuild worklist is now COMPLETE (catches vertical-misfit, not just meta-invariant staleness). Rebuild the cohort once the build-LLM is funded (AL-707 top-up gate). *(GENERATED-SITE QUALITY, AL-712 — verify-cohort-freshness)*
- [x] **C.9 Security headers on generated sites (fleet-wide, now GUARDED)** — the `quality-metrics` rule mandates HSTS + CSP(clickjacking) + X-Content-Type-Options + Referrer-Policy + Permissions-Policy on the core product's public surface, and ~48 site-quality probes cover CWV/SEO/a11y/JSON-LD/PWA/density — but **NONE asserted the security headers**. A regression stripping HSTS (downgrade), dropping `frame-ancestors` (clickjacking), or losing `nosniff` (MIME-confusion XSS) on `{slug}.projectsites.dev` would ship silently, fleet-wide (they come from the Worker's global `securityHeaders` middleware). New `e2e/site-quality/verify-security-headers.mjs` (`fetch`-based, auto-joins run-all) asserts, per site: HTML routes (home + a discovered sub-page) carry HSTS `max-age≥1yr`+`includeSubDomains`+`preload`, `nosniff`, Referrer-Policy, Permissions-Policy, and a CSP that is clickjacking-safe (`frame-ancestors` present + NOT wide-open `*`) with `object-src 'none'`; a real hashed asset carries HSTS + nosniff. **PROD-VERIFIED GREEN 18/18** across the 3-vertical cohort (franklin-barbecue · pizzeria-bianco-phoenix · wally-workman-gallery-austin) — the posture is solid + now regression-guarded. COOP is intentionally absent (generated sites embed cross-origin Maps/Stripe → COOP `same-origin` + COEP would break embeds). **KNOWN GAP for a NEXT fire (build-baked, documented not fixed):** generated-site `llms.txt` ships blank `Tagline:`/`Hours:` + a malformed `Contact:  | phone` (empty email → dangling `|`) when those brand fields are empty — root is the token substitution in `scripts/container-server.mjs` `loadContentMap` (empty token → empty string, not line-omission; same class as the existing `BUSINESS_DESCRIPTION` empty-meta fallback there). Fix = prune empty `Key: ` lines + collapse the dangling `|` on the Contact line after substitution + unit test; lands next build. *(GENERATED-SITE QUALITY, AL-753 — verify-security-headers)*
- [x] **C.10 Mobile horizontal-overflow — NEW dimension GUARDED (AL-760)** — axe (verify-a11y) is BLIND to horizontal overflow: a section a few px past the viewport makes the whole page scroll sideways on a phone (most traffic), losing to the source it should beat — yet all 49 prior probes render at ONE width + never measure cross-axis overflow. New `e2e/site-quality/verify-mobile-overflow.mjs` (auto-joins run-all) loads home + /contact + /about at 375/390 (phone, hard) + 768 (tablet, report) and asserts `documentElement.scrollWidth ≤ innerWidth` (page-level — an internal `overflow-x:auto` carousel does NOT trip it); on a trip it names the culprit element (right-edge overshoot) so the root-fix is targetable. **PROD-VERIFIED ✅ PASS 5/5** (vanta · ironhaus · vantage · franklin · pizzeria — no phone overflow) → the template is clean on this axis + now regression-guarded. *(GENERATED-SITE QUALITY, AL-760 — verify-mobile-overflow)*
- [x] **C.11 Heading hierarchy — fleet-wide H1→H3 skip ROOT-FIXED in TEMPLATE + probe (AL-760)** — the first-after-hero BentoGrid highlights grid is generated WITHOUT a `headline` → no section `<h2>` → but tile titles were always `<h3>`, so the page outline skipped **H1 → H3** on 4/5 deployed sites (vanta · ironhaus · franklin · pizzeria; WCAG 1.3.1 / axe heading-order — verify-a11y hard-fails only AA violations + heading-order is a best-practice rule → the gap shipped fleet-wide, no probe caught it). Root fix in the TEMPLATE (`BentoGrid.tsx`, commit `b788736`): tile titles are `<h3>` only when a section `<h2>` renders, else `<h2>` (no skip, no invented headline; the eyebrow is a decorative `<span>` so only `headline` counts). +3 vitest regression tests (`heading-level.test.tsx` 6/6) + full template build/validate-site green. New `e2e/site-quality/verify-heading-hierarchy.mjs` (report-mode default, `--strict` hard-fail, auto-joins run-all) asserts exactly one `<h1>` + no level-skip; **PROD-detected the live skip 4/5** (report-mode exit 0) → flips ✅ as the cohort rebuilds off the fixed template (build-LLM credit-blocked; lands next build per loop guardrail). *(GENERATED-SITE QUALITY, AL-760 — verify-heading-hierarchy)*
- [x] **C.7 distinctiveness FURTHER ADVANCED — cinematic hero DOLLY (AL-762)** — the hero content recedes (scale-fade-lift) as a coordinated unit as it scrolls off the top: the Apple/Awwwards camera-pull-back that ScrollParallax (decorative-orb drift) never covered. Native `animation-timeline: view()` + `animation-range: exit` → IDENTITY at first paint (LCP-safe: local-render proved LCP stays the hero `<h1>`, transform `none` at rest), transform+opacity only (compositor/INP-safe), `@supports`+`prefers-reduced-motion` gated (static fallback). TEMPLATE `f8e93ca` (both HeroCenter+HeroSplit + 2 vitest tests + full build green). Probe `verify-hero-dolly.mjs` (report-mode, auto-joins run-all) — PROD-detects the effect + LCP-safety; flips ✅ as sites rebuild. *(CINEMATIC-3D, AL-762 — verify-hero-dolly)*
- [x] **C.12 Image optimization — responsive srcset + modern format ROOT-FIXED (AL-765)** — the generator ships raw Unsplash `?fit=max&fm=jpg` URLs: ONE full-width JPEG to every viewport, **`responsive=0` srcset across the whole live cohort** (a phone downloads a ~1000px JPEG for a 390px slot) + `fm=jpg` forcing on older sites — the biggest CWV/bandwidth + beat-source gap, and NO prior probe covered image FORMAT or `srcset`. Root fix in the TEMPLATE (`lib/cdn-image.ts` `cdnImageProps()` — rewrites Unsplash/Pexels/Pixabay URLs to `auto=format` (AVIF/WebP) + a 5-width `srcSet` + `sizes`, idempotent; wired into the HeroSplit LCP image; +5 unit tests + HeroVariants 7/7 + full build green; TEMPLATE `497c219`). Probe `verify-image-optimization.mjs` (report-mode, auto-joins run-all) flags forced-JPEG + tracks srcset adoption; **PROD: 3/5 already auto-format but 0/5 responsive** → all gain responsive + the 2 `fm=jpg` heroes gain modern format on rebuild. *(GENERATED-SITE QUALITY, AL-765 — verify-image-optimization)*
- [x] **C.13 Broken-image render — a visitor-facing dimension no prior probe covered (AL-786)** — `verify-no-asset-404.mjs` catches an image whose REQUEST 404s, but a broken-image glyph ALSO ships when a src returns a 200 that isn't a decodable image (a stock-API rate-limit placeholder, a wrong content-type / HTML error body served as `image/*`, a CORS-tainted decode, a truncated file) — every request "200 OK" → invisible to the 404 probe, yet the visitor sees the broken glyph (strictly worse than the source). New durable prod probe `e2e/site-quality/verify-image-render.mjs` (auto-joins run-all via the `verify-*.mjs` glob): scrolls top→bottom→top to trigger `loading="lazy"` fetches, then flags any VISIBLE `<img>` that is `complete && naturalWidth === 0` (load settled, zero pixels decoded) — never flagging an un-triggered lazy image (`complete === false`) or a 1×1 tracking pixel. Fail-OPEN by default (::notice — a flaked stock URL on a pre-fix build clears on rebuild), `STRICT=1` → exit 1 (a fresh build shipping a broken image is a build-breaker). **PROD-VERIFIED GREEN: 0 broken images across koval-distillery-chicago (17 imgs) · pizzeria-bianco-phoenix (18) · murrays-cheese-nyc (17)** — the image pipeline is already robust; this is the durable regression-guard. Paired PREVENTION root-fix in the TEMPLATE (`Image.tsx` `onError` — a broken image hides its whole `<picture>` so the flex/grid reflows cleanly, never a broken glyph; composes the caller's `onError`; +3 Vitest RED→GREEN; TEMPLATE `39cf375`, lands next build). *(GENERATED-SITE QUALITY, AL-786 — verify-image-render + Image.tsx fail-soft)*

## § D — Platform marketing / SEO (projectsites.dev's OWN public face) · OWNER: GENERATED-SITE QUALITY (extends)
- [x] **D.1 Homepage `/`** — SEO/CWV/a11y/JSON-LD, 0 console errors. ✅ VERIFIED on prod (AL-197 + AL-198): exactly-1 H1, **0 console errors**, server-injected title + distinct meta-description + canonical + 12 OG tags + 5 JSON-LD blocks (AL-197); **CWV measured green AL-198** via chrome-devtools perf-trace on `/` — **LCP 192ms** (TTFB 3ms + render 189ms), **CLS 0.00** — far under the 2.0s / 0.05 gates (INP not separately traced — homepage hero has no heavy interaction; Lighthouse-perf category excluded by the MCP audit tool but CWV is the substantive signal). *(completion-map convergence)*
- [x] **D.2 Public routes** — blog · changelog · status · privacy · terms render + SEO. ✅ VERIFIED on prod (AL-197): `/blog /changelog /privacy /terms` all HTTP **200** with DISTINCT SEO titles + distinct meta-descriptions + per-route `<link rel=canonical>` + OG tags + 5 JSON-LD blocks each (server-injected via the Worker HTMLRewriter). **NOW DURABLY GATED + CORRECTED (AL-502):** new `e2e/site-quality/verify-platform-public-face.mjs` (auto-joins run-all) re-asserts this every pass → **5 CONTENT routes full-SEO (137-144c descs, 5 JSON-LD, distinct titles) GREEN**. Corrected two AL-197 over-claims the durable probe surfaced: (1) **`/contact` is an intentional 301 → `/search#contact-section`** (the funnel's contact surface — the probe follows it → final 200 + 5 JSON-LD), NOT a direct 200 content page; (2) **`/status` is a UTILITY page served the SPA shell** (client title "Status · Project Sites", NO server canonical/desc/JSON-LD) — a status page is not a marketing/indexable target, so the probe hard-gates only 200+title and TRACKS the thin SEO as a ::notice (validator-precision — not a false red). **`/status` SEO-ENRICHED + hard-gated (AL-685):** the dedicated inline system-status handler (`index.ts` — nothing writes `marketing/status.html` so the inline HTML always serves) shipped `<title>` ONLY (0 canonical, 0 meta-desc, live-confirmed). Root-fixed its `<head>`: self-referential `<link rel=canonical>` + a 143c `<meta name=description>` + `robots: index,follow` + full OG (type/site_name/title/description/url) + a proper 50c SEO title ("System Status — Live Service Health | ProjectSites") — a status page IS a legit-indexable public target (cf. status.stripe.com). The probe's tracked `::notice` was UPGRADED to two HARD gates (self-canonical + desc 120-156c) → a regression stripping the status meta now goes RED. *(GENERATED-SITE QUALITY § D, AL-197 verify + AL-502 durable probe + AL-685 /status enrichment)*
- [x] **D.3 404** — real 404 status + branded page (no soft-404). ✅ VERIFIED on prod (AL-197): a bogus path (`/this-does-not-exist-xyz-123`) returns HTTP **404** (not a soft-404 200) while every real route returns 200; the branded 404 renders via the SPA shell per the soft-404 doctrine (real 404 status + shell → the styled 404 route). *(completion-map convergence)*

## § E — Editor (bolt.diy) · OWNER: COMPLETENESS + FULL JOURNEY
- [x] **E.1 Functions + Data workbench tabs** — real, connected (AL-004/018/038/060)
- [x] **E.2 WebContainer boot + edit→save→persist round-trip** — PROVEN live on prod via Browserbase real Chrome (AL-145, `verify-editor-webcontainer-roundtrip.mjs`, wired into run-all): booted the real WebContainer editor → typed a unique marker into CodeMirror → the "Save" affordance APPEARED (file dirty, `unsavedFiles.has`) → clicked Save → affordance CLEARED (workbench `saveFile` awaits the WebContainer FS write BEFORE removing from `unsavedFiles`, so cleared ⟺ persisted) → the marker SURVIVED a file-switch round-trip (re-read from the store/FS, not just the live buffer) → 0 console errors. Fail-open SKIP on boot-timeout (headless flakiness is the boot, not the round-trip); once the editor is interactive a broken round-trip FAILS. Closes the last heavy flow B.6 deferred as "$-gated by nature."

---

## Progress (recompute each fire)

- **§ A Admin:** 100% ✅ (plateau — maintenance-only)
- **§ B Full flows:** 10 / 10 = 100% ✅ (all flows have a headless-prod probe wired into run-all; B.5 billing-full AL-111 · B.6 editor round-trip AL-127 envelope · B.7 auth AL-094 · B.7b guest sign-UP AL-735 · B.1 guest funnel AL-089. The out-of-headless legs — B.5 real-card charge, B.6 full WebContainer publish — are Browserbase/$-gated by nature + covered by unit tests / the FULL JOURNEY loop; FULL-FLOW is now maintenance-only) **RETURNING-OWNER MANAGE JOURNEY now gated (AL-658):** the acquisition path was well-covered but the DAILY-use owner loop (sign in → open a site → walk its detail tabs) had only unit-level specs, no cohesive headless-prod probe. New `e2e/admin-verify/verify-owner-site-journey.mjs` (wired into run-all) drives it click-only: dashboard sites-list → click a site → detail opens with real business-name H1 + live `{slug}.projectsites.dev` reference → every `sd-tab-*` renders real content → 0 console errors. **PROD-GREEN 7/7** (2 sites → Kessler & Boyd → 4 tabs). Regression sweep confirmed the around-golden flows all green (billing-full/auth-flow/editor-roundtrip/create-wizard). Vision-inspect Rec (design call): the global site-switcher label can diverge from the drilled-in detail (`admin-site-selection-in-memory-not-persisted`) — surfaced, not auto-synced (the switcher is a deliberate global scope).
- **§ C Generated-site quality:** 7 / 7 = 100% ✅ (C.1 SEO-invariant finalizer AL-110 · **C.2 CWV edge-cache AL-394 PROD-VERIFIED** cold TTFB 1.3s→~70ms via `caches.default` · C.3 axe/muted-contrast AL-140/290 · **C.4 SEO/GEO** `verify-seo.mjs` 7/7 · **C.5 per-route JSON-LD** AL-206/213 · **C.6 PWA** `verify-pwa.mjs` 6/6 AL-212 · **C.7 beat-the-source** `verify-beat-source.mjs` AL-399 PROD-GREEN (gentle-dental +23% / tartine +78% vs real sources; headless density + warm speed + soft-404-aware PWA) — all prod-verified/root-fixed. Density lever ROOT-FIXED across the thin cluster (AL-474): after AL-466's uniform-FAQ boost, the 6 thin per-vertical packs (wellness/retail/portfolio/agency/dental/nonprofit, all <700-floor proxy) got densified `HERO_SUBHEADLINE`+`ABOUT_DESCRIPTION` in `gen-content-packs.mjs` — all now ≥700 proxy (dental 767, retail 742), real ~800+; flips `verify-content-density.mjs` green as sites rebuild. Template `32b9ffd`. **FRESH-BUILD PROD-VERIFICATION (AL-480):** the recent § C fix fleet was mostly "flips on rebuild (unverified — verified only on pre-fix sites / unit tests)". `perennials-brooklyn` (delivered AL-477, the first fresh build POST-AL-110/474/475) now PROD-VERIFIES the fleet LANDED end-to-end: **C.1** title 52c + desc **153c** (finalizer expansion works) + jsonld 4 + h1 1 · **C.5** 4/4 routes × 4 blocks · **C.6** 6/6 + short_name "Perennials" (word-boundary, AL-475) + non-empty manifest desc (AL-475) · **C.7 density** 757w/18imgs. **ONE open dimension confirmed (corrects AL-425):** C.7 hero-image-vertical does NOT flip green on a fresh rebuild for RETAIL SUB-VERTICALS — perennials (plant shop) shipped the generic pack hero `IMG[retail].hero` ("cozy independent shop interior shelves"), NOT a plant hero, because the fast-path (~150s) renders the SEEDED `{HERO_IMAGE_URL}` token = the pack's generic retail image; AL-424's buildPrompt "pick the vertical-specific _assets.json image" instruction is INERT for fast deliveries (AL-409). ROOT FIX (follow-on slice) = SEED a vertical-specific hero token at delivery time (a curated sub-vertical hero map for plant/garden/nursery/florist/record-store/etc.), not a buildPrompt instruction.) **C.1 TITLE BELT + AUDIT-COHORT SSOT (AL-657) — empirical prod audit:** `run-all` surfaced 3 reds (a11y/pwa/build-invariants) — ALL on `vanta+ironhaus` (e2e-test-org, built 2026-09-05, predate every fix, un-rebuildable via brian). Two root fixes: (1) `finalizeSeoInvariants` title belt — a long city (`San Francisco`) made ` | {city}` overshoot 60 so a 48-char title shipped under the 50 floor (bi-rite); added a ` | {region}` (2-letter state) fallback → PROVEN LIVE on a bi-rite rebuild (title 48c→**53c** "…grocery store | CA"), 122/122; (2) new `e2e/site-quality/_default-sites.mjs` SSOT points the 6 stale-defaulting § C probes at the CURRENT org-brian-001 cohort (franklin/pizzeria/wally, each proven a11y+pwa+invariants) — killing the `reconcile-surface-map-can-be-stale-false-red` class → **`run-all` 31/3-fail → 34/0-fail**. Also proved AL-656 grocery→quickserve LIVE on the same rebuild (hero = real grocery-market aisle, trust chips "Made to order/Order ahead/Grab & go", the "Free shipping/30-day returns" triad GONE). Full visitor journey (franklin contact-leg 200 + "Thanks!" + 0 console + a11y 0×6bp) proven. `e640295bb`+`51470730e`. **DISTILLERY hero cluster — PROVEN LIVE (AL-661/662):** golden-journey delivery koval-distillery-chicago vision-caught a craft distillery showing the generic art-gallery hero (`heroImageForVertical` had no distillery rule). Added a DISTILLERY cluster (real copper-pot-stills Unsplash hero + rule `/\bdistiller\w*\b/`, RED-first hero_image 20/20). **PROVEN LIVE on a koval rebuild:** `verify-hero-image-vertical` GREEN ("distillery copper still spirits" matches the vertical) + vision-inspected (gleaming copper pot stills, was the gallery interior), 0 console. Framing already correct (AL-549 distillery→hospitality). `1baff4725`.
- **§ D Platform marketing:** 3 / 3 = 100% ✅ (D.1 homepage SEO+CWV green AL-197/198 · D.2 public routes AL-197 · D.3 404 AL-197). **D.1 DATA-HONESTY HARDENED (AL-581):** the homepage social-proof band hard-coded a FABRICATED `<app-rolling-counter [value]="2480"> "Sites Built"` while the real D1 published COUNT was **182** (~13× inflation) — a lying stat on our OWN public face that painted cleanly + passed every SEO/route probe (`verify-against-source-of-truth` blind spot). Root-fixed: new worker `GET /api/public/stats` (LIVE published COUNT, edge-cached 10min, fail-soft) + homepage counter bound to a `sitesBuilt` signal refreshed from it (honest + self-updating, never blanked). New durable gate `e2e/site-quality/verify-platform-stats-honest.mjs` (auto-joins run-all) — the first § D probe that reconciles a NUMBER vs the store: three-way D1 store → endpoint → rendered display, hard-gates ≠ 2480. **PROD-VERIFIED 6/6:** endpoint `{"sites_built":182,…}` == D1 182 == rendered aria "182+", never 2480.
- **§ E Editor:** 2 / 2 = 100% ✅ (E.1 tabs AL-004/018/038/060 · E.2 WebContainer edit→save→persist round-trip PROVEN live via Browserbase AL-145 — the last heavy flow closed)
- **Overall app "done": ~94%** (30 / 32 boxes) — admin + editor finished, money flow live+secure, platform § D fully green, and generated-site **§ C now 7/7** (C.2 CWV edge-cache AL-394 · C.4 SEO/GEO · C.5 JSON-LD · C.6 PWA prod-verified; C.1/C.3 root-fixed; **C.7 beat-the-source AL-399 PROD-GREEN** — headless-fair harness, gen beats real sources +23%/+78% on the objective subset + structurally dominates PWA/JSON-LD/speed). The 2 remaining boxes are the deliberately-deferred $-/vision-gated legs (B.5 real-card charge, B.6 full WebContainer publish) — covered by unit tests + Browserbase, not headless-provable. **The core product is now measurably better than the businesses' real sites.** ~~Open enrichment lever: homepage word-density <800~~ → CLOSED (AL-554): fresh builds clear the 800 aim (Heath 937 · Stumptown 1064; only stale pre-AL-409 builds fall short). C.1 hardened (AL-554): the retail content pack's hardcoded "Gear/equipment" persona (→ "Gear we actually stand behind" on a ceramics /about) de-"Gear"'d to vertical-neutral copy; guard `e2e/site-quality/verify-vertical-persona.mjs`. C.1 hardened again (AL-559): `TrustBar` defaulted "Licensed & insured" (a fabricated credential) on ANY quote-using business (agency/salon/consultant, not just trades) → neutralized to "Experienced & reliable" (trades pass the real claim via the `items` seam); same guard extended with a TRADE_OK-exempt licensed-&-insured check. C.1/C.7 hardened again (AL-565): the H1 + SEO title wove a BARE professional-services discipline noun ("Seattle's trusted architecture" on olson-kundig-seattle, "Your neighborhood accounting" on ben-badgley-cpa — a discipline is a field, not a business) → `hero_copy.categoryPhrase` now suffixes architecture/accounting/consulting/engineering/marketing/etc. to the business noun (architecture firm / accounting firm / marketing agency); new guard `e2e/site-quality/verify-headline-grammar.mjs` (prod-verified: flags the 4 live surfaces, 0 false-positives on a concrete-vertical control). § C highest-severity gate CLOSED (AL-571): a DARK site (olson-kundig-seattle rendered ONLY the "Something went wrong" error boundary — a stale build's DevA11yBadge-*.js 404 propagated past a bare `<Suspense>` and crashed the whole page) slipped past every metric probe (cwv/density measure a page assumed-rendered). Root-fixed the CLASS in the template — `ChunkBoundary` (`2d53b27`) renders `null` on any lazy-chunk load failure so a bad chunk degrades to "feature absent", never a whole-site crash — + new durable gate `e2e/site-quality/verify-site-renders.mjs` (prod-verified: flags olson-kundig 🔴 DARK, passes lake-flato ✅). Fleet dark-sweep 45/45 render CLEAN (AL-574 — olson-kundig was the sole dark site, already fixed). C.1/C.7 headline copy hardened again (AL-576): `heroHeadlineOptions`' weakest 4th candidate "Quality {cat} {city} counts on" (a generic "Quality" lead, `pick()`-selectable for neutral personalities, shipped on 6 live sites) → clean relative clause "The {cat} {city} counts on"; `verify-headline-grammar.mjs` extended with a weak-lead H1 check (prod-verified: flags the cohort, 0 false-positives). C.1/C.7 persona-copy hardened again (AL-612): the `botanical` themeStyle is shared by wellness AND (since AL-610) plant/garden/florist retail, so its wellness-CARE hero copy ("Feel better in {city}"/"unhurried care") shipped as a MISFIT on a garden shop (flora-grubb-san-francisco H1 "Feel better in San Francisco") — root-fixed in `hero_copy.ts` (`personaHeroCopy` plant branch → GROW copy) + `THEME_PERSONALITY_BRIEF` (AL-611), guarded by `verify-vertical-persona.mjs`'s new wellness-copy-on-plant check (AL-612), and PROVEN end-to-end on a Flora Grubb rebuild (H1 "Feel better…" → "Grow something beautiful…", probe RED→GREEN). New reusable `e2e/admin-verify/rebuild-site.mjs` (Browserbase brian → POST /reset) proves a landed root fix on a FRESH build. C.1/C.7 persona+hero hardened again (AL-641): a REFINED fine-art gallery shipped the BOLD `brutalist` persona (H1 "Atlanta. Art gallery. No compromise" + "Uncompromising… impossible to ignore" + a messy graffiti-studio hero) because "art gallery" matched `\bart\b` in the brutalist CREATIVE cluster (shared with design/photo studios) — root-fixed in `theme_style.ts` (galler*|fine art|art dealer → `luxe`, refined Playfair persona) + `hero_image.ts` (new gallery-interior hero, prod-200; `art STUDIO` stays creative), 154/154 unit tests, and `verify-vertical-persona.mjs` extended with a RED-first-proven `brutalist-persona-on-gallery` check (flags live jackson-fine-art-atlanta). Lands next build. C.1/C.7 persona-copy hardened again (AL-696): the `noir` themeStyle is shared by NIGHTLIFE venues AND tattoo/body-art STUDIOS (the dark aesthetic fits both), so its nightlife-specific hero copy ("room after dark" / "careful pours" / "candlelit") shipped as a MISFIT on a tattoo studio (live: three-kings-tattoo-brooklyn H1 "Brooklyn's room after dark" + "candlelit… evening you remember" subhead — vision-confirmed; the noir theme/storefront-hero/layout are already beat-the-source quality, only the copy leaked) → root-fixed in `hero_copy.ts` (`personaHeroCopy` noir `isBodyArt` branch → INK/custom-work copy; actual bars keep the after-dark voice), +1 unit test (RED-first), and `verify-vertical-persona.mjs` extended with a RED-first-proven `nightlife-copy-on-tattoo` check (flags live three-kings-tattoo, does NOT false-flag death-and-co the real cocktail bar). Full interactive journey GREEN on the live site (contact/page-audio/chat, 0 console errors). Lands next build.

## Gap-Implementation log (COMPLETENESS + GAP loop — capabilities a best-in-class product should have)

- **Prompt Scheduler** ✅ (AL-633, flag `prompt_schedule`, experimental/dark) — time-windowed activation of a prompt-registry variant for a key (the gap between `prompt_studio`'s variant-weights/rollback and time-based scheduling; the registry had no scheduling). `libs/features/prompt_schedule`: POST/GET/GET-active/DELETE `/api/prompt-schedules`, org-scoped + Zod + typed errors + structured logs, 404-dark. Pure `resolveActiveSchedule` (half-open windows, most-recently-activated wins, **no cron** — read-time eval); `getActiveVariant(key,now)` is the pipeline's consumption API (wired on promotion → 0 prod risk). Migration `0633` (table + flag seed, applied to prod D1) + `FLAG_REGISTRY` entry. 9 unit tests green, validate:features PASS, endpoint prod-verified 404-dark. Enables seasonal/campaign prompts (holiday hero Dec 1-26) with zero owner config.
- **Scheduled Site Publishing** ✅ (AL-642, flag `scheduled_publish`, experimental/dark) — schedule a BUILT site to go live (status→published) at a future datetime (campaign launch / grand opening) with zero babysitting. `libs/features/site_publish_schedule` (slug `site_publish_schedule`): POST/GET/DELETE `/api/sites/:id/publish-schedule`, org-+site-scoped + Zod `.strict()` + future-only + typed 409-on-unbuilt (`action-button-must-gate-on-server-precondition`) + structured logs, 404-dark. Pure `resolveDueSchedules(rows,now)` (unit-pinned) consumed by the every-minute cron `fireDuePublishSchedules` (scheduled() **Stage 6.2**, per-schedule fail-soft, unbuilt→skipped never blank). Migration `0634` (table + flag seed, applied to prod D1) + `FLAG_REGISTRY` + `FLAG_DOCS`. 10 unit tests, validate:features PASS, endpoint prod-verified 404-dark. First CRON-fired gap feature (prompt_schedule was read-time) — reuses the proven Functions-cron dispatch pattern.
- Already-built (verified this fire, NOT rebuilt): audit-log export (`audit_trail_export`), bulk ops (`batch_operations`), notification prefs (`verify-notification-prefs-causal`), onboarding (`onboarding_progress`/`_copilot`), site backup/versioning (`site_versioning`), social scheduling (`content_scheduler` + `social_publishing_native`).
- Next candidate gaps (genuinely missing, per grep): recurring/saved lead-scan scheduling · A/B testing for generated sites (variant serving + winner) · admin UI for the prompt/publish schedulers (both backends are dark — a `/admin/schedules` surface would promote them).

## Maintenance caveat
Scheduled `/loop` crons **auto-expire after 7 days** (scheduler behavior). To keep converging to done, **re-arm weekly** (`/loop` or `CronCreate durable:true`). A weekly re-arm reminder is the safest guard against the whole convergence silently stopping.
