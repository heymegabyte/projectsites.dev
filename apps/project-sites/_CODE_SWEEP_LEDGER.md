# Code-Quality Sweep Ledger

Tracks the hourly CODE-QUALITY SWEEP loop (cron `1534cb49`, every hour at :53). Each fire picks the next unswept batch, evaluates every file against all dimensions (correctness · Zod · TS-strict · a11y · perf · gorgeous polish · dead-code · dup · error-handling · tests · docs), fixes at root cause + a regression test, verifies, deploys, prod-verifies, and appends a closure here. When the whole tree is swept, loop back to the top and re-sweep (code changed).

## Coverage map (sweep order)

- [~] **T. template sections** — `template.../src/components/sections/*` — img a11y/perf class SWEPT tree-wide (Fire 1 alt, Fire 2 decoding); deeper per-dimension sweeps recur
- [~] **template components** — `template.../src/components/*` — high-signal classes SWEPT (Fire 3): no React-19 inline-`<style>` bug, no missing-alt imgs, DevA11yBadge DEV-gated, forms error-handled; TS-strict fix landed (AiChat `as unknown as FormEvent` → native requestSubmit). Deeper per-dimension sweeps recur
- [~] **template pages** — `template.../src/pages/*` — SWEPT (Fire 4): all 18 pages CLEAN on mechanical + a11y classes (no inline-`<style>`, no TS-strict, no console.log, no missing-alt, no clickable-div); found + fixed the partial-fill unfilled-slot class — CaseStudyGrid/TeamGrid dropped `{CS_N}`/`{TEAM_N}` unfilled cards (build_validators only guards `{BUSINESS_*}`). Deeper per-dimension sweeps recur ← _next batch: template lib_
- [~] **template lib** — `template.../src/lib/*` + `src/*.ts` — SWEPT (Fire 5): all 8 lib files evaluated on every dimension. `perfMonitor.ts` had a REAL bug — empty `visibilitychange` handler that its own comment + file JSDoc promised finalizes CLS/INP → definitive vitals NEVER flushed + per-entry emits flooded analytics; root-fixed to track→flush-once on hide+pagehide, pure `finalizeMetrics()` + 8-test suite + real-browser probe. `altText`/`apca`/`i18n` unused-but-intentional (built-ahead affordances, not deleted per knip-unused-not-always-dead); `cursor` live+clean. Deeper per-dimension sweeps recur ← _next batch: worker routes_
- [~] **worker routes** — `apps/project-sites/src/routes/*` (63 files) — SWEPT (Fire 6) on high-signal classes: `console.log` (0 real — both hits are a JSDoc `@example` + a comment), TS-strict `any`/`@ts-ignore` (0 real — `claim.ts`/`api.ts` "any" are prose in comments), unvalidated body casts (auth/token routes use the SAFE `as {field?: unknown}` + manual-narrow parse-don't-trust pattern, not unsafe casts; features.ts casts stay dormant/flag-gated per gotcha #10). ★ ROOT FIX: `analytics.ts` had 4 `env.EVENT_DISPATCHER!` non-null assertions inside `if (env.EVENT_DISPATCHER)` guards (TS drops the narrowing across the async closure) → hoisted a narrowed `const dispatcher` in both enqueue paths → assertion-free + refactor-robust. `forms.ts:181` raw throw is INTENTIONAL (documented anti-lying-success on insert failure). Deeper per-dimension sweeps recur ← _next batch: worker services_
- [~] **worker services** — `apps/project-sites/src/services/*` (390 files) — non-null-`!` TS-strict class SWEPT tree-wide (Fire 7): grepped all 390, fixed the 8 real appeasement-`!` (theme_polarity ×2, google_places, crm_leads, social_account_ctx ×2, external_llm, app_provisioner ×2, content_version, edge_ai_router) — closure/guard-fn/derived-boolean narrowing lost; edge_ai_router upgraded to fail-soft. GraphQL `String!` + array-index `channels[0]!` correctly NOT touched (validator-precision). Deeper per-dimension sweeps recur ← _next batch: worker middleware + prompts + workflows_
- [~] **worker middleware + prompts + workflows** — `src/middleware/*` (15) · `src/prompts/*` (9) · `src/workflows/*` (6) — non-null-`!` class SWEPT (Fire 8): 5 real appeasement-`!` fixed (job-provider ×2 → guard-continue · SITE_BUILDER closure → captured const · finalStatus ×2 → optional-chain fail-soft). `console.log` 0 real; `as unknown as` are all the SAFE env-optional-binding / auth-interface pattern (left). Deeper per-dimension sweeps recur ← _next batch: worker feature modules_
- [~] **worker feature modules** — `apps/project-sites/libs/features/*` (77 dirs, 182 files) — high-signal classes SWEPT tree-wide (Fire 9): `console.log` **0**, `@ts-ignore` **0** (2 legit `no-constant-condition`), `any` all JSDoc-prose FPs except one. ★ ROOT FIX: `places_search` 4× `s.placePrediction!` (filter→map narrowing loss) → type-predicate filter (also drops dead placeId-less rows) + `media_ai` `(brandAssessment as any).brandfetch` → typed optional field. 2 regression tests. DEFERRED by doctrine: 11× `c.get('orgId')!` (auth-guaranteed idiom → wants a shared `requireOrgId(c)` typed-helper slice) + unvalidated `req.json() as` body-casts (gotcha #10: convert per-flag-promotion, mass-retrofit forbidden). Deeper per-dimension sweeps recur ← _next batch: packages/shared, then the `orgId!`→`requireOrgId` helper slice_
- [~] **packages/shared** — `packages/shared/src/*` — mechanical classes CONFIRMED 0 (Fire 11/12); DEEPER-dimension sweep SWEPT (Fire 16): ★ root-fixed the `redactObject` **array-skip PII leak** (arrays passed through UNREDACTED → PII/secrets in `{recipients:[…]}`/`{users:[{email,token}]}` leaked into logs/Sentry/PostHog) via a recursive `redactValue` helper + fixed a latent DEAD set-entry (`apiKey` mixed-case could never match the `.toLowerCase()` lookup → `apikey`) + widened plurals (`secrets`/`tokens`/`cookies`/`api_keys`) + removed a dead `slugSchema` import. Sanitize/redact/entitlements/rbac otherwise clean. Schema boundary-tightenings surfaced (billing currency regex, AI-output `z.array(z.unknown())`) — TRACKED, low-risk, deferred. Deeper per-dimension sweeps recur ← _next batch: Angular admin frontend (drain the 57 sub-leaks) or shared schema boundary-tightening_
- [~] **Angular admin frontend** — `apps/project-sites/frontend/src/app/**` (284 files) — mechanical classes CONFIRMED 0 tree-wide (Fire 17: 0 `any`/`@ts-ignore`/`console.log`). ★ ROOT FIX: `calendar-widget` had a live WCAG 2.1.1 cluster — week/day "add event" cells were bare `<div (click)>` (mouse-only, keyboard-stranded) + a month-view event pill was an interactive `<span (click)>` NESTED in the day `<button>` (invalid + stranded) → newAt cells now role=button+tabindex+Enter/Space+aria-label, month pills display-only; +3 Karma (11/11), FE deployed, /admin prod-verified clean. 3 audit agents fanned out: services/guards/interceptors CLEAN; the subscription-leak flags were FALSE POSITIVES (task-tray `stopTimer()` + notification-bell `unsubscribe()` in ngOnDestroy — agents missed the teardown). Deeper per-dimension sweeps recur. **Fire 18: the 3 tracked settings findings CLOSED** — #2 promoted to a SHIPPED broken-auth root-fix (dead `session_token` key → `auth.getToken()` on knowledge-upload + brand-asset upload + env-var export; all were 401-broken) + upload timeout/abort hardening + 4 Karma (1802/1802); #1 + #3 verified FALSE POSITIVES (api.service toasts; API-key row added in the success handler). ← _next batch: editor (bolt.diy `app/`) — the sole `[ ]` unswept tree — then re-sweep_
- [ ] editor (bolt.diy) — root `app/**`
  - **TRACKED (Fire 17 agent findings, plausible — verify before fixing):** `settings.component.ts:943` knowledge-file upload via raw XHR has no AbortController (navigate-away → orphaned onload on a destroyed component); `settings.component.ts:905` allow-web-research toggle error path resets UI without a user message; `user-settings.component.ts:1300` optimistic API-key row added before the POST resolves. Each needs its own verify + Karma before fixing.

## Dimensions (evaluate EVERY file)

correctness/bugs · Zod at every boundary · TS strictness (no `any`/`@ts-ignore`/non-null `!`) · a11y WCAG 2.2 AA · perf (CWV/bundle/INP) · gorgeous UI polish (contrast/spacing/motion/skeletons/first-result CTA) · dead code + unused exports (knip) · duplicate logic · error handling (typed, fail-soft prod) · missing unit/E2E tests · JSDoc/docs drift.

---

## Fires

### Fire 1 — 2026-09-15 — template sections: image a11y + decode-perf polish (batch 1 of "template sections")
- **Swept:** the 4 first-class industry sections (`Menu`, `ServiceMenu`, `DonationTiers`, `FeaturedCollection`) + their image usage.
- **Improvement:** product/dish `<img>` in `Menu` + `FeaturedCollection` sit INSIDE (or beside) their own visible name text → `alt={name}` made the name announce TWICE to a screen reader (WCAG 1.1.1 redundancy) and blocked the main thread on synchronous decode. Fixed → decorative `alt=""` (the adjacent name carries the meaning) + `decoding="async"` (non-blocking decode, a small CWV/INP win). Regression test asserts the FeaturedCollection product image is decorative when a name is present.
- **Result:** ✅ `Menu` + `FeaturedCollection` product images → `alt=""` + `decoding="async"`; regression test added (product image decorative when name present). Template tsc clean · IndustrySections 9/9 · build green (validate-site 19 routes / 217 tokens). Pushed template. Next batch: remaining template sections (Stats, BentoGrid, Pricing, FeatureSplit, Timeline, …).

### Fire 2 — 2026-09-15 — template sections: `decoding="async"` tree-wide + regression gate (batch 2 of "template sections")
- **Verify-before-implement:** worker `5c972b286`, template `eda2215`; only protected + build-artifacts dirty. prod 200.
- **Swept:** all ~24 section components for the img a11y/perf class (building on Fire 1). Systematic scan found TS-strict CLEAN (0 any/@ts-ignore/non-null!), but **12 sections rendered `<img>` WITHOUT `decoding="async"`** — only Fire 1's Menu/FeaturedCollection + LogoCloud had it → images decoded synchronously, blocking the main thread (an INP/CWV cost on EVERY generated site).
- **★ ROOT FIX:** added `decoding="async"` to every below-the-fold section img (10 files: BentoGrid, BlogList×2, CaseStudyCard, Demo, FeatureSplit, Quote, Spotlight, TeamGrid, Timeline, VideoEmbed) via a context-aware script (correctly skipped Demo's `<iframe>` + inlined Timeline's single-line tag). **HeroVariants EXEMPT** (LCP hero — async decode can delay the largest paint, per ttfr-north-star).
- **★ REGRESSION GATE:** `section-img-decoding.test.ts` — asserts every real section `<img>` (except HeroVariants, comment-mentions stripped) sets `decoding="async"`, so the class can't drift back. tsc clean · 22 section tests green · template build green (validate-site 19 routes / 217 tokens). Pushed template `36a6d81` (lands next build, NO redeploy).
- **Result:** ✅ every generated site's below-fold images now async-decode (non-blocking) + a durable gate. Next batch: **template components** (Header, Footer, ContactForm, AiChat, Breadcrumbs, CommandPalette, …).

### Fire 3 — 2026-09-15 — template components: high-signal defect-class sweep + AiChat TS-strict root fix (batch "template components")
- **Verify-before-implement:** worker HEAD `a8533ad82`; template repo dirty only in generated feeds (`public/atom.xml`/`feed.*`/`sitemap.xml` — build artifacts, another session's build) — avoided; touched only `src/components/*` source. worker tree clean, prod 200.
- **Swept** all 56 top-level template components across the high-signal classes: React-19 inline-`<style>{` client-drop bug (0 — the sections-era class stays fixed), `<img>` without `alt` (0), TS-strict (`: any`/`@ts-ignore`/`as any`/`as unknown as`), `console.log`, form fetch error-handling.
- **★ ROOT FIX — AiChat.tsx `as unknown as FormEvent`:** the chat textarea's Enter-to-send fabricated a `FormEvent` from a `KeyboardEvent` via `onSubmit(e as unknown as FormEvent)` (a TS-strict double-cast smell). Replaced with `e.currentTarget.form?.requestSubmit()` — fires the form's REAL SubmitEvent (identical behavior, since `onSubmit` only did `preventDefault()`) AND runs native constraint validation the direct call skipped. **★ REGRESSION GATE** `AiChat.submit.test.ts` (2 tests, source-idiom: asserts `requestSubmit()` present + `as unknown as FormEvent` absent). tsc clean · vitest 2/2 · template build green (19 routes / 217 tokens). Pushed template `94c984d` (lands next build, NO redeploy).
- **False positives correctly NOT touched (validator-precision):** Header.tsx:216 `any` is prose in a comment ("Best-effort: any failure"), not a type; DevA11yBadge `console.log` is inside a fully DEV-gated component (early-returns null in prod + DEV-only effect) — legitimate dev-tooling, zero prod impact; ContactForm/QuoteForm/Newsletter already carry robust error-handling (18/20/7 refs).
- **Result:** ✅ template components batch swept on the high-signal classes; 1 real TS-strict root fix + durable gate; the rest clean. Next batch: **template pages** (`src/pages/*`).

### Fire 4 — 2026-09-15 — template pages: high-signal sweep CLEAN + partial-fill unfilled-slot card-drop (batch "template pages")
- **Verify-before-implement:** worker HEAD `003fbb133`; template dirty only in generated feeds (build artifacts) — avoided; touched only `src/pages/*` + the two grid sections. prod 200.
- **Swept all 18 template pages** on the high-signal classes: React-19 inline-`<style>` (0), TS-strict `any`/`@ts-ignore`/`as any`/`as unknown as` (0), `console.log` (0), non-null `!` (0), `<img>` without `alt` (0 — the only hit was comment prose in Services), clickable `<div>`/`<span>` without role (0). Pages are CLEAN on mechanical + a11y.
- **★ ROOT FIX (deeper dimension — fail-soft/error-handling):** 6 pages define hardcoded N-slot tokenized arrays (`{CS_N}`, `{TEAM_N}`, `{TIER_N}`, `{SERVICE_N}`, `{GALLERY_N}`, Home's 10). The grids (`CaseStudyGrid`, `TeamGrid`) mapped ALL slots directly → a business with FEWER items than the template's slots left `{CS_3_TITLE}`/`{TEAM_2_NAME}` unfilled, and **build_validators only guards `{BUSINESS_*}` (line 822-825), NOT section slots** → a partial fill rendered a broken/empty card + (TeamGrid) emitted a `{TEAM_3_NAME}` Person JSON-LD leak. Fixed both grids to drop unfilled-slot cards via the existing `isPlaceholder` firewall (`placeholders.ts`), keeping ALL only when NONE are filled (raw template skeleton / unit fixtures still render in dev). **★ REGRESSION GATE** `unfilled-slots.test.tsx` (4 tests: partial→drop the token card, none-filled→keep all, JSON-LD doesn't leak the dropped member). tsc clean · 4/4 · template build green (19 routes / 217 tokens). Pushed template `4105e31` (lands next build).
- **Result:** ✅ template pages swept clean on mechanical/a11y + a real partial-fill fail-soft fix (broken-card class the build gate missed). Next batch: **template lib** (`src/lib/*` + `brand.ts`).

### Fire 5 — 2026-09-15 — template lib: perfMonitor CWV-flush root fix + real-browser proof (batch "template lib")
- **Verify-before-implement:** worker HEAD `6eb899fe2`; template dirty only in generated feeds (`atom.xml`/`feed.*`/`sitemap.xml` — another session's build artifacts) — avoided; touched only `src/lib/*`. apex prod 200.
- **Swept all 8 template lib files** (`perfMonitor`, `altText`, `apca`, `i18n`, `cursor`, `businessSchema`, `placeholders`, `utils`) on every dimension. TS-strict CLEAN (0 `any`/`@ts-ignore`/non-null-`!` outside a test). `console.log` in `perfMonitor` is DEV-gated (legit dev-tooling, not touched — same class as Fire 3's DevA11yBadge FP).
- **★ ROOT FIX — `perfMonitor.ts` empty `visibilitychange` handler:** the handler body was EMPTY, its inline comment + the file docstring + line-13 JSDoc all promised "Re-finalize CLS + INP on visibility change to hidden." So on EVERY generated site the definitive per-page CLS/INP/LCP were **never flushed**, while the observers emitted per-entry — escalating intermediate CLS/INP readings that flood GA4/PostHog and never mark the final value (wrong analytics model; CrUX + GA4 expect ONE true value per metric per page). Fixed: track LCP/CLS/INP continuously → flush ONCE on first `visibilitychange→hidden` + `pagehide` (bfcache-safe, `flushed`-guarded). Extracted pure **`finalizeMetrics()`** (+ exported `rate`/`MetricEvent`/`PerfSnapshot`) so the contract is unit-testable. **★ REGRESSION GATE** `perfMonitor.test.ts` (8 tests: rating thresholds for LCP/INP/CLS + the flush contract — CLS always emits, LCP/INP only when observed, all `meta.final`).
- **★ REAL-BROWSER PROOF (not a compile):** new committed probe `e2e/verify-perf-flush.mjs` serves the freshly-built `dist/`, stubs `gtag`+`posthog`, dispatches `pagehide`, asserts a `final:true` flush + 0 console errors. Ran GREEN: **FINAL LCP=436ms (good) + FINAL CLS=0.068 (good)** flushed on hide to BOTH analytics sinks, 0 console errors. Zero API cost — no rebuild (per API-credit discipline; the fix lands organically on the next generated build).
- **Dead-code judgment (validator-precision):** `altText` (dedupeAltText/findDuplicateAlts), `apca` (WCAG3 contrast), `i18n` ("idea #156") have 0 importers but are intentional built-ahead template affordances — LEFT per `knip-unused-not-always-dead`, not deleted. `cursor` live (main.tsx:34) + clean.
- **Verify:** tsc 0 · vitest 8/8 · template build green (validate-site 19 routes / 217 tokens) · real-browser probe GREEN. Pushed template `56d89a6` (fix) + `cfb5593` (probe) — land next build, NO redeploy.
- **Result:** ✅ template lib swept; 1 real CWV-correctness root fix (analytics under-reporting class the build gate can't see) + pure-fn regression suite + a reusable real-browser flush probe. Next batch: **worker routes** (`apps/project-sites/src/routes/*`).

### Fire 6 — 2026-09-15 — worker routes: high-signal sweep CLEAN + analytics non-null-assertion root fix (batch "worker routes")
- **Verify-before-implement:** worker HEAD `f41d23703`; prod 200; only protected + untracked-artifact files dirty (avoided).
- **Swept all 63 `src/routes/*.ts`** on the high-signal classes: `console.log` (**0 real** — `docs.ts:701` is a JSDoc `@example`, `api.ts:3491` is a comment), TS-strict `any`/`@ts-ignore`/`as any` (**0 real** — `claim.ts:82` + `api.ts` "any" are PROSE in comments, the Fire-3 Header.tsx false-positive class), unvalidated body casts (the auth/token routes — `auth_sessions`/`auth_org`/`api_tokens_admin` — use the SAFE `as { field?: unknown }` + manual-narrow "parse-don't-trust" pattern, NOT unsafe `as {field:string}` casts; the `features.ts` casts stay dormant/flag-gated per project gotcha #10, do-not-mass-retrofit).
- **★ ROOT FIX — `analytics.ts` non-null assertions:** the pageview-ingest hot path had 4 `env.EVENT_DISPATCHER!.get(env.EVENT_DISPATCHER!.idFromName(...))` non-null assertions in TWO enqueue paths — each INSIDE an `if (env.EVENT_DISPATCHER)` guard, but TS drops the narrowing across the `async () => {}` closure boundary, so the `!` was a pure appeasement smell (the sweep's "no non-null-`!`" dimension). Hoisted a narrowed `const dispatcher = env.EVENT_DISPATCHER;` in each guard → the closure captures the non-optional type → **assertion-free + more refactor-robust** (a narrowed const can't silently become a runtime crash the way a re-accessed `!` could if the guard were ever moved). tsc 0 · **246/246** analytics jest (26 suites) · 0 remaining `EVENT_DISPATCHER!`.
- **False positives correctly NOT touched (validator-precision):** the console.log comments; the prose "any"; the `unknown`-typed defensive casts; `forms.ts:181 throw new Error` is INTENTIONAL (documented anti-lying-success — surfaces a 500 on a form_submissions insert failure instead of a lying 200).
- **Result:** ✅ worker-routes batch swept; 1 real TS-strictness root fix on the analytics ingest hot path (assertion-free), the rest clean on the high-signal classes. Pure refactor (identical behavior, tsc + 246 tests green) → the ingest path stays continuously verified by the golden-journey propagation probe (AL-646 PASS). Worker → CI. Next batch: **worker services** (`src/services/*`).

### Fire 7 — 2026-09-16 — worker services: non-null-`!` TS-strict class swept tree-wide + edge_ai_router fail-soft (batch "worker services")
- **Verify-before-implement:** worker HEAD `95b877050`; prod 200; AL-647 deploy landed. Dirty tree clear of target (only `.claude/scheduled_tasks.json` + 2 untracked artifacts — other sessions, avoided). `console.log` across all 390 services = **0 real** (every hit is a JSDoc `@example`/comment) → structured-logging clean. `as unknown as {X?}` hits are the SAFE optional-binding-access pattern (env vars not in the typed `Env`) — left. GraphQL `String!`/`IssueCreateInput!` + array-index `channels[0]!` are false positives — NOT touched (validator-precision).
- **★ ROOT FIX — 8 real appeasement non-null `!` (the "no non-null `!`" dimension, same class as Fire 6's analytics fix):** each was a value provably present where TS loses the narrowing across a closure / separate guard-fn / derived boolean →
  - `theme_polarity.ts` ×2 — `relativeLuminance(logoHex!)` + `contrastRatio(logoHex!, bg)` → pass the already-VALIDATED `normalized` hex (the guard validated `normalized`, not the raw `logoHex`; behavior-identical since both fns re-normalize, but now the `!` is gone AND the validated value flows through). Theme polarity is BUILD-BREAKING (logo luminance drives light/dark), so feeding the validated value is the correct-by-intent fix.
  - `google_places.ts` — `d.opening_hours!.periods!` inside `if (d.opening_hours?.periods)` → hoisted `const periods` before the `.map` closure.
  - `crm_leads.ts` — `env.TWENTY_API_URL!` after `if (!isCrmConfigured(env))` → made **`isCrmConfigured` a type predicate** (`env is Env & {TWENTY_API_URL:string; TWENTY_API_KEY:string}`) so the narrowing flows to the call site (kills the `!` at the source + benefits the `TWENTY_API_KEY` read too).
  - `social_account_ctx.ts` ×2 — `row.access_token_encrypted!` in `buildCtx` after both callers guard `if (!row.access_token_encrypted)` → pass the caller-narrowed token as an explicit `string` param.
  - `external_llm.ts` — `options.documents!.map` inside the `hasDocuments` derived-boolean branch → `const documents = options.documents ?? []`.
  - `app_provisioner.ts` ×2 — `result.postgres!`/`result.redis!` inside rollback closures → capture `const createdPg`/`const createdRedis` (the definitely-non-null just-created resource) and close over that.
  - `content_version.ts` — `changed.push(newLine!)` → made the branch condition explicit (`oldLine !== undefined && newLine !== undefined && oldLine !== newLine`) so TS narrows `newLine` to `string` (behavior-identical; the two prior branches already handled the add/remove cases).
  - **★ `edge_ai_router.ts` — upgraded to FAIL-SOFT (hardened error-handling dimension):** `stream!.getReader()` in the instant-AI SSE closure → extracted a single `aiUnavailable()` SSE fallback, added `if (!stream) return aiUnavailable()` after the try/catch, and closed the reader over a narrowed `const readable`. A null/undefined Workers-AI stream now degrades to a clean `[DONE]` frame instead of crashing inside the `ReadableStream.start()` closure (this path had a prior live streaming incident per its own comment — so fail-soft matters here).
- **★ REGRESSION TEST:** `theme_polarity_unit.test.ts` +1 — a 3-char `#`-prefixed logo (`#f0f`, `#111`, `#b0b`) drives `resolveThemePolarity` (theme + reason + the mid-luminance contrast-flip path) IDENTICALLY to its 6-char form, locking the "normalized, not raw `logoHex!`" intent. **theme_polarity 14/14.**
- **Verify:** tsc **0 errors** · **0 swept `!` remain** (grep) · all 8 touched suites GREEN (**120 tests**: theme_polarity 14, + crm_leads/social_account_ctx/external_llm/app_provisioner/content_version/edge_ai_router/google_places 106) · eslint **0 errors** (193 warnings all pre-existing `perfectionist/sort-objects` key-order, none introduced). Pure behavior-preserving refactors + one fail-soft hardening → verified by full existing suites, no prod-observable surface change. Worker → CI on push.
- **Fan-out note:** these were judgment-dense micro-edits (each `!` needs control-flow proof it's safe to remove without behavior change) — per `parallel-subagent-economy` ("don't re-fan-out for judgment-heavy repair") + `validator-precision` (false-fix risk), main-thread precision beat a 4-6 agent fan-out here; the tree-wide grep already swept all 390 for the class. Threaded the whole batch for ONE cross-cutting defect class in one coherent slice.
- **Result:** ✅ worker-services batch swept for the non-null-`!` class tree-wide; 8 real appeasement-`!` eliminated (refactor-robust narrowing) + `edge_ai_router` hardened to fail-soft + a regression test locking the theme-polarity fix. **Files mine:** the 8 service files + `theme_polarity_unit.test.ts` + `_CODE_SWEEP_LEDGER.md`. Protected files untouched. Next batch: **worker middleware + prompts + workflows**.

### Fire 8 — 2026-09-16 — worker middleware + prompts + workflows: non-null-`!` class swept (batch "middleware/prompts/workflows")
- **Verify-before-implement:** worker HEAD `97d246070`; prod 200; only protected + untracked-artifact files dirty (avoided). Batch = 15 middleware + 9 prompts + 6 workflows. `console.log` = **0 real**. `as unknown as` = all the SAFE established patterns (env-optional-binding access `(env as unknown as {X?:string}).X` for dynamically-provisioned secrets not in the typed Env; `makeAuth(c.env) as unknown as SessionAuthLike` auth-SDK interface cast) — left, same call as Fire 7. GraphQL `String!` etc. NOT touched.
- **★ ROOT FIX — 5 real appeasement non-null `!` (same class as Fire 6/7, all in `workflows/`):**
  - `job-provider.ts` ×2 — `binding!.get(jobId)` in the `for (const binding of Object.values(this.bindings))` loops (values are `Workflow | undefined`) → added `if (!binding) continue;` (skip an UNCONFIGURED optional binding explicitly, instead of relying on the try/catch to swallow a `undefined.get` TypeError). Fixes both `getJobStatus` + `cancelJob`.
  - `site-generation.ts:512` — `env.SITE_BUILDER!.get(containerId)` in the `getContainer` arrow closure: the `if (!env.SITE_BUILDER) throw` guard (line 501) narrows straight-line code but the closure loses it (the Fire-6 EVENT_DISPATCHER class) → hoisted `const siteBuilder = env.SITE_BUILDER;` after the guard, used in both `idFromName` + the closure → assertion-free + refactor-robust.
  - `site-generation.ts:1831 + 2132` — `finalStatus!.fileCount || 0` / `finalStatus!.elapsed || 0` inside `step.do` closures → `finalStatus?.fileCount || 0` / `finalStatus?.elapsed || 0`: since both already default with `|| 0`, optional-chaining is behavior-identical on the happy path AND strictly **fail-soft** on the null path (null → 0, never a TypeError crash mid-finalize) — an error-handling hardening, not just an `!` removal.
- **★ REGRESSION TEST:** `cloudflare_workflow_provider.test.ts` +1 — an UNCONFIGURED (undefined) binding in the map → `getJobStatus`→null + `cancelJob` no-throw (locks the guard-not-catch fix).
- **Verify:** tsc **0 errors** · **0 swept `!` remain** (grep) · job/workflow provider suites **26/26** + workflow suites **177/177** (14 suites incl. `site_generation_workflow`) GREEN. Pure behavior-preserving narrowing + 2 fail-soft hardenings; no prod-observable change. Worker → CI on push.
- **Result:** ✅ middleware/prompts/workflows batch swept for the non-null-`!` class; 5 real appeasement-`!` eliminated (2 upgraded to fail-soft) + a regression test. **Files mine:** `job-provider.ts` + `site-generation.ts` + `cloudflare_workflow_provider.test.ts` + `_CODE_SWEEP_LEDGER.md`. Protected files untouched. Next batch: **worker feature modules** (`libs/features/*`).

### Fire 9 — 2026-09-16 — worker feature modules: high-signal classes swept tree-wide + type-predicate/typed-field root fixes (batch "libs/features")
- **Verify-before-implement:** worker HEAD `0fb298482` (another session pushed past Fire 8's `97d246070` — rebased before push); apex prod 200. Batch = **77 dirs / 182 `.ts` files** (excluding tests). Dirty tree clear of my targets (only `.claude/scheduled_tasks.json` + untracked `_ADMIN_CONTRACT_REPORT.json`/`_delivery-*.png`/`_navshot.png` from other sessions — avoided).
- **Tree-wide grep of the high-signal classes:** `console.log` **0** (structured-logging clean); `@ts-ignore`/`@ts-nocheck`/`eslint-disable` — **0 real** (only 2 legit `// eslint-disable-next-line no-constant-condition` in `admin_ai/handlers.ts` retry loops); `: any`/`as any`/`<any>` — **5 hits, 4 are JSDoc PROSE** ("Defensive: any D1 error…" in `site_analytics`/`visitor_events_core` — the Fire-3 comment-FP class), only ONE real.
- **★ ROOT FIX 1 — `places_search/handlers.ts` 4× non-null `!` (the "no non-null `!`" TS-strict dimension, same class as Fire 6/7/8):** the `/api/search/address` autocomplete mapped `suggestions.filter(s => s.placePrediction).map(s => ({ place_id: s.placePrediction!.placeId, … }))` — 4 `!` because `.filter()`'s boolean callback doesn't narrow the element type across into `.map()`. Fixed with a **type-predicate filter** (`(s): s is AutocompleteSuggestion & { placePrediction: NonNullable<…> } => Boolean(s.placePrediction?.placeId)`) → the map body reads `s.placePrediction.placeId` assertion-free. The predicate ALSO requires a truthy `placeId`, so a blank-id prediction or a non-place `queryPrediction` (a **dead row the client can't resolve**) is dropped — a small value-add aligned with `embarrassingly-easy-to-use` (never serve a doomed control).
- **★ ROOT FIX 2 — `media_ai/handlers.ts` `(brandAssessment as any).brandfetch = brandfetchData` (the sole real `as any`):** the brand-discovery handler tacked a `brandfetch` field onto the typed `brandAssessment` object via `as any`. Fixed by adding a typed optional field to the declaration (`brandfetch?: NonNullable<typeof brandfetchData>`) → the assignment is fully typed, no cast, and the response shape is now honest in the type.
- **★ REGRESSION TESTS:** `src/__tests__/search_routes.test.ts` +2 (38→40) — (a) a mixed autocomplete (valid + blank-id + queryPrediction) keeps only the actionable row (`place_id:'p1'`), no fallback; (b) an all-dead-prediction autocomplete is treated as a MISS and falls through to Text-Search (never serves phantom rows). Both lock the type-predicate behavior the `!`-removal introduced.
- **DEFERRED by doctrine (recorded, NOT half-done — one coherent slice per fire):**
  - **11× `c.get('orgId')!`** across mru_cards/onboarding_progress/site_clone/analytics_annotations ×2/notification_badge/cmdk_ai_actions/usage_gauges/activity_feed/batch_operations — a consistent auth-middleware-guaranteed idiom. The correct root-fix is a shared **`requireOrgId(c): string`** typed helper (throws 401 if absent) applied to all 11 call sites + tests — its own coherent slice (a per-site `!`-removal would be a one-off, which the sweep forbids). Flagged as an explicit next-batch target.
  - **Unvalidated `req.json() as {…}` body-casts** (api_keys, media_ai ×3, domains ×4, inbox, site_versioning ×2, site_urls, …). Per project CLAUDE.md gotcha #10: these are flag-gated experimental handlers; **mass-retrofit is explicitly forbidden** — convert per-feature to a colocated Zod schema + `zValidator` as each flag is promoted to beta/stable, with a unit test per endpoint. The good handlers (site_mcp_server/site_files/site_clone/domains-purchase/site_comparison/abuse_takedown) already use Zod `.parse()`/`.safeParse()`.
- **Verify:** tsc **0 errors** (full `--noEmit`) · eslint **0 errors** on touched files · `search_routes` jest **40/40** GREEN (incl. the 2 new regressions). Pure type-safety refactor + one tiny UX-correctness value-add; the only behavior delta (dropping dead autocomplete rows) is locked by the new regressions. Worker → CI on push, then prod-verify `/api/search/address`.
- **Result:** ✅ libs/features batch swept tree-wide for the high-signal classes; **2 real root fixes** (type-predicate kills 4 `!`; typed field kills the `as any`) + a UX drop-dead-row value-add + 2 regression tests; 2 larger classes correctly deferred with a documented plan. **Files mine:** `places_search/handlers.ts` + `media_ai/handlers.ts` + `search_routes.test.ts` + `_CODE_SWEEP_LEDGER.md`. Protected files untouched. Next batch: **packages/shared**, then the `orgId!`→`requireOrgId` typed-helper slice.

### Fire 10 — 2026-09-16 — the `orgId!`→`requireOrgId` typed-helper slice (batch "libs/features org-scoped handlers")
- **Verify-before-implement:** worker HEAD current; apex prod 200; my targets clean (only `.claude/scheduled_tasks.json` + untracked artifacts from other sessions — avoided). This is the exact slice Fire 9 DEFERRED + flagged ("a shared `requireOrgId(c)` typed helper applied to all call sites + tests — its own coherent slice"). Full grep: **38** `c.get('orgId')!` sites — **24 across 12 libs/features handlers** (this fire) + **14 in `src/index.ts`** (a HOT file other sessions touch → its own next slice, not mixed in).
- **★ ROOT FIX — new `src/middleware/require_org.ts` `requireOrgId(c): string`:** `Variables.orgId` is `string | undefined` (auth middleware sets it only after a valid session — it does NOT reject unauthed), so every org-scoped handler read it as `c.get('orgId')!` — a non-null assertion that (a) violates the "no non-null `!`" TS-strict dimension AND (b) silently fed `undefined` into `isFlagOn`/SQL if a request ever arrived unauthenticated (a latent wrong-scope/lying-empty bug). The helper returns the narrowed `string`, or throws the platform `UnauthorizedError` (clean 401 `{code:'UNAUTHORIZED'}` via the error envelope) — **fail-closed**, the correct response for an org-scoped route reached without auth (hardened-error-handling dimension). Converted all **24 sites across 12 handlers** (analytics_annotations ×5 · usage_gauges/site_clone/onboarding_progress/notification_badge/mru_cards/cmdk_ai_actions/batch_operations/activity_feed ×2 · system_status/site_health_sparklines/site_comparison ×1) — the `!` is GONE tree-wide in libs/features.
- **Behavior delta (intentional + more-correct):** an UNAUTHED request to a flag-dark route now gets a clean **401** (was `undefined`→isFlagOn→404-ish). Authed requests (orgId present) are UNCHANGED. Doesn't violate the flag-dark-404 rule (that's for an authed-but-flag-off user; an unauthed user getting 401 leaks nothing).
- **★ REGRESSION TEST:** `src/__tests__/require_org.test.ts` (3) — present→string · absent→401 `UnauthorizedError` (status+code) · empty-string→401 (falsy fail-closed). Typed fake context via `as unknown as` (the safe test-mock cast — `any`-free, the very class the sweep removes).
- **Verify:** tsc **0 errors** (all 24 call sites typecheck against the `Context<{Bindings:Env;Variables}>` param) · **0 `c.get('orgId')!` remain in libs/features** (grep) · **10 suites / 62 tests GREEN** (new require_org 3 + the 9 affected feature service-suites, unaffected — they test the service layer, not the unauthed path) · eslint **0 errors** (auto-fixed the 2 import-order warnings on the new helper). Worker → CI on push; the routes are flag-dark so the authed-behavior-preserving conversion is verified by the suites, not a live journey.
- **Fan-out note:** mechanical but judgment-checked (each site needs "is this an authed route where 401-on-absent is correct?") — a scripted tree-wide transform (Node, idempotent import-insert + replace) on 12 disjoint files beat both hand-editing 24 sites AND a worktree fan-out (the ceremony of committing the helper + branching + merging trivial 1-line edits saves no wall-clock per `parallel-subagent-economy`). One coherent slice, one class, one helper.
- **Result:** ✅ the `orgId!` class KILLED across all 12 libs/features org-scoped handlers via a shared typed fail-closed helper + a regression test. **Files mine:** `src/middleware/require_org.ts` (new) + `src/__tests__/require_org.test.ts` (new) + 12 handler files + `_CODE_SWEEP_LEDGER.md`. Protected files untouched. Next batch: **`src/index.ts` (14 inline-handler `orgId!` sites)**, then **packages/shared**.

### Fire 11 — 2026-09-16 — `src/index.ts`: finish the `orgId!` class + kill the residual real `as any` + de-dup the source SSOT (batch "index.ts")
- **Verify-before-implement:** worker HEAD current; apex prod 200; `src/index.ts` CLEAN (not dirty in another session) — the exact Fire-10-flagged next batch ("`src/index.ts` (14 inline-handler `orgId!` sites)", deferred as a HOT file to its own slice). `packages/shared/src` grepped for all high-signal classes → **0** (already clean). index.ts other-class hits: 4 are the established SAFE `env as unknown as {…}` optional-binding pattern (left) + 1 CF `(c.req.raw as any).cf` (left, established) + **1 real `as any`** (line 615, fixed below).
- **★ ROOT FIX 1 — the 14 `c.get('orgId')!` in index.ts → `requireOrgId(c)`:** all 14 were the identical `isFlagOn(c.env, '<flag>', { orgId: c.get('orgId')! })` idiom on flag-dark experimental routes (system_status/activity_feed ×7/cmdk_ai_actions/site_doctor/batch_operations ×3/onboarding_copilot). Applied the Fire-10 `requireOrgId(c)` typed fail-closed helper (imported from `./middleware/require_org.js`) → the `orgId!` class is now **GONE tree-wide** in the worker (libs/features Fire 10 + index.ts Fire 11). Same intentional behavior delta as Fire 10: an UNAUTHED request now gets a clean 401 (was `undefined`→isFlagOn→404-ish); authed requests unchanged; leaks nothing (401 before the flag check).
- **★ ROOT FIX 2 — line 615 `filterBySource(d, filter.split(',') as any)` → validated boundary (Zod-boundary + no-`any` dimensions):** the `?sources=` query CSV was cast `as any` into `MetricSource[]`. Added `parseMetricSources(csv)` to `marketing_dashboard/service.ts` — splits/trims/de-dupes and DROPS any token not in the source vocabulary (a bad `?sources=foo` narrows to nothing instead of smuggling an unknown string through `as any`). index.ts now calls `filterBySource(d, parseMetricSources(filter))` — assertion-free, boundary-validated. Behavior-identical on valid input (invalid sources filtered nothing before too), now type-safe.
- **★ ROOT FIX 3 — de-duplicated the source SSOT (duplicate-logic dimension):** `MetricSource` union AND `metricSources()`'s hardcoded array were TWO hand-maintained copies of the same 6 values (drift risk). Introduced `export const METRIC_SOURCES = [...] as const` as the SSOT; `MetricSource` now DERIVES (`(typeof METRIC_SOURCES)[number]`), `metricSources()` returns `[...METRIC_SOURCES]`, and `parseMetricSources` validates against it. One source of truth, three consumers.
- **★ REGRESSION TESTS:** `marketing_dashboard/__tests__/service.test.ts` +5 — parseMetricSources (drops unknown / empty→[] / trims+dedupes / all-valid round-trips) + metricSources()-derives-from-SSOT. The `orgId!` conversion is covered by Fire-10's `require_org.test.ts` (the helper) + tsc (all 14 sites compile against the `Context` param).
- **Verify:** tsc **0 errors** (full `--noEmit`) · **0 `c.get('orgId')!` remain in index.ts** (grep) · **FULL worker suite 704 suites / 11640 tests GREEN** (the hot-file change warrants the full run; incl. dashboard_routes + require_org + marketing_dashboard) · eslint clean on touched files. Worker → CI on push; prod-verify the dashboard route + a flag-route 401.
- **Result:** ✅ index.ts batch swept — the `orgId!` class KILLED tree-wide, the last real `as any` in index.ts replaced with a validated boundary parser, and the metric-source SSOT de-duplicated; +5 regressions; full suite green. **Files mine:** `src/index.ts` · `libs/features/marketing_dashboard/service.ts` · `libs/features/marketing_dashboard/__tests__/service.test.ts` · `_CODE_SWEEP_LEDGER.md`. Protected files untouched. Next batch: **packages/shared** (grepped clean of high-signal classes → deeper dimensions: Zod-boundary/JSDoc/dead-export audit), then the **Angular admin frontend**.

#### Fire 11 addendum — PROD-VERIFY caught a 500 regression in the `requireOrgId` helper (Fire 10 + 11), root-fixed
- **The catch (why "never a compile"):** after the index.ts deploy landed, curling the changed flag routes UNAUTHED returned **500 INTERNAL_ERROR, not the intended 401** (`/api/system/status`, `/api/activity`, `/api/onboarding`). A compile/unit pass would NEVER have caught this — the `require_org.test.ts` unit test was GREEN because it asserted the thrown class's `.status` in isolation.
- **Root cause (TWO AppError classes):** `requireOrgId` threw `UnauthorizedError` from `src/platform/errors.ts` — a SEPARATE abstract `AppError` (property `.status`) from `@project-sites/shared`'s `AppError` (property `.statusCode` + `.toJSON()`). The global `error_handler` maps ONLY `err instanceof AppError` **from the shared barrel**, reading `.statusCode`/`.toJSON()` — so the platform error was NOT `instanceof` it → fell through to the `INTERNAL_ERROR` 500 branch. This silently affected ALL **38** orgId!→requireOrgId sites (Fire 10's 24 libs/features + Fire 11's 14 index.ts). The other platform-error throwers (`routes/jobs.ts`, `middleware/authz.ts`) are FINE — they self-map via `toErrorResponse(err)` locally; `requireOrgId` was the sole thrower relying on the GLOBAL handler.
- **★ ROOT FIX:** `require_org.ts` now throws the shared `unauthorized()` factory (`@project-sites/shared` → a real `AppError` the global handler maps to a 401 `{code:'UNAUTHORIZED'}` envelope). One helper fix repairs all 38 sites.
- **★ REGRESSION HARDENED:** rewrote `require_org.test.ts` to lock the ACTUAL error_handler contract — `instanceof` the SHARED `AppError` + `.statusCode`===401 (the property the handler reads, NOT `.status`) + `.toJSON().error.code`==='UNAUTHORIZED'. The old test asserted the platform class's `.status`, which is exactly why it stayed green while prod 500'd. Now a wrong-class regression FAILS the unit test.
- **Verify:** tsc 0 · full worker suite **704 / 11640 GREEN** · pushed `cb61c3ae4` → CI; re-curl the flag routes unauthed → expect **401** (was 500).
- **Lesson (durable):** a shared helper that throws a typed error consumed by the GLOBAL error_handler MUST throw the `@project-sites/shared` `AppError` (via its factories: `unauthorized`/`badRequest`/…), never the `platform/errors.ts` class (that one is for LOCAL `toErrorResponse` sites only). And a regression for such a helper must assert `instanceof` the SHARED class + `.statusCode`, not the thrown class's own props — else it passes green while prod 500s.

### Fire 12 — 2026-09-16 — Angular admin frontend: type the Stripe SDK shims (kill 6 `no-explicit-any` disables + 2 `any` + a cast) (batch "frontend/services")
- **Verify-before-implement:** worker HEAD current; apex prod 200; frontend clean of my targets. This is the Fire-11-flagged next batch (**Angular admin frontend**, worker-only swept 1-11). Tree-wide high-signal grep of `frontend/src`: `: any`/`as any` = **3** (1 JSDoc PROSE in homepage:476 + 2 real in `stripe.service.ts`); `console.log` = **1** (a JSDoc `@example` in org-api.service — prose); `@ts-ignore`/`eslint-disable` = **9**, all justified (no-constant-condition retry, max-len docs, Stripe no-explicit-any ×6). The frontend is already TS-strict-clean — the ONE real cluster is the Stripe SDK shims.
- **★ ROOT FIX — `services/stripe.service.ts` (conversion-critical billing path):** the Stripe.js typings were `type StripeElements = any` + `type StripeElement = any` + `initEmbeddedCheckout(): Promise<any>` + `mountEmbeddedCheckout(): Promise<any>` behind **6 `// eslint-disable-next-line @typescript-eslint/no-explicit-any`** comments, plus a `return ec as unknown as MountedElement` cast. Replaced with **narrow local interfaces** declaring only the SLICE of Stripe.js actually called — `StripeElement {mount,unmount,destroy,on}`, `StripeElements {create}`, `StripeEmbeddedCheckout {mount,unmount,destroy}`, and typed `StripeInstance`. Result: **0 `any`, 0 eslint-disables, 0 casts** in the file — every call site (`elements.create`, `ec.on/mount`, `checkout.mount`, `confirmPayment`) is now type-checked, and `return ec` type-checks because `StripeElement` is a structural superset of `MountedElement` (no cast needed). Full type safety WITHOUT bundling the heavy `@stripe/stripe-js` types.
- **★ REGRESSION TEST — new `stripe.service.spec.ts` (4, the file had NONE):** locks the fail-soft contract the typed methods must honor — no publishable key (no `<meta x-stripe-pk>`) → `loadStripe()` + `mountEmbeddedCheckout()` + `mountExpressCheckout()` + `mountPaymentElement()` all resolve to **null without throwing** + clear the `loading` signal (a throw here would crash the billing dialog for every visitor). **4/4 SUCCESS** (isolated `ng test --include`).
- **Verify:** `tsc -p tsconfig.app.json` **0 errors** (narrow types compile at every call site incl. `return ec`) · eslint **0 errors** on the file (the 8 remaining warns are pre-existing `perfectionist/sort-objects` nits, untouched) · `build:prod` GREEN (all 10 build gates pass, `main-P6O4LDZN.js`) · **R2-deployed** (291 files) · **PROD-VERIFIED**: served hash = `main-P6O4LDZN.js` (matches) + `/admin/billing` (the StripeService consumer) loads with **0 console errors** + plan copy present. Types erase at runtime → behavior identical, verified no regression on the billing surface.
- **★ FINDING (pre-existing, separate slice — Rec):** `npm run test:ci` (full 1781-spec Karma suite) crashes with an **order-dependent "object is not iterable" thrown in an `afterAll`** → runner DISCONNECTED (reproduced WITHOUT my spec at 1660/1781; WITH at 22/1785 — Karma randomizes order). The full unit suite doesn't complete cleanly, so frontend unit CI is unreliable; isolated `--include` runs work. A dedicated fire should trace the crashing `afterAll` (a spec iterating something undefined in teardown — cf. CLAUDE.md gotcha #9 module-global order-fragility) + fix its isolation. Not mixed into this slice.
- **Result:** ✅ frontend/services batch — the Stripe `any`+disable cluster KILLED at root with narrow honest types + the file's first regression spec; billing prod-verified clean. **Files mine:** `frontend/src/app/services/stripe.service.ts` + `frontend/src/app/services/stripe.service.spec.ts` + `_CODE_SWEEP_LEDGER.md`. Protected files untouched. Next batch: **frontend components** (grep clean too → deeper a11y/dead-code/duplicate-logic dimensions), and the **pre-existing Karma afterAll crash** as its own slice.

### Fire 13 — 2026-09-16 — Karma full-suite DISCONNECT: PRECISELY BISECTED to billing.component.spec.ts (diagnosis; reliable fix = dedicated deep-debug)
- **Verify-before-implement:** worker HEAD current; apex prod 200; targets clean. Batch = the Fire-12-flagged **pre-existing Karma `afterAll` crash** (the full frontend unit suite `npm run test:ci`, 1781 specs, DISCONNECTs mid-run → frontend unit CI unreliable).
- **★ SYSTEMATIC BISECTION → exact file:** no user `afterAll` exists (framework/RxJS teardown). Parallel-shard (`KARMA_PARALLEL=1`) showed 3/4 shards GREEN, 1 crashes → a specific spec, not a global-after-everything. Directory `--include` bisect (all CLEAN unless noted): services **105✓** · directives **13✓** · components **232✓** · animations/guards/interceptors/utils **✓** · pages-non-admin **141✓** · admin-top-level **73✓** · sections **M-Z 423✓** · analytics-isolated **73✓** · ai-* **31✓** · sections **[f-l] 133✓** · **[d-e]/[b-e] narrowed to `b*`** → **`pages/admin/sections/billing.component.spec.ts`** DISCONNECTs **24-33 of 39 in ISOLATION** (order-dependent). The 39 specs PASS on lucky orders; the crash is a Jasmine-random-order **teardown "object is not iterable" (RxJS errorContext)** that hangs the runner 30s → DISCONNECT.
- **★ RULED OUT (2 hypotheses, empirically):** (1) `teardown: { destroyAfterEach: false }` (double-teardown w/ the spec's legacy `afterEach(TestBed.resetTestingModule())`) — UNRELIABLE, still DISCONNECTs on some orders; (2) stubbing the unstubbed real `StripeService` (the component's embedded-checkout `effect()` injects it) — NO effect, still crashes 24-27/39 across 3 runs. Both reverted (systematic-debugging: don't ship a symptom patch after failed fixes).
- **Smoking-gun context:** the spec's OWN header admits it was authored in a worktree where "`ng test` (Karma) is not runnable ... AOT-verified via `npx nx build`" → it NEVER ran under Karma, so its teardown bug shipped unnoticed. It uses the heavy legacy per-`it` `build()` (`TestBed.configureTestingModule`) + `afterEach(resetTestingModule)` pattern; `AdminBillingComponent` has an `effect()` (`_embeddedMountFx`) + many `.subscribe()`s without `takeUntilDestroyed` — the destroy path is the prime suspect.
- **NEXT-FIRE recipe (dedicated deep-debug):** set Jasmine `random:false` + a fixed seed to make the crash deterministic → binary-search the 39 `it`s via `fdescribe`/`fit` to the exact spec → inspect that spec's component-DESTROY path (the `effect()` teardown under rapid per-`it` create/destroy, or a leaked subscription/observer). Likely real fix = migrate the spec off the per-`it` `build()`/`resetTestingModule` legacy pattern to a `beforeEach`-configure + automatic teardown, and/or add `takeUntilDestroyed` where the component leaks.
- **Result:** ⚠️ DIAGNOSIS-ONLY (no code shipped) — the crash is a genuinely hard order-dependent framework-teardown bug; I precisely LOCATED it (from opaque full-suite DISCONNECT → one file, 2 causes ruled out, deterministic-repro recipe) but did not land a RELIABLE fix within a bounded fire. Tree reverted clean. **Files mine:** `_CODE_SWEEP_LEDGER.md` only. Protected files untouched.

### Fire 14 — 2026-09-16 — Karma DISCONNECT ROOT-FIXED: leaked subscription + non-defensive iteration in billing.component (batch "the Fire-13 crash")
- **Verify-before-implement:** worker HEAD current; apex prod 200; targets clean. Batch = the Fire-13 pre-existing crash (the full frontend unit suite `npm run test:ci` DISCONNECTed → frontend unit CI unreliable since ~Fire 12).
- **★ CAPTURED THE EXACT STACK (Fire 13 never did):** ran the isolated spec 3× → `TypeError: object is not iterable (cannot read property Symbol(Symbol.iterator))` at **`billing.component.ts:2312`** inside an RxJS `errorContext` (a `.subscribe({next})`). NOT a teardown/framework bug — a real COMPONENT bug: `for (const cap of caps.data ?? [])`. `?? []` guards null/undefined but NOT a non-array `.data`; `for…of` a non-array object throws.
- **★ TWO root causes (both real, both fixed):** (1) the `/credit-caps` `.subscribe()` had **NO `takeUntilDestroyed`** (the whole component leaks ~26 subs) → on an unlucky Jasmine-random order a destroyed prior-test component's leaked subscription received ANOTHER test's mock (wrong shape); (2) the `next` handler **iterated an unvalidated response** → threw on the wrong shape → hung the runner 30s → DISCONNECT (order-dependent = the leak). ROOT FIX: `.pipe(takeUntilDestroyed(this.destroyRef))` on the crash-vector sub (no-leak correctness — `inject(DestroyRef)` added) + `for (const cap of Array.isArray(caps?.data) ? caps.data : [])` (fail-soft boundary — a malformed/wrong-source response degrades to empty drafts, same as the error path).
- **★ PROVEN FIXED (empirical, not a compile):** isolated billing spec **6/6 clean 39/39** (was 24-33/39 DISCONNECT) + **FULL suite `npm run test:ci` = 1787 SUCCESS** (was DISCONNECTing — the frontend unit CI is now RELIABLE). tsc 0 errors. `build:prod` GREEN → R2-deployed `main-7UAK5LKB.js` → **PROD-VERIFIED** `/admin/billing` authed loads (rootLen 38452), **0 console errors**.
- **Charter v2:** debugging via captured-stack root-cause (not the Fire-13 recipe's guess) + hardened error-handling (fail-soft defensive iteration) + correctness (no-leak subscription) + the regression proof is the now-reliable full suite. The 6/6 + 1787-green is the durable regression (the crashing spec is now stable).
- **Result:** ✅ the opaque full-suite Karma DISCONNECT is DEAD — root-caused to one line + one missing operator, fixed at both, full unit suite green + billing prod-verified. **Files mine:** `frontend/src/app/pages/admin/sections/billing.component.ts` · `_CODE_SWEEP_LEDGER.md`. FE deployed `main-7UAK5LKB.js`. Protected files untouched. **Next batch:** the residual **leaked-subscription class** — billing.component still has ~25 other `.subscribe()`s without `takeUntilDestroyed` (they set signals, don't throw, so no crash — but a real leak); a tree-wide `takeUntilDestroyed` audit across admin components is its own coherent slice.

### Fire 15 — 2026-09-16 — leaked-subscription class: RATCHET GATE (stop the bleeding) + 2 fixes (batch "the Fire-14-flagged leak class")
- **Verify-before-implement:** worker HEAD current; apex prod 200; targets clean. Batch = the Fire-14-flagged **leaked-subscription class** (`.subscribe()` without `takeUntilDestroyed` → the subscription outlives the component → a late emission mutates a destroyed component; the exact class that DISCONNECTed the Karma suite).
- **★ QUANTIFIED the class:** a precise scan (`.component.ts`, `.subscribe(` present, no `takeUntilDestroyed`/`takeUntil(`/`.unsubscribe(`) → **59 leaky components** (of 89 with subscriptions) — far too big for one fire, and a blanket `takeUntilDestroyed` transform is judgment-dense (existing `.pipe()` chains, manual `Subscription`s, poll intervals) + Karma-verification-heavy per-site.
- **★ THE FORCE-MULTIPLIER — a RATCHET gate (`frontend/scripts/check-subscription-leaks.mjs`):** scans for the class + compares to a `BASELINE` count; **FAILS only if the leaky count RISES** (a new component subscribes without `takeUntilDestroyed`) → **NO NEW leaks ever land** while the existing debt drains across future fires (lower BASELINE as fixed). Reports the count so progress is visible. **Wired into `build:prod`** (runs on every FE build + deploy + CI) — the class now can't grow. This is the audit-arc Detect→Surface→ratchet step for the whole class, not a one-component patch.
- **★ 2 root fixes (lower the baseline + demonstrate the pattern):** `public-analytics.component.ts` + `roadmap.component.ts` (1 subscribe each, clean `ngOnInit` `.get().subscribe()`) → added `inject(DestroyRef)` + `.pipe(takeUntilDestroyed(this.destroyRef))`. Baseline **59→57**, locked.
- **Verify:** the gate PASS at baseline 57 · FE tsc **0 errors** · **`build:prod` GREEN with the gate in the chain** (all 11 checks pass incl. sub-leaks 57=57) · **R2-deployed** + **PROD-VERIFIED** `/roadmap` (rootLen 13173, 0 console errors) + `/changelog` (20112, 0 errors) render clean — the `takeUntilDestroyed` fixes don't regress the live pages.
- **Charter v2:** progressive enhancement (a durable ratchet gate + 2 root fixes + wired into the build chain + structured count-reporting — several dims) + full journey (prod-verified the touched public pages render + 0 console errors) + faster pace (quantify→gate→fix→wire→build→deploy→verify in one coherent slice).
- **Fan-out note:** weighed a 4-6 agent fan-out over the 59 components — DEFERRED. Per `parallel-subagent-economy`, `takeUntilDestroyed` is judgment-dense (per-site: is this a pipe-chain / manual-sub / poll?) + each fix needs Karma verification (fragile in worktrees) → the ratchet GATE (which stops the whole class at once) beats parallel-patching 59 files this fire; the debt drains via the ratchet across dedicated future fires (or a careful fan-out when the recipe is proven per-shape).
- **Result:** ✅ the leaked-subscription class is now RATCHET-GATED (can't grow) + 2 fixed. **Files mine:** `frontend/scripts/check-subscription-leaks.mjs` (new) · `frontend/package.json` (wired) · `frontend/src/app/pages/public-analytics.component.ts` · `frontend/src/app/pages/roadmap/roadmap.component.ts` · `_CODE_SWEEP_LEDGER.md`. FE deployed. Protected files untouched. **Next batch:** drain the 57 — fix the high-sub init-firing components (user-settings 13 / snapshots 13 / forms 9) carefully with Karma verification, lowering BASELINE each fire.

### Fire 16 — 2026-09-17 — packages/shared: redactObject array-skip PII-leak root fix + latent dead-key bug (batch "packages/shared", deeper-dimension)
- **Verify-before-implement:** worker HEAD current; apex prod 200; `packages/shared` NOT dirty in another session (only my 4 files). Mechanical classes re-confirmed **0** (grep across all 44 files: 0 real `any`/`@ts-ignore`/non-null-`!`/`as unknown as`/`console.log` — the 1 hit is a JSDoc `@example`). This is the Fire-12-flagged deeper-dimension sweep (Zod-boundary/dead-export/correctness).
- **Fan-out:** 2 parallel `Explore` agents audited the 20 schema files (core/identity + feature) on deep boundary defects while I foreground-read the 6 security-critical utils (sanitize/redact/crypto/errors/rbac/entitlements). Agents surfaced boundary-tightenings; the foreground read found the real bug.
- **★ ROOT FIX (correctness + SECURITY — the canonical PII-redaction util) — `redactObject` array-skip leak:** `redact.ts:141` gated the recurse on `typeof value === 'object' && !Array.isArray(value)`, so **arrays fell to the `else` branch and passed through COMPLETELY UNREDACTED**. The file's own docstring says "all log output should pass through" it — yet `redactObject({recipients:['a@x.com','b@x.com']})` leaked both emails, and `redactObject({users:[{email,token}]})` leaked every element into logs/Sentry/PostHog. Fixed at root: extracted a shape-agnostic recursive **`redactValue(value)`** (string→`redact()`, array→`map(redactValue)`, object→`redactObject`, primitive→pass) so PII/secrets are redacted at EVERY depth incl. arrays-of-strings, arrays-of-objects, and nested array→object→array.
- **★ LATENT DEAD-KEY BUG fixed (found while refactoring the set):** the sensitive-keys set held `apiKey` (mixed-case) but the lookup does `key.toLowerCase()` → `'apikey' !== 'apiKey'` → the entry could **NEVER match**; an `{apiKey: '…'}` field was only value-pattern-redacted, never key-masked. Corrected to `apikey`. **Value-add:** widened to the common plural log-keys (`secrets`/`tokens`/`cookies`/`api_keys`) + froze the set as `ReadonlySet` (`SENSITIVE_KEYS`, module-const, keyed before any type dispatch so a `token:[…]` array is masked whole, never walked).
- **★ TDD (RED-first proof):** the new array cases FAILED against the old code (leak), pass now — 4 new `redact-secrets.test.ts` cases (array-of-strings PII, array-of-objects secret+PII, sensitive-KEY-holding-array fully masked, deep array→object→array) + **rewrote the stale `edge-cases.test.ts` test that LOCKED IN THE LEAK** ("passes through array values unchanged" → "recurses into array values, redacting at every element; returns a new array, original not mutated"). **Full shared suite 505/505 green** (was 505, +4 new −0), tsc 0, lint 0 (also cleared a pre-existing dead `slugSchema` import in `site-repository.ts` — the sweep's dead-code dimension). **Worker consumer tsc 0** against the updated shared (no contract break).
- **Charter v2:** progressive enhancement (a security root fix + a latent dead-key bug + a hardened+frozen key set + a dead-import cleanup + failing-test-first + a corrected leak-locking test — 6 dims) + full-journey-adjacent (the util feeds the whole app's structured logging — no user surface to browser-verify, so proven by the RED→GREEN suite + worker-consumer typecheck) + faster pace (2-agent schema fan-out ∥ foreground security read → fix → tests → verify in one coherent slice).
- **Result:** ✅ `redactObject` no longer leaks PII/secrets nested in arrays — the #1 blast-radius defect in the shared package (every `redactObject` caller across the worker is now safe by construction). **Files mine:** `packages/shared/src/utils/redact.ts` · `packages/shared/src/__tests__/redact-secrets.test.ts` · `packages/shared/src/__tests__/edge-cases.test.ts` · `packages/shared/src/schemas/site-repository.ts` · `_CODE_SWEEP_LEDGER.md`. Worker → CI on push (shared is `file:`-linked; the fix ships with the next worker deploy). Protected files untouched. **Next batch:** Angular admin frontend (drain the 57 sub-leaks) OR the shared schema boundary-tightenings the agents surfaced (billing currency ISO-4217 regex, AI-output `z.array(z.unknown())` → typed) — both low-risk, TRACKED.

### Fire 17 — 2026-09-17 — Angular admin frontend: calendar-widget WCAG 2.1.1 keyboard cluster root-fixed (batch "Angular admin frontend")
- **Verify-before-implement:** worker+FE git-clean of my files; apex + /admin 200. Mechanical scan across all **284** non-spec frontend files: **0 real** `any`/`as any`/`@ts-ignore`/`console.log` (a disciplined tree). Non-null-`!` hits are template-binding narrowing (`data()!.x` under a `@if (data())`) — benign, left. mousedown-only handlers on homepage/search/create were ALREADY keyboard-fixed (AL-481/482); billing `triggerRipple` mousedowns are cosmetic; social `insertMention` is textarea-keydown-driven.
- **★ FAN-OUT (3 read-only audit agents, disjoint dirs):** `components/*`, `services/guards/interceptors/utils`, `pages/admin/sections/*`. **services layer CLEAN** (ApiService error taxonomy + Sentry breadcrumbs + localStorage try/catch + finalize teardown). The subscription-leak cluster the component+section agents flagged (task-tray, notification-bell, command-palette) was **FALSE-POSITIVE** — verify-before-implement read each: task-tray `stopTimer()` + notification-bell `pollSub?.unsubscribe()` in `ngOnDestroy` (agents missed the teardown). Validator-precision held — did NOT "fix" non-bugs.
- **★ THE REAL DEFECT (a11y, WCAG 2.1.1 Keyboard, Level A) — `calendar-widget.component.ts`, dashboard-mounted (widgets.ts):** (1) week `.wc` + day `.d-row` "add event at this time" cells were bare `<div (click)="newAt(...)">` — mouse-only, keyboard-stranded; (2) a month-view event pill was an interactive `<span (click)="openEvent">` NESTED inside the day `<button>` (invalid nested-interactive HTML + a keyboard-stranded span + a double-fire with the day's own click).
- **★ ROOT FIX:** newAt cells → `role="button"` + `tabindex="0"` + `(keydown.enter)` + `(keydown.space)` (preventDefault) + a descriptive `aria-label` (the AL-481/482 pattern); month-view event pills → DISPLAY-ONLY (removed the nested `(click)` — interaction is the keyboard-operable day button; events are opened as real `<button>`s in the week/day views, which were already keyboard-fine). `openEvent` stays live (week/day buttons).
- **★ TDD + PROVEN:** +3 Karma (real ChromeHeadless DOM render: week `.wc` + day `.d-row` carry role=button/tabindex/aria-label; month `.evt` pills carry NO role/tabindex) → **11/11 green**, app tsc 0. FE built + R2-deployed; **/admin prod-verified in real Chromium** (200, "Dashboard" H1, rootLen 92222, 0 console errors — the calendar is an optional catalog widget, not default-visible, so the a11y attrs are Karma-proven on the deployed template + the dashboard verified live).
- **Charter v2:** a11y dimension advanced through a real keyboard-journey fix + sweep-more (mechanical scan of 284 files + 3-agent fan-out over 3 disjoint dirs) + failing-test-first Karma + false-positive discipline (verify killed the leak cluster) — several dims; faster pace via the parallel audit.
- **Result:** ✅ the dashboard calendar's "add event" cells are keyboard-operable + the invalid nested-interactive month pill is resolved; the rest of the frontend is mechanically clean + the services layer is architecturally sound. **Files mine:** `frontend/…/dashboard/calendar-widget.component.ts` + `.spec.ts` · `_CODE_SWEEP_LEDGER.md`. FE → R2 (deployed + prod-verified). Protected files untouched. **Next batch:** editor (bolt.diy `app/`), then the 3 tracked settings.ts findings + re-sweep.

### Fire 18 — 2026-09-17 — Angular admin: the 3 tracked settings findings — ROOT-FIXED a shipped broken-auth bug (dead `session_token` key) + upload timeout hardening (batch "Angular admin frontend", AL-710)
- **Verify-before-implement:** apex 200; settings files git-clean (no other-session dirtiness). Verified each Fire-17-tracked finding rather than blindly fixing (validator-precision): **#1 (toggle error path, L905) = FALSE POSITIVE** — ApiService.handleError toasts "on every error" + reverts optimistically (comment "api.service already toasted" is accurate). **#3 (API-key optimistic row, user-settings L1300) = FALSE POSITIVE** — the row is added in the `next:` success handler from the real `created` server response + `loadApiKeys()`, error path sets `createError`. Did NOT touch either non-bug.
- **★ THE REAL BUG (#2 promoted from "no AbortController" to a SHIPPED BROKEN-AUTH defect):** the raw-XHR knowledge-file upload (`settings.component.ts` ×2) + the `.env` export (`env-vars-manager.component.ts`) all read `localStorage.getItem('session_token')` — **a key NEVER written anywhere** (3 GETs, 0 SETs; refactor drift when the token moved to `ps_session`). So every upload/export sent `Authorization: Bearer null` → the **Bearer-ONLY worker** (`middleware/auth.ts`, no cookie fallback) rejected them. Knowledge-file upload + brand-asset upload + env-var plaintext export were **all shipped-broken (401)** — a "doomed control" (embarrassingly-easy forbids). ROOT FIX: `this.auth.getToken()` (the SSOT `AuthService`, `ps_session`) in all 3 sites (added the `AuthService` inject to both components).
- **★ HARDENING value-add (the original #2 point):** the raw XHR bypasses ApiService's 30s timeout → a hung upload spun "uploading" forever (owner stranded). Added `xhr.timeout=60000` + `ontimeout` → fails the row + an actionable toast ("… timed out — check your connection and try again."); + `AbortController`-equivalent: an `inFlightUploads` Set aborted in `destroyRef.onDestroy` (navigate-away mid-upload no longer fires a late toast on a dead view). Both upload paths + a `settle()` cleanup.
- **★ TDD + PROVEN LIVE:** +4 Karma regression (`settings.component.spec.ts`, AL-710 describe — stubs `XMLHttpRequest`: Bearer from `getToken()` NOT the dead key · `timeout===60000` · `ontimeout`→row error+toast · signed-out omits Authorization, never `Bearer null`) → **Karma 1802/1802 SUCCESS**, app+spec tsc clean. FE built (`main-DAHQHHL6.js`) + **R2-deployed** (291/291, CDN purged). **PROD-VERIFIED in real Chromium:** fresh bundle live · /admin 0 console errors · **CAUSAL AUTH: POST the upload endpoint with the real Bearer → 400 (auth PASSED, empty body) vs a bogus Bearer → 401 (auth rejected)** — the old `Bearer null` would 401 like bogus, so the fix is proven broken→working end-to-end.
- **Charter v2:** advanced correctness (broken-auth root-fix) + hardened error-handling (timeout + abort + owner-guiding message) + failing-test-first regression + a real-browser causal prod-verify — several dims; every gate green. Pairs with EMBARRASSINGLY-EASY (removed 3 doomed controls; a hung upload now guides instead of stranding).
- **Result:** ✅ knowledge-file upload + brand-asset upload + env-var export now authenticate correctly (were silently 401-broken) + can't spin forever. **Files mine:** `frontend/…/sections/settings.component.ts` · `frontend/…/components/env-vars-manager/env-vars-manager.component.ts` · `frontend/…/sections/settings.component.spec.ts` · `_CODE_SWEEP_LEDGER.md`. FE → R2 (deployed + prod-verified). Protected files untouched. **Next batch:** editor (bolt.diy `app/`) — still the sole `[ ]` unswept tree — then re-sweep from the top.

### Fire 19 — 2026-09-17 — editor (bolt.diy `app/`): RED "CI/CD" pipeline GREENED at root — `multiline-comment-style` + a cache-masked prettier-3.6.2 heritage-clause drift class (batch "editor app", AL-716)
- **Verify-before-implement:** apex prod 200; `app/` git-clean (not dirty in another session). Batch = the Fire-18-flagged editor tree — the sole `[ ]` — whose CI (`ci.yaml` = "CI/CD", separate from the worker deployer) had been **RED for multiple fires**. Pulled the EXACT failed lint log rather than guessing: ONE error — `DataPanel.tsx:473:27 multiline-comment-style` ("Expected a block comment instead of consecutive line comments"). typecheck/tests/docker all passed — lint was the sole blocker.
- **★ ROOT FIX #1 (the named blocker):** a prior WCAG-fix fire wrote a 2-line `//` comment in the row `onKeyDown`; the Blitz preset's starred-block rule rejects consecutive `//`. Converted to a starred `/* * */` block matching the file's own 176-179 convention (exactly `--fix`'s output).
- **★ ROOT FIX #2 (a hidden class, surfaced by installing the toolchain):** `pnpm install --ignore-scripts` dodges the electron-builder blocker → could run the EXACT CI gates locally. A FRESH full `eslint app` surfaced **2 MORE errors CI never reported** (`Button.tsx`, `StickToBottom.tsx`, `prettier/prettier`) — cache-masked in CI by eslint's `restore-keys`. These are genuine multi-line `extends` heritage-clause drift: the root devDep `prettier ^3.5.3` resolves in `pnpm-lock.yaml` to **3.6.2** (eslint-plugin-prettier binds `prettier@3.6.2`); prettier 3.6.0 changed heritage-clause wrapping, so the files were 3.6.2-dirty (a latent cold-relint red).
- **★ THE VERSION-DRIFT TRAP (bit me once mid-fire — recorded):** my local `--prefer-offline` install pulled prettier **3.8.3** (not the locked 3.6.2). I first "fixed" the 2 files with `prettier@3.9.1 --write` → CI (3.6.2) REJECTED it ("Insert `⏎`"). Root cause: NEVER format editor files with a prettier version not confirmed against the lock. Corrected by (a) reading the resolved version from the lock's root importer, (b) `prettier@3.6.2 --write`, (c) **swapping `node_modules/prettier` → 3.6.2** (npm-pack + extract over the pnpm symlink) so `eslint app` is authoritative == CI. (Memory `[[editor-ci-prettier-version-drift-npm-vs-pnpm]]` sharpened with this exact procedure.)
- **★ VALUE-ADD (extra-mile keyboard UX):** Escape now collapses an open row detail — the natural "close this" gesture pairing the Enter/Space open. New pure `isDismissKey(key)` in `data-panel-logic.ts` (mirrors `isRowActivationKey`, full JSDoc+`@example`) used in the row `onKeyDown`; +2 RED-first `it` blocks in `data-panel-logic.spec.ts`.
- **★ VERIFY (exact CI toolchain, node_modules/prettier pinned to 3.6.2):** `tsc` exit 0 (0 errors) · `eslint app` **exit 0 (0 problems — was 3)** · `vitest data-panel-logic.spec.ts` **22/22** · `prettier@3.6.2 --check` clean on all 5 touched files. Pushed → editor **"CI/CD" run 35276493488 = SUCCESS** (type check ✓ · ESLint ✓ · tests ✓) — GREEN for the first time in multiple fires. Editor prod is CF-gated (headless→403, curl→200 — the `cf-access` bot-challenge pattern, not a break); the Escape value-add is unit-proven + on main and ships on the editor's next Pages `bolt-diy` deploy (a deliberate separate deployable, not main-push-triggered — a full `remix vite:build` rebuild of the large gated app isn't warranted for a keyboard rider).
- **Charter v2:** correctness (2 root fixes) + a11y/keyboard enhancement + failing-test-first + the drift-trap lesson captured — several dims; the authoritative gate (editor CI) went red→green.
- **Result:** ✅ the editor "CI/CD" pipeline is GREEN — the DataPanel blocker + a hidden prettier-3.6.2 heritage-clause drift class both root-fixed, plus a keyboard-UX value-add + regression. **Files mine:** `app/components/workbench/DataPanel.tsx` · `app/components/workbench/data-panel-logic.ts` · `app/components/workbench/data-panel-logic.spec.ts` · `app/components/ui/Button.tsx` · `app/lib/hooks/StickToBottom.tsx` · `_CODE_SWEEP_LEDGER.md`. Editor → Pages bolt-diy (CI on push, deploy separate). Protected files untouched. **Next batch:** the WHOLE tree is now swept once (worker 1-11 · frontend 12-15,17,18 · shared 16 · editor 19) → **re-sweep from the top** (worker src / frontend — code changed since Fire 1), OR deeper editor batches (workbench siblings, chat, terminal).

### Fire 20 — 2026-09-17 — RE-SWEEP (4-agent fan-out): Zod-validated the CORE FUNNEL boundary (worker) + AiChat network-failure catch (template) (batch "worker + template", AL-724)
- **Verify-before-implement:** worker+template git-clean of my files; apex + platform face 200. Whole tree swept once (Fires 1–19) → re-sweep. Confirmed the Fire-9 deferral (`c.get('orgId')!`→`requireOrgId`) is ALREADY resolved (helper exists + in use; only 3 `!` remain, in the helper's own impl/test). **★ FAN-OUT: 4 parallel `feature-dev:code-reviewer` agents** on DISJOINT subsets (AI/LLM services · auth/billing/db services · feature modules · template components/lib) — each returned 1 verified real defect. Validator-precision held (each agent VERIFIED before reporting; rejected the false-positive candidates — e.g. `media.ts` R2-orphan cleanup is a deliberate saga, `webhook.ts` race is DB-UNIQUE-guarded).
- **★ PRIMARY ROOT FIX (worker, correctness+Zod boundary) — `POST /api/sites/create-from-search`:** THE primary conversion funnel (mounted always-live at `index.ts`) read its body via a raw `as CreateFromSearchBody` cast with ZERO runtime narrowing + the module had NO `schemas.ts` (feature-module drift). Failure chain: a numeric `business_name` → `(12345).trim()` **500 crash**; a numeric `business_type` → `as string` **lying-success persist** of a number into the `business_category` TEXT column → later `.toLowerCase()` crash in the vertical classifier. Fix: new `libs/features/site_creation/schemas.ts` (`createFromSearchSchema`, every field optional STRING, nested `business` + `types:string[]`, `.passthrough()` for Places extras, `z.infer` type) + `safeParse → 400` at the handler top (the contact_newsletter sibling idiom) + dropped the 3 `as string|undefined` casts in the category chain (now type-safe). **+7 regression tests** (numeric business_type/name/types→400 · v1+v2 valid→pass · Places-extras passthrough · empty→pass). tsc 0, **full worker suite 11,697/11,697**.
- **★ SECONDARY ROOT FIX (template, error-handling/UX) — `AiChat.tsx` missing `catch`:** `onSubmit` wrapped fetch+SSE-read in `try{…}finally{setStreaming(false)}` with NO `catch` → a fetch REJECTION (offline/DNS/CORS/ad-blocker) or a mid-stream reader throw left the empty assistant bubble hanging FOREVER + an unhandled promise rejection (console-error-gate fail). Fix: a `catch` that fills the bubble with a graceful fallback, PRESERVING any partial stream (`…(Connection lost — email us…)`) so a late disconnect keeps the half-answer. **+1 render regression** (`AiChat.resilience.test.tsx`: real `@testing-library/react` render + rejecting `fetch` → asserts the fallback text appears + fetch was attempted; jsdom-shimmed `showModal`/`close`/`scrollIntoView`/`scrollTo`). Template **352/352**, build green (19 routes / 217 tokens).
- **★ TRACKED (verified-real, next fires — not this coherent slice):** (1) **billing duplicate-Stripe-customer race (HIGH)** — `billing.ts:88-169` `getOrCreateStripeCustomer` does a non-atomic check-then-act + Stripe `POST /v1/customers` with NO `Idempotency-Key` (the code's own comment admits it) → a double-click/retry mints DUPLICATE customers + no `UNIQUE(org_id)` on subscriptions; fix = stable `Idempotency-Key: create-customer:${orgId}` header (+ a dedup-first `UNIQUE(org_id)` migration, one-way-door → its own careful fire). (2) **`ai_workflows.ts` observability-blind** — `safeCaptureWorkersAi` is fully built but has ZERO call sites + `runPrompt` hardcodes `tokenCount:0` → the entire Workers-AI research pipeline fires 0 PostHog `$ai_generation` events (JSDoc lies); wire it into runPrompt success/error + a chars/4 token estimate (the `built-but-unwired` class).
- **Charter v2:** swept MANY files (4-agent fan-out over 4 disjoint trees) + advanced SEVERAL dims (correctness · Zod boundary · drift (added missing schemas.ts) · graceful error-handling · dead-code (dropped `as` casts) · failing-test-first ×2) + faster pace (parallel audit) — two disjoint trees fixed, every gate green.
- **Result:** ✅ the core funnel boundary is Zod-validated (wrong-typed field → clean 400, never a 500/lying-success) + the AiChat widget degrades gracefully offline. **Files mine (worker):** `libs/features/site_creation/schemas.ts` (new) · `handlers.ts` · `__tests__/create-from-search-schema.test.ts` (new) · `_CODE_SWEEP_LEDGER.md`. **Files mine (template):** `src/components/AiChat.tsx` · `AiChat.resilience.test.tsx` (new). Worker→CI on push; template→repo push (next build). Protected files untouched. **Next batch:** the tracked billing-idempotency + ai_workflows-observability fixes, then continue the re-sweep (worker services deeper-dimension).

### Fire 21 — 2026-09-18 — closed BOTH Fire-20 tracked findings: billing dup-customer race (Stripe Idempotency-Key) + ai_workflows observability-blind (built-but-unwired) (batch "worker services", AL-746)
- **Verify-before-implement:** worker git-clean of my files; apex + platform 200. Confirmed BOTH Fire-20-tracked findings are STILL real (not fixed since) by reading the source, not trusting the note.
- **★ ROOT FIX #1 (correctness + REVENUE, HIGH) — `billing.ts` `getOrCreateStripeCustomer` dup-customer race:** the check-then-act (SELECT existing → else POST `/v1/customers`) is NON-atomic AND the Stripe POST carried NO `Idempotency-Key` — the code's own comment admitted "a retry would create a DUPLICATE customer." Two concurrent checkouts (double-click / client retry) both miss the SELECT → mint TWO Stripe customers for one org (billing mess + no `UNIQUE(org_id)`). Fix: a STABLE `Idempotency-Key: create-customer:${orgId}` header → Stripe returns the SAME customer on any retry within its 24h window (beyond 24h the persisted row short-circuits the SELECT). **Bonus self-heal:** a lost subscription-row insert (currently logged-not-thrown so checkout proceeds) now heals on the next call — SELECT misses → re-POST same key → same customer (no dup) → row insert retries. Updated the stale "no idempotency key" comment. **The UNIQUE(org_id) migration remains TRACKED** (one-way-door → dedup-first, its own careful fire) — the idempotency key is the safe, additive, non-migration half that closes the live race NOW.
- **★ ROOT FIX #2 (observability / built-but-unwired + lying-JSDoc) — `ai_workflows.ts` `runPrompt`:** `safeCaptureWorkersAi` was fully built (fail-soft PostHog `$ai_generation` wrapper) with **ZERO call sites**, and `runPrompt` hardcoded `tokenCount: 0` — so the ENTIRE Workers-AI research pipeline (research_profile/social/brand/selling_points/images/generate_website) emitted **0 generation events**, despite the `@remarks Every successful env.AI.run() fires a PostHog $ai_generation event` JSDoc. Fix: wired `safeCaptureWorkersAi` after the `withObservability` call (provider `workers_ai`, model, promptId, best-effort `~chars/4` input+output token estimates, costUsd 0, status ok, trace-rolled-up via `traceContext.orgId/traceId`); replaced the hardcoded `tokenCount:0` with `Math.ceil(text.length/4)` so `tokensUsed` + `$ai_output_tokens` are real estimates. Now the build pipeline's per-prompt cost/latency lands in PostHog LLM Observability + Langfuse (captureLLMCall tees both).
- **★ TDD (RED-first both):** billing — the "creates new customer" test now asserts `init.headers['Idempotency-Key'] === 'create-customer:org_1'` (RED before: no header) + fixed the stale leak-locking comment in the sub-row-fail test; ai_workflows — a NEW test mocks the analytics boundary (GLOBAL `jest` hoist per swc-jest-module-mock-pitfall) + asserts `captureLLMCall` fires ONCE with `provider:workers_ai` + `inputTokens>0` + `outputTokens>0` + `result.tokensUsed>0` (RED before: 0 call sites, tokensUsed 0). **Both suites GREEN — 50/50** (billing + ai_workflows), tsc 0, lint 0 errors.
- **Charter v2:** swept 2 disjoint worker services + advanced SEVERAL dims (correctness/revenue root-fix · observability wiring · dead-code (killed the unreachable safeCaptureWorkersAi + hardcoded-0) · lying-JSDoc→true · hardened idempotency + self-heal · failing-test-first ×2) — closed BOTH prior tracked findings in one coherent slice.
- **Result:** ✅ concurrent checkouts can't mint duplicate Stripe customers + the Workers-AI build pipeline now emits real `$ai_generation` telemetry (was invisible). **Files mine:** `src/services/billing.ts` · `src/services/ai_workflows.ts` · `src/__tests__/billing.test.ts` · `src/__tests__/ai_workflows.test.ts` · `_CODE_SWEEP_LEDGER.md`. Worker → CI on push (prod-verify the billing customer path via verify-billing-full-flow post-deploy). Protected files untouched. **Next batch:** the TRACKED `UNIQUE(org_id)` subscriptions migration (dedup-first, one-way-door) + continue the worker-services re-sweep (domains/media/rag deeper-dimension).

### Fire 22 — 2026-09-18 — worker-services re-sweep (4-agent fan-out): external_llm vision-fetch hang (fail-soft) + mcp Twilio paste-key validation (batch "worker services", AL-748)
- **★ FAN-OUT (4 parallel `feature-dev:code-reviewer` agents, DISJOINT files, read-only):** domains+domain_suggester · media · mcp_client · rag+external_llm. Each verified-before-reporting + returned its single highest-confidence defect. Validator-precision held — **2 of 4 rejected as false-positives on my own re-verification:** (1) domain_suggester positional-index "bug" (MEDIUM) — the agent couldn't read `rdap_availability.checkBatch`; I did: it's `new Array(domains.length)` + `out[idx]=…` for every index → GUARANTEED order+length-preserving, so `probes[i]↔fresh[i]` is safe. NOT a bug; did NOT Map-refactor a correct positional access. (2) media — agent returned CLEAN (every stock-API fetch gates on `res.ok`, R2 keys routed through `safeFileName`, upload-orphan cleanup is a deliberate saga). (3) mcp Discord/zapier "SSRF" — the fetch URL IS the owner's own pasted webhook (trusted, vendor-documented), not attacker-controlled → rejected.
- **★ ROOT FIX #1 (HIGH — fail-soft / hung-request) — `external_llm.ts:1094` `callAnthropicWithVision`:** the Anthropic-vision path base64s `options.imageUrl` via a BARE `fetch(options.imageUrl)` — NO AbortSignal/timeout, while EVERY other outbound call in the file (`callOpenAI` L410, `callAnthropic` L574) wraps fetch in an `AbortController`+`setTimeout`. `withRetry` bounds retry COUNT, not per-attempt duration, so a slow/dead image host (stalled signed-URL proxy, dead CDN) HANGS the whole vision call indefinitely until the platform wall-clock kills the isolate — breaking the file's own fail-soft contract. Fix: same `AbortController`+`setTimeout(30_000)`+`finally{clearTimeout}` pattern. +regression (`external_llm.test.ts` AL-748): mocks global.fetch for the image download, asserts it's called with `init.signal instanceof AbortSignal` (RED before: bare fetch, no signal).
- **★ ROOT FIX #2 (MEDIUM — guide-the-owner / hardened error-handling) — `mcp_client.ts:640` Twilio paste-key:** `const [sid,key] = token.split(':')` assumed the pasted key always has a colon; pasting only the AUTH_TOKEN → `key===undefined` → `btoa('sid:undefined')` → an opaque Twilio 401 with no hint. Fix: the Twilio `endpoint()` now throws a clear `"…must be ACCOUNT_SID:AUTH_TOKEN"` when the split is malformed, AND the generic paste-key executor (L433) now wraps `opts.endpoint()` in try/catch → surfaces ANY endpoint validation-throw as a typed `{ok:false,error}` (benefits all 15 paste-key adapters) BEFORE any network call. +2 regressions (`mcp_client.test.ts` AL-748): malformed token → clean error + NO fetch; well-formed `SID:TOKEN` → builds the correct Twilio URL.
- **★ VERIFY:** both suites GREEN — **69/69** (external_llm + mcp_client), tsc 0, lint 0 errors. Worker→CI on push (both are worker-internal — the AbortSignal + typed-error contracts are unit-proven; no user-facing route to browser-verify).
- **Charter v2:** 4-agent parallel fan-out swept 6 disjoint service files + advanced SEVERAL dims (fail-soft hung-request fix · guide-the-owner validation + shared-executor hardening · failing-test-first ×3 · validator-precision that rejected 2 false-positives + 1 CLEAN) — faster pace via the parallel audit, quality via the re-verification.
- **Result:** ✅ a slow/dead vision-image URL can no longer hang the LLM pipeline + a malformed Twilio paste-key now guides the owner instead of an opaque 401; media/domains verified clean. **Files mine:** `src/services/external_llm.ts` · `src/services/mcp_client.ts` · `src/__tests__/external_llm.test.ts` · `src/__tests__/mcp_client.test.ts` · `_CODE_SWEEP_LEDGER.md`. Worker → CI on push. Protected files untouched. **Next batch:** the TRACKED `UNIQUE(org_id)` migration (dedup-first) + re-sweep site_serving/build_validators deeper-dimension.

### Fire 23 — 2026-09-18 — re-sweep (4-agent fan-out): brand-gate stopword false-negative (build_validators) + notifications suppression-skip observability (batch "worker services deeper-dimension", AL-758)
- **★ FAN-OUT (4 parallel `feature-dev:code-reviewer` agents, DISJOINT files, read-only, verify-before-reporting):** `site_serving.ts` · `build_validators.ts` · `auth.ts` · `notifications.ts`. Validator-precision held — **2 of 4 came back CLEAN on the agent's OWN verification:** site_serving (traced the full hot serve path + serve-time transforms + edge-cache + JSON-LD strip + hero synth — every risky op fail-soft-wrapped, rejected 6 candidates) and auth (full token lifecycle: magic-link atomic compare-and-swap consumption, OAuth-state 10-min expiry + server-delete, SHA-256 hashed sessions + parameterized D1, `db.batch()` atomic user creation, no raw-token logging). 2 returned a verified real defect.
- **★ ROOT FIX #1 (correctness, HIGH — a HARD publish-gate false-negative) — `build_validators.ts:828` `validateBrandNameMatch`:** the sub-page brand check uses the brand SHORT name (first word of the expected name) to fit the 50-60 char SEO title cap, computed as `eNorm.split(' ')[0]` with NO stopword/length guard. For a business named **"The Coffee House"** (or "A1 Diner" / "New Leaf Yoga" — an extremely common leading-stopword pattern) `firstWord = "the"`, so the sub-page check degrades to `title.includes("the")` → it matches ANY sub-page title carrying an incidental "the" (an LLM-invented "Hearth & Crumb — About **the** Team") → the HARD brand gate (`site-generation.ts` publish-blocker, throws `Brand gate failed`) **wrongly PASSES an invented, unrelated business name**, defeating the validator's entire purpose. Verified myself (I own this fn from AL-749): read the exact substring mechanism + confirmed the gate is reachable + no test covered a stopword-leading name. Fix: `firstWord = words.find(w => w.length >= 3 && !BRAND_STOPWORDS.has(w)) ?? words[0]` (first DISTINCTIVE word; falls back to the raw first token only if the whole name is stopwords, so it never degenerates to '' or over-matches). **+2 RED-first tests** (invented sub-page for "The Coffee House" → flags · legit "The Coffee House — Menu" → passes on "coffee").
- **★ ROOT FIX #2 (observability / hardened error-handling) — `notifications.ts:121` `sendEmail` SES branch:** the branch `await`ed `email.sendTransactional(...)` and `return`ed, DISCARDING the `EmailResult` — so a router `{accepted:false}` (a SUPPRESSED hard-bounced/complained recipient, reached only when the top-of-fn suppression check fail-opened on a D1 error) silently `return`ed as if delivered (a lying-success — no log). Fix: check `result.accepted`; on `false`, log an explicit `send_skipped_suppressed_at_router` skip so it's OBSERVABLE + never masquerades as a delivered SES send. Verified `accepted:false` = suppression-ONLY (`email-router.ts:94`; real failures THROW → SendGrid fallback), so the fix correctly does NOT fall through to SendGrid (a fallback would BYPASS suppression + re-hit a bounced address, harming domain reputation). **+1 RED-first test** (router suppresses → skip log fires + no SendGrid fetch). (The caller-return-contract half — `notifySiteBuilt` reporting `ok:true` for a skip — is the JSDoc-tracked gap; a bigger `Promise<void>→{delivered}` refactor, a separate slice.)
- **★ VERIFY:** both suites GREEN — **184/184** (build_validators + notifications + related), tsc 0, **eslint 0 errors** (161 warnings are pre-existing codebase-wide `sort-objects` style; my log block matches the file's key order — no `--fix` = no scope-creep into non-mine files).
- **Charter v2:** 4-agent parallel fan-out swept 4 disjoint service files + advanced SEVERAL dims (correctness root-fix on a hard gate · observability/hardened-error-handling · failing-test-first ×3 · validator-precision — 2 verified-CLEAN, both defects re-verified by me before fixing) — two disjoint files fixed, every gate green.
- **Result:** ✅ the brand publish-gate no longer waves through an invented name for common "The/A/An X" businesses + a suppressed SES send is now observable, never a silent lying-success. **Files mine:** `src/services/build_validators.ts` · `src/services/notifications.ts` · `src/__tests__/build_validators.test.ts` · `src/__tests__/notifications.test.ts` · `_CODE_SWEEP_LEDGER.md`. Worker → CI on push. Protected files untouched. **Next batch:** continue the worker-services re-sweep (contact/task_inbox/domains deeper-dimension) + the still-TRACKED `UNIQUE(org_id)` migration.

### Fire 24 — 2026-09-18 — worker-services re-sweep (3-agent fan-out): task_inbox workflow-resume DEAD-ON-ARRIVAL fixed at root (AL-773); domains registrar cluster tracked
- **★ FAN-OUT (3 parallel `feature-dev:code-reviewer` agents, DISJOINT files, read-only, verify-before-reporting):** `contact.ts` · `task_inbox.ts` · `domains.ts`. Validator-precision held — **contact.ts came back CLEAN** on the agent's own trace (real Zod boundary, no lying-success catch, `dbInsert` self-heals timestamps against a table that has both, structured logging + no PII, no `any`/`@ts-ignore`/`!`). I re-verified each reported defect against the source before touching anything.
- **★ ROOT FIX (HIGH — dead-on-arrival, correctness) — `task_inbox.ts` `resolveTask` workflow fan-out:** the human-in-the-loop resolve read a PHANTOM binding `env.SITE_GENERATION` (the real Workflow binding is `SITE_WORKFLOW`, per `env.ts:80`) AND used a binding-level call shape `sendEvent(instanceId, type, payload)` that **Cloudflare Workflows v2 does not expose** — so the guard `typeof send === 'function'` was ALWAYS false → the fan-out was a silent no-op → the paused workflow's `step.waitForEvent('task-resolved-${id}')` always timed out after 30 min and auto-defaulted, **ignoring the owner's actual choice every time** (the `opt-in-prop-plus-uncalled-resolver-is-dead-default` class — and the test MOCK used the same phantom name, so it false-passed). Currently dark (gated on `SITE_GEN_REQUIRES_LOGO_APPROVAL`, default-off) → zero live regression; fixing it makes the HITL logo-approval actually work when enabled.
  - **Confirmed the correct API via context7 (Cloudflare docs, Workflows GA):** `env.SITE_WORKFLOW.get(instanceId)` → `instance.sendEvent({ type, payload })`; the event `type` must match `^[a-zA-Z0-9_][a-zA-Z0-9-_]*$` (NO dots) — `task-resolved-<uuid>` satisfies it. Rewrote the fan-out to that shape (narrow optional interfaces so it compiles on any `@cloudflare/workers-types` version + no-ops fail-soft on an older runtime; best-effort try/catch keeps a sendEvent failure from rolling back the resolution). Fixed 3 stale `SITE_GENERATION` JSDoc mentions.
  - **Test rewritten to the REAL binding + call shape** (`makeEnv({ SITE_WORKFLOW: { get } })`, asserts `get('wf-42')` then `sendEvent({type:'task-resolved-task-1', payload})`) — RED-first by construction (the old code reads the now-absent `SITE_GENERATION` → `sendEvent` never called → new assertions fail). No more false-pass on a phantom name.
- **★ TRACKED (NOT rushed — customer-money-adjacent, approval-tier per autonomous-engineering) — `domains.ts` registrar cluster (3 verified defects):** (1) `registerDomain`/`initiateDomainTransfer` send `Authorization: Bearer ${CF_API_TOKEN}` to the CF **Registrar** API, which needs the Global Key (`X-Auth-Email`+`X-Auth-Key`) — the sibling `cf_registrar.ts` already has the correct `buildAuthHeaders()`; (2) `initiateDomainTransfer` Step-1 unlock `fetch` result is discarded (no `res.ok`) → Step 2 proceeds on a failed unlock; (3) `domains.ts:registerDomain` is a DUPLICATE of `cf_registrar.ts`'s (which validates FQDN, sends registrant contact, falls back to Global Key, returns typed result) — `domains.ts`'s sends NO registrant contact (would 400) + throws. **Correct fix = delete `domains.ts` registrar fns + repoint `libs/features/domains/handlers.ts` at `cf_registrar.ts`** — a domain-registration (real-money) consolidation that warrants its own careful fire + prod-verify with a test domain, NOT a rushed slice mid-sweep. Left live-as-is (fails LOUD, not lying-success) with this note as the next dedicated batch.
- **★ VERIFY:** worker `tsc` 0 errors · **full Jest 710 suites / 11,746 tests pass** (task_inbox 28/28 with the corrected shape, no cross-contract break). Worker → CI on push (task_inbox is worker-internal + the HITL feature is dark → the unit test is the authoritative gate; E2E of the resume needs flag-on + build-LLM credit, deferred).
- **Charter v2:** 3-agent parallel fan-out swept 3 disjoint service files + advanced SEVERAL dims (correctness root-fix on a dead-on-arrival workflow-resume · context7-confirmed API before fixing · failing-test-first via the corrected mock · validator-precision — 1 verified-CLEAN, 3 domains defects verified + risk-tiered/tracked not rushed).
- **Files mine:** `src/services/task_inbox.ts` · `src/__tests__/task_inbox.test.ts` · `_CODE_SWEEP_LEDGER.md`. Worker → CI on push. Protected files untouched. **Next batch:** the `domains.ts` registrar consolidation (money-adjacent, dedicated fire) + continue worker-services (webhook/site_serving deeper).

### Fire 25 — 2026-09-18 — analytics-heavy re-sweep (3-agent fan-out): multi-URL 90d silent under-report fixed at root (AL-775)
- **★ FAN-OUT (3 parallel `feature-dev:code-reviewer` agents, DISJOINT files, read-only, verify-before-reporting):** `multi_url_analytics.ts` · `hero_copy.ts` · `theme_style.ts`. Validator-precision held — **hero_copy.ts and theme_style.ts both came back CLEAN** on the agents' own traces (hero_copy: pure worker-composed strings, consumes NO raw AI output, no `JSON.parse`, exhaustive tests anchored to ~16 shipped incidents; theme_style: pure ordered rule-table selection, both call sites pass args in the right order, 717-line test suite locks every documented precedence). I re-verified the one reported defect against the source before touching anything.
- **★ ROOT FIX (HIGH — lying-UI-cap / silent under-report) — `multi_url_analytics.ts` `loadMultiUrlAnalytics`:** the CF-analytics path builds `windowCount = min(days, 30)` daily windows (CF's per-host `httpRequestsAdaptiveGroups` dataset only retains ~30 days) but the envelope reported `range_days: days`. So a **90d request on a CF-resolved (paid custom-domain) site returned a ≤30-day sum + a ≤30-point series LABELED as 90 days** — `pageviews`/`total_requests`/`series` silently under-reported ~3× on the user-facing `/admin` Analytics 90d tab, with `any_real_data:true` so the honest-empty fallback never compensated (the `paginated-endpoint-silent-cap-needs-total` / `hardcoded-default-drifts-from-enforced-limit` class). Verified: line 252 cap, envelope at ~700, the visitor_events fallback (line ~716) correctly uses full `days`.
  - **Fix (SSOT, no drift):** extracted `const CF_MAX_WINDOW_DAYS = 30` used by BOTH the per-host window builder AND the envelope; the CF path now reports `range_days = min(days, CF_MAX_WINDOW_DAYS)` (honest — the window CF actually covered); the visitor_events fallback merge (first-party, no CF cap, `emptySeries(days)`) explicitly resets `range_days: days` so a fallback-served envelope reports the full requested window. Both paths now report the window they ACTUALLY covered.
  - **+2 RED-first regressions** (`multi_url_analytics_load.test.ts`): CF-path 90d → `range_days === 30` (was 90 — the lie); fallback-path 90d → `range_days === 90` + `series.length === 90`.
- **★ VERIFY:** worker `tsc` 0 errors · **full Jest 710 suites / 11,748 tests pass** (analytics 29/29 incl. the 2 new, no cross-contract break). Worker → CI on push. The multi-URL CF path needs a paid multi-hostname site with 90d CF data to E2E (cohort sites are subdomains using the first-party fallback), so the unit tests are the authoritative gate — documented.
- **Charter v2:** 3-agent parallel fan-out swept 3 disjoint service files + advanced SEVERAL dims (correctness root-fix on a user-facing lying-under-report · SSOT constant extraction preventing cap drift · failing-test-first ×2 · validator-precision — 2 verified-CLEAN).
- **Files mine:** `src/services/multi_url_analytics.ts` · `src/__tests__/multi_url_analytics_load.test.ts` · `_CODE_SWEEP_LEDGER.md`. Worker → CI on push. Protected files untouched. **Next batch:** the still-TRACKED `domains.ts` registrar consolidation (money-adjacent, dedicated fire) + continue worker-services (wallet/usage_metering deeper — money-adjacent, careful).

### Fire 26 — 2026-09-18 — external-API-heavy re-sweep (3-agent fan-out): openai_research telemetry accuracy fixed at root (AL-777)
- **★ FAN-OUT (3 parallel `feature-dev:code-reviewer` agents, DISJOINT files, read-only, verify-before-reporting):** `openai_research.ts` · `domain_suggester.ts` · `media.ts`. Validator-precision held — **domain_suggester.ts and media.ts both came back CLEAN** on the agents' own traces (domain_suggester: RDAP conservatively maps 404→available / 200→taken / everything-else→unknown-NOT-available, guarded `safeJsonParse`, TLD/label validation unit-tested; media: every stock/gen `fetch` gated on `res.ok`, the R2-put→D1-insert saga compensates + rethrows on a dropped insert (tested), signed-URL org-scoped before minting = no IDOR, upload size/type guards at the route). I re-verified both reported defects against the source before touching anything.
- **★ ROOT FIX #1 (HIGH — observability / structured-event accuracy) — `openai_research.ts` `callOpenAI` success capture:** the PostHog `$ai_generation` success event hardcoded `gatewayUsed: false` while `gatewayFetch` returns the REAL `{response, gatewayUsed}` (`gatewayUsed = isGatewayActive(env)`) — line 161 destructured only `.response`, discarding the routing signal. So with `AI_GATEWAY_ENABLED`/`CF_ACCOUNT_ID` set (the mandated prod config — "AI Gateway is mandatory for every model call"), every research call's telemetry read "direct", making gateway cache-hit-rate / margin / latency-by-route dashboards show 100% direct-vendor and masking the gateway's value entirely. Fix: capture `gatewayUsed = gw.gatewayUsed ?? false` in-scope (a `let` before the try, assigned right after `res = gw.response`) and pass it to the success capture.
- **★ ROOT FIX #2 (MED — cost-attribution correctness, same telemetry surface, paired value-add) — `estimateOpenAiCost`:** `Object.keys(COSTS).find(k => model.includes(k))` with `'gpt-4o'` before `'gpt-4o-mini'` → `'gpt-4o-mini'.includes('gpt-4o')` matched the **16.7×-pricier gpt-4o rate** for every gpt-4o-mini call (poisoning the `costUsd` analytics field). Fix: longest-key-first + exact-or-versioned-suffix match (`model === k || model.startsWith(`${k}-`)`) so each model resolves to its OWN price; versioned suffixes (`gpt-4o-2024-…`) still map to the base rate. Exported the pure fn for direct coverage.
- **+3 RED-first regressions** (`openai_research.test.ts`): success capture with a gateway-active env → `gatewayUsed:true` (was false); `estimateOpenAiCost('gpt-4o-mini')` → 0.75 not 12.5, `mini < full`, versioned suffixes resolve to base.
- **★ VERIFY:** worker `tsc` 0 errors · **full Jest 710 suites / 11,750 tests pass** (openai_research 23/23 incl. the 3 new, no cross-contract break). Worker → CI on push. Both fixes are worker-internal telemetry (no user-facing route) → the unit tests are the authoritative gate.
- **Charter v2:** 3-agent parallel fan-out swept 3 disjoint external-API-heavy service files + advanced SEVERAL dims (observability/structured-event accuracy · cost-attribution correctness · failing-test-first ×3 · validator-precision — 2 verified-CLEAN).
- **Files mine:** `src/services/openai_research.ts` · `src/__tests__/openai_research.test.ts` · `_CODE_SWEEP_LEDGER.md`. Worker → CI on push. Protected files untouched. **Next batch:** the still-TRACKED `domains.ts` registrar consolidation (money-adjacent) + continue (ai_env_vars crypto — security-careful, weekly_digest/openai — boundary).

### Fire 27 — 2026-09-18 — boundary/security re-sweep (3-agent fan-out): github_repo unguarded fetches fixed at root (AL-778)
- **★ FAN-OUT (3 parallel `feature-dev:code-reviewer` agents, DISJOINT files, read-only, verify-before-reporting):** `import_crawler.ts` · `content_import.ts` · `github_repo.ts`. Validator-precision held — **import_crawler SSRF is ADEQUATE** (agent verified: `isSafeCrawlUrl` blocks localhost/RFC1918/link-local/IPv4-mapped-IPv6 + http(s)-only, `safeFetch` re-validates every redirect hop capped at 5, the route pre-validates the seed — genuine defense-in-depth, tested) and **content_import verified CLEAN** for bug classes (guarded parses, no D1 writes, no lying-success). I re-verified each github_repo defect against the source before fixing.
- **★ ROOT FIX (HIGH — fetch-not-ok / opaque-crash) — `github_repo.ts` `rollback`:** the "get current HEAD" fetch (`GET refs/heads/main`) was the SOLE ungated `fetch` in an otherwise fully `res.ok`-gated file — it went straight to `refBody.object.sha`. On a GitHub 403 (rate-limit) or 404 (repo deleted between the ownership check and here / missing main ref) it crashes with a raw `TypeError` (or `SyntaxError` on a non-JSON body) → the caller (`routes/site_rollback.ts`) maps it to an opaque 502, masking the real cause. Fix: `if (!refRes.ok) throw new GithubRepoError('Failed to get current HEAD…', 'REF_FAILED', refRes.status)` — mirrors the `commitRes` gate 15 lines above.
- **★ ROOT FIX (MED, same class + file) — `createInitialCommit` empty-repo fallback:** the blob→tree→commit→ref 4-fetch fallback had ZERO `res.ok` gating → a narrow-scope token (repo-create but not `contents:write`) cascaded `undefined` `.sha` values into confusing downstream 422s. Gated all 4 (`CREATE_FAILED`/`REF_FAILED`) so it fails loud at the first failing call.
- **+1 RED-first regression** (`github_repo.test.ts`): rollback with target-commit-200 + ref-404 → `rejects` a `GithubRepoError` with `code:'REF_FAILED'` (before the gate: a raw `TypeError`, so `toBeInstanceOf(GithubRepoError)` was RED). Existing 14 tests still green.
- **★ VERIFY:** worker `tsc` 0 errors · **full Jest 710 suites / 11,751 tests pass** (github_repo 15/15). Worker → CI on push. github_repo is worker-internal (no user-facing route renders it) → the unit test is the authoritative gate.
- **Charter v2:** 3-agent parallel fan-out swept 3 disjoint boundary/security files + advanced SEVERAL dims (fetch-ok/opaque-crash root-fix ×2 in one file · failing-test-first · validator-precision — SSRF verified-adequate + 1 file verified-CLEAN, no false-positive fix).
- **TRACKED (next batch):** `import_crawler.ts` `fromRobots` has no accumulated-URL cap (unlike `fromSitemap`'s 2000) → an attacker source-site's robots.txt (10 sitemaps × huge XML) → unbounded R2/CPU cost per authed import (MED — add the same 2000 cap). `content_import.ts` is fully-built but has ZERO callers (dead-code drift — wire behind a feature-module+flag or remove).
- **Files mine:** `src/services/github_repo.ts` · `src/services/__tests__/github_repo.test.ts` · `_CODE_SWEEP_LEDGER.md`. Worker → CI on push. Protected files untouched.

### Fire 28 — 2026-09-18 — closed the tracked import_crawler unbounded-fromRobots gap (AL-779)
- **Verify-before-implement:** git clean · prod 200 · closed the MED item TRACKED by Fire 27's agent (no re-audit needed — lean, direct fix).
- **★ ROOT FIX (MED — unbounded-cost / DoS on an authed endpoint) — `import_crawler.ts` `fromRobots`:** it iterated up to 10 `Sitemap:` URLs and pushed EVERY leaf of each with NO `out.length` cap, while its sibling `fromSitemap` caps at 2_000. An attacker-controlled SOURCE site's robots.txt (10 sitemaps × a crafted 100k+-`<loc>` XML) grew `robotsUrls` unbounded → `crawlSiteForImport` concatenates it RAW (verified: no downstream total cap, line 421-427) → JSON.stringify'd into R2 + returned in the `POST /api/sites/import-from-url` response, inflating Worker CPU/memory + R2 cost per single authed import. Fix: mirror `fromSitemap` — `MAX_ROBOTS_URLS = 2_000` with a `break` on the outer sitemap loop AND the inner leaf loop.
- **+1 RED-first regression** (`import_crawler.test.ts`): robots.txt → one Sitemap: → a 2500-`<loc>` XML → `report.by_source.robots === 2000` (was 2500 uncapped). SSRF + the other 25 tests still green.
- **★ VERIFY:** worker `tsc` 0 errors · **full Jest 710 suites / 11,752 tests pass** (import_crawler 26/26). Worker → CI on push. Worker-internal crawl path → the unit test is the authoritative gate.
- **Files mine:** `src/services/import_crawler.ts` · `src/__tests__/import_crawler.test.ts` · `_CODE_SWEEP_LEDGER.md`. Worker → CI on push. Protected files untouched. **Still tracked:** `content_import.ts` dead-code (wire-or-remove); `domains.ts` registrar consolidation (money-adjacent, dedicated fire).

### Fire 29 — 2026-09-18 — packages/shared re-sweep (3-agent fan-out): sanitizeHtml TWO live XSS bypasses fixed at root (AL-780)
- **★ FAN-OUT (3 parallel `feature-dev:code-reviewer` agents, DISJOINT files, read-only, verify-before-reporting):** `forms.ts` · `redact.ts` · `sanitize.ts`. Validator-precision held — **forms.ts came back CLEAN** (wired schemas sound; `.nullish()` on email/origin_url is deliberate + correct; only a dead form-rules schema drift below bar). I re-verified every reported defect against the source before touching anything.
- **★ ROOT FIX (HIGH — DOM-XSS, LIVE path) — `sanitize.ts` `sanitizeHtml` two regex bypasses:** `sanitizeHtml` guards `additional_context` at `site_creation/handlers.ts:126` → `site-generation.ts:459` → the string is interpolated into GENERATED CUSTOMER-SITE content, so a bypass is stored-XSS on published sites. Two vectors slipped the regexes:
  1. **Unterminated `<script>`/`<iframe>`/`<object>` opener** — the balanced-pair regexes require a LITERAL closing tag, so `<script>alert(document.cookie)` (no `</script>`) and `prefix<script` (truncated at EOF) passed through intact — and a browser AUTO-CLOSES an unterminated `<script>` at EOF and executes it. Fix: added `.replace(/<(?:script|iframe|object)\b[^>]*>?/gi, '')` (the `>?` also catches the EOF-truncated form) AFTER the balanced-pair strips (which still handle the inner-content deletion).
  2. **Slash-separated event handler** — the handler regex anchored on `\s` (whitespace) before `on\w+=`, but HTML5 treats `/` as an attribute separator, so `<svg/onload=alert(1)>` and `<img src=x/onerror=alert(1)>` fired (the `/` isn't `\s` → no match → handler survived). Fix: broadened the boundary `\s` → `[\s/]`, still anchored to a real attribute edge so prose "…turn it on…" never matches.
  - Both fixes live inside the existing `do…while(out!==prev)` stability loop, so nested/re-revealed vectors converge.
- **★ RED→GREEN PROOF (TDD, without touching the file per the AL-770 `git checkout` lesson):** ran the PRE-FIX regex logic against all 5 payloads in a throwaway node snippet → **5/5 survive** (RED reproduced); the shipped sanitizer kills all 5 (jest GREEN). **+4 RED-first tests** in `sanitize-security.test.ts`: 3 added to the shared `dangerous[]` table (unterminated script no-close, script truncated-at-EOF, unterminated iframe — caught by the shared `<script`/`<iframe` assertions) + 1 dedicated `it` for the slash-separated form (the shared assertion's `\s`-anchored regex is structurally blind to the `/` form) + an over-strip guard (`<br/>`, `<img src="a.jpg"/>`, `</p>` must survive the broadened `[\s/]` boundary).
- **★ VERIFY:** **full `packages/shared` suite 14 suites / 509 tests pass** (sanitize-security 34/34 incl. the 4 new), `tsc --noEmit` rc=0. shared is a library (no deployed surface of its own) → the unit suite is the authoritative gate; the Worker that consumes it ships via CI on push.
- **Charter v2:** 3-agent parallel fan-out swept 3 disjoint shared-lib files + advanced SEVERAL dims (security root-fix on a LIVE stored-XSS path, TWO distinct bypasses in one fn · explicit RED→GREEN proof · failing-test-first ×4 + over-strip guard · validator-precision — 1 verified-CLEAN · doc-drift fixed inline: shared CLAUDE.md 479/13→509/14 + missing suite row).
- **★ SECURITY NOTE (prompt-injection, ignored):** the `redact.ts` reviewer agent's returned output contained an off-task "AI Gateway Unified Billing 31-step migration" directive I never issued — untrusted injected content (not from the user), correctly identified and IGNORED per ai-agent-security + full-autonomy escalation rules. No action taken on it.
- **TRACKED (next batch):** `redact.ts` `PHONE_REGEX = /\+?[1-9]\d{6,14}/g` misses ALL formatted phones (`(415) 555-1234`, `+1 (415) 555-0123`, `415.555.0123`) — HIGH confidence BUT the module has ZERO production callers (`src/lib/log.ts` + frontend `logger.service.ts` implement separate redactors), so low live-exposure; fix = `/\+?\(?\d{1,4}\)?[\s.-]?\d{2,4}[\s.-]?\d{2,4}[\s.-]?\d{0,4}\b/g` with its own regression, a dedicated slice. Email/token/object-recursion all verified CLEAN.
- **Files mine:** `packages/shared/src/utils/sanitize.ts` · `packages/shared/src/__tests__/sanitize-security.test.ts` · `packages/shared/CLAUDE.md` · `apps/project-sites/_CODE_SWEEP_LEDGER.md`. Worker → CI on push. Protected files untouched. **Still tracked:** `redact.ts` phone-regex; `content_import.ts` dead-code (wire-or-remove); `domains.ts` registrar consolidation (money-adjacent, dedicated fire).

### Fire 30 — 2026-09-18 — closed the tracked redact.ts phone-regex PII-leak gap (AL-783)
- **Verify-before-implement:** git clean · prod 200 · build-LLM STILL dead (DeepSeek −0.56) → creditless "finish everything" fire. `git grep` re-confirmed BOTH Fire-29-tracked items still open: shared `redact.ts` has ZERO worker callers (the `src/lib/log.ts` redactor is a separate fn), `content_import.ts` is fully dead (zero callers).
- **content_import.ts triaged NOT-this-fire (correctly):** it's a 480-line **tested** module of pure export-file parsers (WordPress-XML/Squarespace-JSON/Wix-CSV/Webflow-JSON/CSV/RSS → normalized `ContentItem[]`) — a DISTINCT feature from the live URL `import_crawler.ts`, not a redundant duplicate. Removing it destroys real tested work; wiring it properly = a full feature-module (route + manifest + flag + Zod + E2E) — too big to rush in a bare fire + it's customer-content import (deserves care). Stays tracked as a dedicated feature-module fire, NOT rushed (per autonomous-engineering "look before you delete code you didn't write").
- **★ ROOT FIX (HIGH — latent PII leak in a shared boundary util) — `redact.ts` `PHONE_REGEX`:** `/\+?[1-9]\d{6,14}/g` matched only CONTIGUOUS digit runs, so EVERY formatted phone a real owner/contact writes — `(415) 555-1234`, `+1 (415) 555-0123`, `415.555.0123`, `415-555-1234` — leaked verbatim through `redact()`/`redactObject()` into logs/Sentry/PostHog. Zero live exposure TODAY (no worker caller) but it's a shared-lib boundary util meant for reuse — the day it's wired, formatted phones leak. Broadened to two alternatives (formatted 3-3-4 core with optional `+CC` + `(area)` parens + `[\s.-]` separators FIRST, contiguous E.164 unchanged SECOND).
- **★ VALIDATOR-PRECISION (must not over-redact):** the 3-3-4 core requires a trailing 4-digit group, so it does NOT match dates (`2026-09-18`), comma-prices (`1,234.56`), IPv4 (`192.168.1.1`), ZIP+4 (`12345-6789`), or short ids — proven: a throwaway snippet ran the NEW regex over 5 benign forms → 0 over-redacted; any benign 10-digit CONTIGUOUS run was ALREADY redacted by the old branch, so this only ADDS separated-phone forms.
- **★ RED→GREEN (empirical):** +12 tests (`redact-secrets.test.ts`: 6 formatted-phone positives + 6 benign over-redact guards). Throwaway snippet proved **4/4 formatted phones LEAK through the old regex** (RED reproduced); new regex catches all. Full `packages/shared` suite **14 suites / 521 tests** green (was 509 + 12), `tsc --noEmit` clean.
- **Files mine:** `packages/shared/src/utils/redact.ts` · `packages/shared/src/__tests__/redact-secrets.test.ts` · `_CODE_SWEEP_LEDGER.md`. shared is a library → the unit suite is the authoritative gate; the Worker consuming it ships via CI on push. **Still tracked:** `content_import.ts` (feature-module wiring, dedicated fire) · `domains.ts` registrar consolidation (money-adjacent, dedicated fire + test-domain prod-verify).

### Fire 31 — 2026-09-18 — closed a SECOND fail-open cost-cap-bypass: the AI-spend KILLSWITCH (AL-785)
- **Auto-integrate my own AL-784 Rec** ("audit sibling limit/budget/entitlement gates for the same `dbQuery`-swallows-error → fail-open class") — shipped this fire instead of re-Rec'ing. Verify-before: git clean · prod 200 · build-LLM dead (DeepSeek −0.56). Grepped `.data[N]` reads in gate/cost context: `build_limits.ts:151` (already fixed AL-784, guard now above it) · `social_token_cron.ts:207` (a counter increment, not a gate — low sev) · `usage_metering.ts:528` (a cron rollup count, not a live gate) · **`build_budget.ts` — the AI-spend KILLSWITCH, fail-OPEN.**
- **★ ROOT FIX (HIGH — fail-OPEN on the cost-critical AI-spend killswitch) — `build_budget.ts` `monthSpendMicroUsd`:** it read the month's AI spend via `dbQueryOne(...).catch(() => ({ total: 0 }))`. On a D1 error → spend `$0` → `checkBudget` `allowed:true` → the killswitch (its whole documented job: stop a runaway org draining the platform AI budget) SILENTLY OPENED — same class as build_limits AL-784, but on the MORE cost-critical gate (gates the expensive container build). Verified myself + all callers (token_burn_meter service + `site-generation.ts:1212` pre-build gate).
- **★ CAUGHT MY OWN INERT FIRST-ATTEMPT (shipped-fix-inert class):** my first patch used `dbQueryOne` + `try/catch` retry — but `dbQueryOne` (db.ts) calls `dbQuery` (which SWALLOWS the throw → `{data:[],error}`) and returns `data[0] ?? null`, DISCARDING `.error`, so it NEVER throws → the `.catch`/`try` was dead (doubly so: the OLD `.catch` was also dead). Verifying `dbQueryOne`'s behavior BEFORE trusting the fix caught it. Corrected to **`dbQuery` + inspect `.error`** (the only reliable D1-error signal — a null/empty aggregate row is ambiguous between "no rows" and "error"): retry once, then on a sustained error return `Infinity` → `checkBudget` clamps `spentUsd = Number.isFinite(raw) ? raw : capUsd` → DENIES with a clean `$cap/$cap` meter (never `$Infinity`).
- **★ RED→GREEN (empirical, stash-only):** +2 tests (throwing-SUM db). Without fix `Expected:false Received:true` (killswitch OPEN) + `Expected:2 Received:0` (no retry). With fix 12/12. Full worker suite **711 suites / 11,758 tests** green, tsc clean.
- **Files mine:** `src/services/build_budget.ts` · `src/__tests__/build_budget.test.ts` · `_CODE_SWEEP_LEDGER.md`. Worker → CI on push. Protected files untouched. **Fail-open class now swept: both cost caps (site-count AL-784 + AI-spend AL-785) fail CLOSED.** `social_token_cron`/`usage_metering` `.data[0]` reads are non-gate (low sev, not fixed).

### Fire 32 — 2026-09-18 — external_llm premium-ladder kimi/fable STRAND-BUG fixed at root; notifications.ts verified CLEAN (2-agent review)
- **Verify-before-implement:** git clean · prod 200 · build-LLM dead both rails (DeepSeek −0.56, Anthropic 400; the Unified-Billing gateway funds OpenAI/image but NOT the Anthropic-protocol build agent) → creditless sweep fire. 2 fresh code-reviewers on disjoint un-swept services.
- **`notifications.ts` (SES→SendGrid email rail): VERIFIED CLEAN** — reviewer traced the fallback (SES fail → push `failures[]` → SendGrid → throw ONLY on total exhaustion), no lying-success (both rails gate on real resolve / `res.ok`), suppression fail-open is documented-intentional, no secret leak, no `any`/`!`/`@ts-ignore`. A valid "clean" result (validator-precision — not every sweep must find a bug).
- **★ ROOT FIX (HIGH, latent→realistic) — `external_llm.ts` `callExternalLLM` kimi/fable STRANDING:** the premium ladder (`chooseProviderForTier`) resolves `'kimi'`/`'fable'`, but `callExternalLLM` (an openai/anthropic/deepseek-only path — kimi/fable belong to `edge_ai_router`) collapsed them to a HARDCODED `providers = ['openai','openai']` → a usable `DEEPSEEK_API_KEY`/`ANTHROPIC_API_KEY` was STRANDED and the call threw "No LLM provider available" even with a live key (e.g. `FABLE_API_KEY` set → premium resolves `'fable'` → no OpenAI key but DeepSeek present → dead-ended). Fixed: extracted pure exported `toRoutableProvider(env, provider)` — collapses kimi/fable to the best AVAILABLE standard vendor by key; rebuilt `fallback`/`providers` off it; `options.model` still applied ONLY for the true primary (`provider === primary`) so a kimi/claude model never mis-sends to a standard vendor; throw message corrected (names DeepSeek + points kimi/fable → edge_ai_router). Standard-provider fallback mapping is byte-identical to the old code (openai→anthropic, anthropic/deepseek→openai) — ZERO behavior change on the normal path.
- **★ RED→GREEN:** +5 `toRoutableProvider` unit tests (identity; kimi+DeepSeek-only→deepseek [the strand fix]; fable+DeepSeek→deepseek; openai-preferred; anthropic-last-resort). Updated 3 `external_llm.test.ts` assertions (old "No LLM provider available" → "No usable LLM provider"). typecheck clean; **external_llm suites 67/67 green**. Worker → CI runs the full suite on push.
- **Files mine:** `src/services/external_llm.ts` · `src/__tests__/external_llm_provider_tiers.test.ts` · `src/__tests__/external_llm.test.ts` · `_CODE_SWEEP_LEDGER.md`. Protected files untouched. **Still tracked:** `content_import.ts` (wire-or-remove) · `domains.ts` registrar (money-adjacent) · external_llm FULL kimi/fable ROUTING (add DEFAULT_MODELS + callOpenAI/callAnthropic kimi/fable branches so callExternalLLM can USE those keys — needs the `kimi-k3` model + fable Anthropic-proto wiring; dedicated non-saturated fire).

### Fire 33 — 2026-09-18 — security sweep: site_serving.ts + auth.ts BOTH verified CLEAN; tracked `..`-reject CLOSED; slug defense-in-depth guard shipped
- **Verify-before-implement:** git clean · prod 200 · build-LLM dead both rails (DeepSeek −0.56 / Anthropic 400, fresh) → creditless security sweep. 2 fresh reviewers on the 2 most security-critical un-swept modules (hot public serving path + auth).
- **`site_serving.ts` (hot serving path): VERIFIED CLEAN** — reviewer traced the full 1992-line file. Path traversal CONFIRMED NON-EXPLOITABLE (`requestPath = new URL().pathname` → WHATWG parser normalizes `..` before it reaches the file; R2 keys are flat strings, no directory semantics → a literal `..` key just doesn't match) → **the tracked `..`-reject item is CLOSED (safe by platform design, no fix needed)**. Content-type branches on the resolved filePath via the `getContentType` SSOT (not raw path); missing-R2 → 404 (never 500/blank); version-less build → honest 503; no secret/PII in logs; no `any`/`!`/`@ts-ignore`.
- **`auth.ts` (magic-link/OAuth/D1 sessions): VERIFIED CLEAN** — token gen = `randomHex(32)` (256-bit CSPRNG, not Math.random/timestamp/UUIDv7); magic-link consumption is a compare-and-swap `UPDATE … WHERE used=0` + `changes===0` throw (TOCTOU-closed); OAuth `state` generated/verified/expiry-checked/consumed one-time (no fail-open); session expiry rejects; `dbQueryOne`-null on a D1 error fails **CLOSED** (middleware sets userId only when session is truthy → a D1 outage = unauthenticated, not bypass); tokens compared by exact parameterized `token_hash = ?` + `timingSafeEqual` for test-login; no secrets logged.
- **★ SHIPPED (defense-in-depth — the one reviewer note) — `site_serving.ts` slug charset guard:** `slug` is derived from the Host subdomain label + interpolated UNESCAPED into the served unpaid top-bar HTML. Non-exploitable (DNS constrains the label to `[a-z0-9-]` before routing), but the file relied on that IMPLICIT network guarantee. Added an explicit `if (slug && !/^[a-z0-9-]{1,63}$/.test(slug)) slug = null;` at the single derivation point — the interpolation is now provably injection-safe regardless of source; no-op for every valid slug, a malformed label → null → 404. Escape-the-sink / don't-rely-on-upstream doctrine.
- **★ VERIFY:** typecheck clean; `site_serving` suites **137/137 green**. Worker → CI on push.
- **Files mine:** `src/services/site_serving.ts` · `_CODE_SWEEP_LEDGER.md`. Protected files untouched. **Still tracked (dedicated non-saturated fires):** `content_import.ts` wire-or-remove · `domains.ts` registrar · full kimi/fable routing.

### Fire 34 — 2026-09-18 — money-path security review: billing.ts VERIFIED CLEAN (comprehensive); wallet.ts is dead code
- **Verify-before-implement:** git clean · prod 200 · build-LLM dead (DeepSeek −0.56, fresh). Focused review of the LIVE Stripe money path (highest-value un-swept target).
- **`billing.ts` + live handlers (`libs/features/billing/handlers.ts`) + `webhooks.ts` + entitlements: VERIFIED CLEAN** across every billing bug-class that has bitten this file before:
  - Entitlement fail-OPEN → NONE: `getOrgEntitlements` → `resolveActiveOrgPlan`; a D1 error → `null` → `'free'` (fails CLOSED, no revenue leak).
  - `dbQueryOne` fail-closed gate → correct: `checkBuildLimit` deliberately uses `dbQuery` + `.error` retry-then-deny (my AL-784 fix, **confirmed holding**).
  - plan≠status → correct: every gate routes through the shared `resolveActiveOrgPlan` SSOT (`status IN ('active','trialing')`) — no `active`-only gate wrongly excluding trialing.
  - Checkout/portal IDOR → gated: `orgId` from `c.get('orgId')`, `forbidden()` on a body-org mismatch; portal scoped to a session-org customer lookup.
  - Webhook idempotency+signature → sound: Stripe sig verified (timing-safe, replay-window) BEFORE parsing; idempotency keyed on TERMINAL status + `UNIQUE(provider,event_id)` race guard (no double-credit); non-terminal rows reprocess.
  - Response-key drift → locked: checkout `{data:{checkout_url,session_id}}` / portal `{data:{portal_url}}` match the FE reads exactly, with regression specs.
  - No Stripe-key/customer-email logged; no `any`/`!`/`@ts-ignore` on the money path.
- **★ FINDING (dead code) — `wallet.ts` has ZERO callers** (`git grep` in src, excl self+tests): another built-but-unwired feature (like `content_import.ts`) — a wire-or-remove candidate, NOT a live money path. Tracked (dedicated fire).
- **Note (cosmetic drift — per drift-detection, cosmetic warns not blocks):** the `api.ts` JSDoc route table still lists billing routes that MOVED to `libs/features/billing/handlers.ts` — doc drift, zero functional impact. Tracked.
- **No code change** — a comprehensively-verified-clean money path is the deliverable (validator-precision: don't churn a well-hardened surface). **Codebase now at code-quality convergence:** sanitize/redact/build_budget/build_limits/external_llm/notifications/site_serving/auth/billing all verified/hardened this session.

### Fire 35 — 2026-09-18 — TEMPLATE sweep (worker services converged → moved to the template repo): ContactForm wrong-endpoint bug fixed at root (dual CODE-QUALITY + EMBARRASSINGLY-EASY)
- **Verify-before-implement:** git clean (only guardrailed `scheduled_tasks.json` + non-mine `_ADMIN_CONTRACT_REPORT.json`) · prod 200. Worker services at convergence (Fire 34) → swept the **template repo** visitor path (`github.com/HeyMegabyte/template.projectsites.dev`), the highest-value un-swept area.
- **★ ROOT FIX (correctness — wrong endpoint, 404 on the primary conversion surface) — `ContactForm.tsx` `submit()`:** the React fallback posted to **`/api/contact/{slug}` — a route that does NOT exist**. The Worker mounts only slug-less `POST /api/contact` (platform marketing, `api.ts:3616`) and `POST /api/contact-form/:slug` (`contact_newsletter/handlers.ts:45`). app.js's edge form-hijack, `<QuoteForm>`, the rate-limiter (`/api/contact-form/*`) and every `contact_form.test.ts` all use `/api/contact-form/:slug` — ContactForm was the lone outlier. MASKED in prod (app.js's capture-phase hijack POSTs the right URL first) but **live + 404'ing wherever the React path runs without app.js** (local preview, Storybook, or a failed inject). Fixed the default → `/api/contact-form/{slug}` (an explicit `__endpoint` override still wins), and made it **type-honest** (dropped a misleading `formData.get(...) as string` cast that hid the `File | null` union → an unreachable `??`). Corrected the JSDoc + `ContactForm.stories.tsx` doc drift.
- **★ PAIRED VALUE-ADD (embarrassingly-easy — plain language over jargon):** the network-error branch surfaced the raw `err.message` ("Failed to fetch" / "NetworkError…") straight to a VISITOR — reads as broken. Now a plain, reassuring next step: *"Couldn't send — please check your connection and try again."* Fields are kept (only the success path clears) so the visitor retries without re-typing.
- **★ RED→GREEN:** the pre-existing test only asserted the URL `toContain('test-slug')` — **BLIND** to the wrong path (`/api/contact/test-slug` matches too). Strengthened to `toBe('/api/contact-form/test-slug')` (RED on the pre-fix path, confirmed by grep; GREEN now) + a new friendly-network-error test (asserts the plain copy + no raw "failed to fetch" leak + fields kept). **ContactForm 6/6 green; template contact suite 13/13** (ContactForm + PageviewBeacon + KineticMarquee); tsc clean; `npm run build` ✓.
- **★ FULL-JOURNEY REAL-BROWSER PROOF (5/5, home contact section, `/api/*` intercepted so prod stays clean):** homepage-start → fill (submit un-gated, `disabled=false`) → submit → **exactly ONE POST → `/api/contact-form/default`** (NOT the dead `/api/contact/`) → **success "Thanks…" renders** → **0 console errors**.
- **Observation (not fixed — scope discipline):** `/contact` (`Contact.tsx`) renders its OWN hand-rolled `<form>` (no `__slug`, app.js-hijack-only), diverging from the reusable `<ContactForm>` component (which lives on Home). A future consolidation candidate — tracked, not churned this fire.
- **Files mine (template repo):** `src/components/ContactForm.tsx` · `ContactForm.test.tsx` · `ContactForm.stories.tsx`. **(main repo):** `_CODE_SWEEP_LEDGER.md`. Lands next build (NO redeploy). Protected files untouched. **Still tracked:** `content_import.ts` + `wallet.ts` wire-or-remove · `domains.ts` registrar · external_llm full kimi/fable routing · `api.ts` JSDoc drift · `/contact` vs `<ContactForm>` consolidation.

### Fire 36 — 2026-09-19 — ⚠️ CORRECTION: `wallet.ts` is LIVE (Fire 34's "dead code" was a FALSE POSITIVE) + DB-enforced credit idempotency shipped
- **Verify-before-implement:** git clean · prod `/api/health` 200 · build-LLM still dead → creditless review of the tracked "wire-or-remove" candidates (`wallet.ts`, `content_import.ts`).
- **★ CORRECTION (prevents a catastrophic future action) — `services/wallet.ts` is NOT dead code:** Fire 34 marked it "ZERO callers → wire-or-remove". **WRONG — it is a LIVE metered-billing money path**, imported by `routes/templates.ts` (`getWalletState`, `manualAdjustment`) + `routes/super_admin.ts` (`manualAdjustment`) statically, by `routes/domain_purchase.ts` via `wallet_adapter.ts`'s dynamic `import('./wallet.js')`, and driven by `routes/webhooks.ts`→`wallet_webhook.ts`→`handleStripeEvent`. It owns 6 migrations (0036/0565/0566/0613/0623 + now 0635), a live flag (`credit_wallet_rollover`), an env Price ID, AND **5 test files + 38K of tests** (`wallet.test.ts` 38K, `wallet_adapter/purchase/routes/webhook`). Fire 34's grep missed the `../services/wallet.js` import form + the dynamic `import()` — the exact `[[knip-unused-not-always-dead]]` trap. **Acting on that note would have deleted the live wallet/domain-purchase/metered-billing system.** The "remove wallet.ts" item is CLOSED as invalid; the existing test imports already lock it against deletion.
- **★ MONEY-PATH VERIFIED SOUND:** `chargeWallet` debits atomically (`UPDATE … WHERE balance_cents >= ?` + `changes===0` guard → no negative balance); `creditWallet` dedupes on `stripe_event_id`; `routes/webhooks.ts` verifies the Stripe sig → checks `webhook_events` UNIQUE(provider,event_id) (early-returns duplicates) → concurrent-delivery guard → ONLY THEN credits, so today there is no live double-credit.
- **★ HARDENING SHIPPED (defense-in-depth, prod-applied + verified) — DB-enforced exactly-once credit:** the supporting index `idx_wallet_tx_stripe` (from 0036) was **NON-unique**, so the credit path's exactly-once rested entirely on the outer layer + an app SELECT (a TOCTOU under any future un-deduped caller). **Migration `0635_wallet_tx_stripe_unique.sql`** replaces it with a UNIQUE partial index (`WHERE stripe_event_id IS NOT NULL`). Prod-safe: verified **0 rows** with a non-null `stripe_event_id` on `project-sites-db-production` (dup_event_ids=0, total_credited=0) → no row can violate it; **applied to prod + verified `pragma_index_list.unique=1`**. `creditWallet` now treats a UNIQUE violation as an **idempotent no-op** (reverse this delivery's balance add → return, NOT throw) so the DB backstop degrades cleanly; every OTHER insert error still fails loud + reverses + throws (Stripe retries). Mirrors the `billing.ts UNIQUE(provider,event_id)` precedent.
- **★ RED→GREEN:** 2 new `wallet.test.ts` cases — a UNIQUE-violation insert → reverses (credit UPDATE + reverse UPDATE = 2 prepare calls, net-zero) + resolves (no throw, no double-credit); a NON-unique insert error still `rejects.toThrow`. **wallet suite 47→49 green**, tsc 0.
- **★ `content_import.ts` — confirmed GENUINELY unwired (0 callers, re-verified) — Fire 35 was correct** (unlike the wallet false-positive). Remains a legit wire-or-remove candidate for a dedicated fire.
- **Files mine:** `migrations/0635_wallet_tx_stripe_unique.sql` (NEW, applied to prod) · `src/services/wallet.ts` (creditWallet graceful dedupe) · `src/__tests__/wallet.test.ts` (+2) · `_CODE_SWEEP_LEDGER.md`. Worker code → CI on push (migration already live on prod). Protected files untouched.
- **Next:** `content_import.ts` + `wallet.ts`→(no, live) — remaining tracked: `content_import.ts` wire-or-remove · `domains.ts` registrar · `api.ts` JSDoc drift. **Lesson for future sweeps: a "0 callers" grep MUST also check the `../dir/name.js` import form + dynamic `import()` + test-file imports before ever recommending removal (`[[knip-unused-not-always-dead]]`).**

### Fire 37 — 2026-09-19 — TEMPLATE sweep (Newsletter): 404-on-subscribe wrong-endpoint + missing-siteId fixed at root (dual CODE-QUALITY + EMBARRASSINGLY-EASY)
- **Verify-before-implement:** git clean (own files; only guardrailed `scheduled_tasks.json` + non-mine `_ADMIN_CONTRACT_REPORT.json` dirty) · prod 200 · read the sweep ledger (36 fires; worker services converged Fire 34, template swept from Fire 35). Build-LLM credit dead → the **template repo is the creditless-verifiable target** (vitest + tsc + build + real-browser, ships via git push). Scanned EVERY template `fetch('/api/…')` literal for wrong-endpoint siblings of Fire 35's ContactForm bug.
- **★ ROOT FIX (correctness — 404 + 400 on a primary conversion surface) — BOTH Newsletter components:** `sections/Newsletter.tsx` (the WIRED one, in the `sections` barrel) AND `components/Newsletter.tsx` (an orphan, no importer) POSTed `{email}` to **`/api/newsletter` — a route with NO Worker handler (404)** — and omitted the **`siteId` the real route REQUIRES**. The live route is `contactNewsletter.post('/api/newsletter/subscribe', …)` with `newsletterSubscribeSchema = { email, siteId (id OR slug, min 1), segment? }` (`libs/features/contact_newsletter/handlers.ts:294`). So even had it hit the right URL it would 400 (`siteId is required`). **Every real newsletter subscribe silently failed** (double-opt-in confirm never sent → the owner's subscriber never lands in `newsletter_subscribers`). Same wrong-endpoint class as ContactForm (Fire 35).
- **★ ROOT WIRE (DRY the site-identity derivation) — new `src/lib/siteSlug.ts`:** the canonical `siteSlug()` (+ `appJsAttr()`) — injected app.js `data-slug` wins, else `location.hostname` first label, else `'site'`, never throws — was PRIVATE in `PageviewBeacon.tsx`. Newsletter became its 3rd consumer → **extracted to a shared lib** (per inverted-abstraction-pyramid: write twice then extract). `PageviewBeacon` now imports it (behavior byte-identical). Both Newsletter fetches now send `{ email, siteId: siteId || siteSlug() }` to `/api/newsletter/subscribe` (an explicit `siteId` prop still overrides for callers/tests).
- **★ PAIRED VALUE-ADD (embarrassingly-easy — plain language over jargon):** the error branch surfaced the raw `err.message` ("Failed to fetch" / "Subscription failed") to a VISITOR. Now a plain, reassuring line — *"Couldn’t subscribe just now — please check your email and try again."* — and the typed email is KEPT (only success clears it) so they retry without re-typing.
- **★ RED→GREEN (+11; Newsletter had ZERO test coverage — a missing-test gap closed):** NEW `siteSlug.test.ts` (6 — `data-slug` precedence, hostname fallback, empty-attr fall-through, never-throw) + NEW `sections/Newsletter.test.tsx` (2 — renders the REAL component, asserts exactly ONE POST to `/api/newsletter/subscribe` with body `{email, siteId}` [RED on the old `/api/newsletter` + `{email}`], + the friendly-error/kept-email path) + PageviewBeacon 3 (unchanged, proves the refactor is behavior-identical). Full template suite **436→445 green** (54 files), tsc -b exit 0, `npm run build` clean (19 routes validated), prettier clean.
- **★ REAL-BROWSER PROOF (local preview of the built dist — creditless):** `/` + `/blog` both render (root populated, H1s "Your Business" / "Notes from the field"), **0 console errors**, and **PageviewBeacon's extracted `siteSlug()` fires `/api/events` with `siteId` after a client-side nav** — the shared-module extraction boots identically in the real bundle. (The Newsletter component itself is a library section NOT placed on the demo page, so its authoritative gate is the component-render unit test — a real `<Newsletter>` render asserting the real endpoint/payload — exactly the creditless-library gate this sweep uses; a live generated-site render awaits build-LLM credit.)
- **★ SPILLOVER (docs-drift, a sweep dimension) — `Home.tsx` stale JSDoc:** it still said ContactForm "POSTs to `/api/contact/{slug}`" — the exact wrong endpoint Fire 35 fixed to `/api/contact-form/{slug}`. Corrected (the stale comment could mislead someone back into the bug).
- **Files mine (template repo, pushed):** `src/lib/siteSlug.ts` + `siteSlug.test.ts` (NEW) · `src/components/sections/Newsletter.tsx` + `Newsletter.test.tsx` (NEW) · `src/components/Newsletter.tsx` · `src/components/PageviewBeacon.tsx` · `src/pages/Home.tsx` — commits `e06d69e` (fix) + `5f2f757` (doc). **(main repo):** `_CODE_SWEEP_LEDGER.md`. Lands on next build (NO redeploy). Protected files untouched (feed/sitemap build churn left unstaged).
- **Next:** template conversion-form sweep continues — `/contact` (`Contact.tsx`) hand-rolled `<form>` vs the reusable `<ContactForm>` consolidation (tracked from Fire 35); `AiChat` (`/api/ai/chat`) + `PageAudio` (`/api/page-audio/:slug`) endpoint-verify; then the remaining un-swept sections (BentoGrid, Testimonials, StickyActionBar, GoogleMapEmbed a11y). Worker-side tracked: `content_import.ts` wire-or-remove · `domains.ts` registrar · `api.ts` route-table JSDoc drift.

### Fire 38 — 2026-09-19 — TEMPLATE sweep (ContactForm/QuoteForm slug): 'default' → siteSlug() (React fallback 404'd); AiChat verified clean
- **Verify-before-implement:** git clean (own files) · prod 200. Continued Fire 37's conversion-form endpoint sweep — scanned the remaining template `/api/…` consumers (AiChat, PageAudio, Contact) against the REAL Worker routes.
- **★ VERIFIED CLEAN (validator-precision — 2 of 3 needed no change):** `PageAudio` → `POST /api/page-audio/:slug` EXISTS (`api.ts:3771`) ✓. `Contact.tsx` already renders `<ContactForm />` (line 52) — the Fire-35-tracked "`/contact` hand-rolled form vs `<ContactForm>` consolidation" is **ALREADY DONE** (item CLOSED). `AiChat` → `/api/ai/chat` has NO Worker route, BUT it's **Gallery-demo-only** (placed only on `Gallery.tsx`, backed by the template's own Pages Function `functions/api/ai/chat.ts`) — NOT shipped to Worker-served generated sites, matching the `[[ai-concierge-exists-gallery-only-was-502-dead-model]]` memory. No fix (correct-by-design demo feature) — item CLOSED.
- **★ ROOT FIX (correctness — the SAME latent wrong-slug class as Newsletter Fire 37) — `ContactForm.tsx` + `QuoteForm.tsx` default `slug='default'`:** both forms defaulted the slug to the LITERAL `'default'`, so the React-fallback submit path (reached whenever app.js's capture-phase hijack is absent — local preview, Storybook, a failed inject) POSTed to **`/api/contact-form/default` — a 404** (the Worker handler does `SELECT … WHERE slug = ?` and no site is named 'default', `contact_newsletter/handlers.ts:76`). Every fallback submission silently failed. Fixed by resolving to the REAL site identity via the shared `siteSlug()` (Fire 37's extracted helper: injected `data-slug` wins, else the hostname's first label) — `slug || siteSlug()` in the hidden `__slug` input + the module-level `submit()` fallback (ContactForm) and the URL builder (QuoteForm). An explicit `slug`/`endpoint` prop still wins; the app.js path is unaffected (or improved, since `__slug` now carries the real slug).
- **★ RED→GREEN (+2):** a slug-less-render regression in EACH form's existing test — inject an app.js `data-slug`, render `<ContactForm />` / `<QuoteForm />` with NO slug prop, submit, assert the POST hits `/api/contact-form/{real-slug}` and **NOT** `/api/contact-form/default` (RED on the old literal). Full template suite **445→447 green** (54 files), tsc -b exit 0, `npm run build` clean (19 routes).
- **★ REAL-BROWSER PROOF (local preview of the built dist — ContactForm IS on `/contact`, unlike Newsletter):** loaded `/contact`, filled name/email/message, submitted → the POST hit **`/api/contact-form/localhost`** (the real `siteSlug()` on the localhost preview) — **NOT** the dead `/api/contact-form/default` — with **0 console errors**. End-to-end proof in the real bundle.
- **Files mine (template repo, pushed `a581204`):** `src/components/ContactForm.tsx` + `ContactForm.test.tsx` · `src/components/QuoteForm.tsx` + `QuoteForm.test.tsx`. **(main repo):** `_CODE_SWEEP_LEDGER.md`. Lands on next build (NO redeploy). Protected files untouched.
- **Next:** template conversion path is now endpoint-clean across Newsletter (Fire 37) + Contact/Quote (Fire 38). Remaining un-swept template sections: `BentoGrid` · `Testimonials` · `StickyActionBar` (mobile click-to-call) · `GoogleMapEmbed` (iframe `title` a11y) · `SocialLinks`/`AddressLink` (NAP). Worker-side tracked: `content_import.ts` wire-or-remove · `domains.ts` registrar · `api.ts` route-table JSDoc drift.

### Fire 39 — 2026-09-19 — TEMPLATE sweep (click-to-call integrity): shared telHref() kills dead tel: buttons across 4 consumers (dual CODE-QUALITY + EMBARRASSINGLY-EASY)
- **Verify-before-implement:** git clean (own files) · prod 200. Continued the conversion/NAP sweep (Fire 37/38 Next) — the un-swept `tel:` consumers (StickyActionBar/Footer/ExitIntentOffer/ServiceMenu/LocationMap), none had tests.
- **★ ROOT FIX (correctness + embarrassingly-easy "never a doomed control" + DRY) — dead click-to-call:** 4 consumers built the tel: href as `phone ? `tel:${phone.replace(/[^+\d]/g,'')}` : ''` (Footer: `: '#'`) with **NO digit-count guard**, so a filled-but-digitless phone (`pick()` nulls `{token}`s, but "Call for hours" / a partial / "555-CALL" pass it) rendered a **dead Call button** — `href="tel:"` / `href="#"` / `tel:555` that dials nothing (a doomed control; the busy owner's visitor taps Call → nothing). `LocationMap` ALREADY had the correct `≥7 digits` guard — so the DRY win is real: extracted its logic into shared **`src/lib/phone.ts`** (`telHref`/`telDigits`: a dialable `tel:` for a real phone — present, not `{token}`, ≥7 digits — else `''`) and routed all 4 weak consumers through it (per inverted-abstraction-pyramid: the thing written-5-times, one of them correctly, is now the one shared thing). Footer's phone ROW now gates on the valid `phoneHref` (not raw `phone`), so a digitless phone hides the row instead of a dead `href="#"`. ExitIntentOffer's exit CTA falls through to email/contact when the phone isn't dialable. ServiceMenu's `cta=''`→button hides.
- **★ RED→GREEN (+4; these had ZERO coverage):** NEW `src/lib/phone.test.ts` — `telHref('(415) 555-0123')→'tel:4155550123'`, `+1 415 555 0123`→`tel:+14155550123`, `555-1234`(7-digit min)→valid; and the dead-control regressions `'Call for hours'`/`'555-CALL'`/`'{PHONE}'`/`123456`(<7)/undefined → `''` (RED on the old ungated code). Full template suite **454→458 green** (56 files) — the existing `ExitIntentOffer.test`'s `deriveOffer` still green through the new helper — tsc -b 0, `npm run build` clean (19 routes).
- **★ REAL-BROWSER PROOF (local preview @390 mobile):** scrolled past the hero to reveal StickyActionBar → **0 `tel:` anchors, 0 dead controls** (no `tel:`-empty / `#`-phone), no sticky Call button (correctly ABSENT for the demo's placeholder phone rather than a dead one), **0 console errors**. The digitless-phone bug path is authoritatively locked by the unit test (a demo phone can't exercise it).
- **Files mine (template repo, pushed `3c97bb6`):** `src/lib/phone.ts` + `phone.test.ts` (NEW) · `src/components/StickyActionBar.tsx` · `Footer.tsx` · `ExitIntentOffer.tsx` · `sections/ServiceMenu.tsx`. **(main repo):** `_CODE_SWEEP_LEDGER.md`. Lands on next build (NO redeploy). Protected files untouched. LocationMap left as-is (it was the CORRECT reference the helper generalizes — no bug, no churn).
- **Next:** click-to-call is now integrity-guarded fleet-wide. Remaining un-swept template conversion/NAP: `SocialLinks`/`AddressLink` (dead-link/aria) · `GoogleMapEmbed` (already titled — verify src validity) · `TrustBar` (phone chip). Worker-side tracked: `content_import.ts` wire-or-remove · `domains.ts` registrar · `api.ts` JSDoc drift.

### Fire 40 — 2026-09-19 — TEMPLATE sweep (NAPFooter NAP integrity): completes Fire 39 — no dead address/phone/email/social controls (dual CODE-QUALITY + EMBARRASSINGLY-EASY)
- **Verify-before-implement:** git clean (own files) · prod 200. Swept the remaining conversion/NAP components (Fire 39 Next). `SocialLinks`/`AddressLink` are orphans (no callers). The REAL wired local-business surface is `NAPFooter` (LocalBusiness microdata) — it has a maintained test but **no page consumer**, so its component-render tests are the authoritative gate.
- **★ ROOT FIX (a CLUSTER of the dead-control class — one component, 4 dead controls) — `local/NAPFooter.tsx`:** it UNCONDITIONALLY rendered every NAP contact link, so placeholder/empty data shipped dead controls + token leaks: (1) **address** → a maps `destination=` link on ANY value (broken `destination=%7BADDRESS%7D` + leaked `{ADDRESS}` text); (2) **phone** → `tel:${phone.replace(/[^+\d]/g,'')}` with **NO digit guard** — the EXACT Fire 39 class; **NAPFooter is a 5th click-to-call consumer Fire 39 missed** (a digitless phone → `href="tel:"` dialing nothing); (3) **email** → `mailto:${email}` on any value (dead `mailto:` when empty, `mailto:{EMAIL}` on a token); (4) **social** → EVERY link incl. `url:'#'` (its OWN stories pass `{platform:'facebook', url:'#'}` → a dead social link). Fixed by fail-safing each on real data (mirrors LocationMap's guards + the shared `telHref()`): address gated on `!startsWith('{') && ≥6 chars`, phone through `telHref()` (row hides unless dialable), email on `includes('@') && !{token}`, social filtered to `url.startsWith('http')` — and the "Connect With Us" heading hides when no real socials survive (no empty section).
- **★ RED→GREEN (+2 on the existing test):** the current test used fully-valid props (all controls still render → backward-compatible, 4/4 still green). Added a placeholder block: all-placeholder (`{ADDRESS}`/`"Call for hours"`/`{EMAIL}`/`url:'#'`) → **0 tel:, 0 mailto:, 0 maps/dir, 0 `href="#"`, no `{TOKEN}` text, no "Connect With Us" heading**; + a mixed set (real phone only → `tel:4155550123`, token address/email omitted). NAPFooter **4→6 green**; full template suite **458→460** (56 files), tsc -b 0, `npm run build` clean (19 routes).
- **Files mine (template repo, pushed `84fe604`):** `src/components/local/NAPFooter.tsx` · `NAPFooter.test.tsx`. **(main repo):** `_CODE_SWEEP_LEDGER.md`. Lands next build (NO redeploy). Protected files untouched. `SocialLinks`/`AddressLink` (orphans) left as-is; `SocialLink`/`AddressLink` are dumb leaves whose lone concern is the caller — NAPFooter (the real consumer) now guards.
- **★ Click-to-call class now COMPLETE fleet-wide:** all 6 tel: consumers guarded (StickyActionBar/Footer/ExitIntentOffer/ServiceMenu Fire 39 + LocationMap already-correct + NAPFooter Fire 40) — no template surface renders a dead click-to-call.
- **Next:** conversion/NAP dead-control class swept. Remaining un-swept template: `TrustBar` (phone/address CHIPS are display-only labels — lower sev, verify no `{token}` leak) · `GoogleMapEmbed` src-validity. Worker-side tracked: `content_import.ts` wire-or-remove · `domains.ts` registrar · `api.ts` JSDoc drift.
